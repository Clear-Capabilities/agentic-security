{
  "language": "js",
  "dataClass": [],
  "sourceCategory": "storage-read",
  "sinkCategory": "email",
  "expectedProtection": null,
  "expectedTransformKind": null,
  "tier": "capability",
  "notes": "MEASURED, DISCLOSED LIMITATION — EXPECTED to report FAIL, hence capability-tier. `storage-read`'s only catalog entries are Python/C++ (`py-open-read`, `cpp-read`, `cpp-fread`, `cpp-fgets`); runner.mjs parses every fixture as JS, so `_languageAllowed` discards them and no source node with subtype 'storage-read' can exist. Verified live: the graph contains only the email sink node (external/email, modeled, with a coverageReason) and ZERO dataElements. A SECOND, independent reason this shape is unscoreable even in a Python-capable runner: `open(path)` is a bare call assigned straight to a variable, so source-seeding.js's seedPathFor finds no enclosing member access and accessPathOf returns null — the match lands in planSeeds' `unseedable[]`, exactly the shape scanner/test/lineage/source-seeding.test.js's E2/4 pins. `dataClass` is [] because a file handle has no field name to classify. See js-http-upload-to-object-storage-phi's notes for the shared root cause and the F3+ deferral."
}
