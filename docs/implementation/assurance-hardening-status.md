# Assurance Hardening — Status

**Last updated:** 2026-08-23 (cycle 1)
**PRD:** `agentic-security-assurance-hardening-prd.md` (v1.0, stated baseline commit `248c4cf`, scanner 0.141.0)
**Repo now at:** commit `f57f496`, scanner 0.143.0 — two versions ahead of the PRD's stated baseline. Evidence-table line numbers in the PRD are being re-verified against current HEAD before any edit relies on them.

## Milestone progress

| Milestone | Status | Notes |
|---|---|---|
| M0: Contracts & instrumentation | in_progress | State files created; background verification of PRD evidence table (A-01..A-11) against current HEAD dispatched |
| M1: Assurance core | pending | Blocked on M0 exit |
| M2: Verified remediation & privacy | pending | Blocked on M1 |
| M3: Governance | pending | Blocked on M2 |
| M4: Enterprise scale | pending | Blocked on M3 |

## Requirement counts by status

(See `assurance-hardening-state.json` for the authoritative per-FR table.)

- verified: 12 (FR-101, FR-102, FR-103, FR-104, FR-105, FR-106, FR-206, FR-301, FR-401, FR-501-scoped, FR-701, FR-703)
- ready (evidence gathered, not yet implemented): 5 (FR-201, FR-202, FR-205, FR-601, FR-801)
- pending (not yet reached): 56
- implemented / blocked / not_applicable: 0
- **total: 73 FR-* requirements** (correcting this doc's earlier "66" — 73 is the accurate count per the PRD's 10 epics)

## Cycle log

**Cycle 1:** Read PRD in full. Read `CLAUDE.md`, `scanner/CLAUDE.md`, `scanner/src/mcp/CLAUDE.md`, `scanner/src/posture/CLAUDE.md`, `scanner/src/dataflow/CLAUDE.md`. Created `docs/implementation/` state files. Dispatched a background research agent to verify PRD evidence items A-01..A-12 against current HEAD. Recorded decision D-0003: extend existing mature infrastructure (`verification-separation.js`, `falsification.js`, `privacy-framework.js`, `scan-checkpoint.js`, `autopilot.js`) rather than build a parallel system.

**Cycle 2:** Verification agent confirmed all A-01..A-12 conditions with precise file:line evidence at HEAD `f57f496`; recorded per-FR in state.json (16 FRs moved pending->ready). Two conditions found more nuanced than the PRD states: A-05 (CI deep-mode) already has a working override + partial notice; A-11 (risk dollars) already has per-org `impactUSD` config, just not `familyBaseProb`. Implemented **FR-206** (scanHealth, additive): new `scanner/src/pipeline/scan-health.js`, wired into `engine.js`'s final return object and `report/index.js`'s `toJSON` (S7 convention). As a side effect, closed the one real remaining gap in A-05 (deep mode silently downgrading in CI with zero signal when never explicitly requested) by adding an explicit `deepAnalysis` status descriptor. 10 new tests in `test/scan-health.test.js`, all scoped suites green (smoke, deep-taint, lifecycle guards). Full `npm test` gate run in progress.

**Cycle 3-4:** Implemented **FR-105** (versioned finding schema). Discovered `report/index.js#normalizeFindings()` is already a comprehensive 4-channel canonical merge with an honest-null field allowlist, called by every JSON/SARIF/HTML/CSV/JUnit/OSCAL output path — narrowing FR-105's real gap to one place: `mcp/tools.js`'s `scan_diff` hand-rolled a divergent 3-channel merge missing `supplyChain` entirely. Fixed it to call `normalizeFindings()`. Documented honestly that this does NOT close scan_diff's SCA-detection gap end-to-end (a separate, deeper `depFileContents` wiring limitation remains, out of FR-105's scope). Created `scanner/src/pipeline/finding-schema.js` (versioned, observability-only, Milestone 0 scope). 6 new tests including a drift guard against a real scan. Full gate green (3980/3980).

**Cycle 4-5:** Implemented **FR-106** (Promise-aware annotator runner). Extracted `_runAnnotator`'s logic into `scanner/src/pipeline/annotator-runner.js#runAnnotatorAsync` (the fix is `await fn()` inside the try, not `return fn()` — the latter let an async callback's rejection escape the catch entirely). Added `await` to all 51 call sites in `engine.js` (proven syntactically valid via `node --check`, proven order-preserving by grep confirming none capture the return value). Since this codebase's own annotators are deliberately built never to throw (posture/CLAUDE.md), extracted the logic into a standalone module specifically so the reject-is-captured contract could be fault-injection tested directly — 6 new tests including one with a live `unhandledRejection` listener proving the original bug class is closed. Highest blast-radius change so far (touches the pipeline every finding passes through); full gate green (3986/3986), no flakes.

**Cycle 5-6:** Implemented **FR-701/FR-703** (registry-driven reset). Built `posture/artifact-registry.js` from a full audit of every `statePath()`/`stateDir()` call site — corrected 5 items the PRD's own A-10 evidence had wrongly assumed were generated-output gaps (they're actually operator/agent-authored inputs: `logic-claims.json`, `current-intent.md`, `exploit-history.jsonl`, `cve-alerts.json`, `network-policy.json` — misclassifying these as "generated" would have made `reset` delete real input data). Built a drift-preventing completeness-guard test that caught 4 more real gaps my manual audit missed on the first pass. Rewired `cmdReset` to the registry. 12 new tests including 4 real end-to-end CLI subprocess tests. Full gate green (3998/3998).

**Cycle 6-7:** Implemented **FR-501 (scoped)**. `auditor-walkthrough.js`'s shared `evaluateFramework()` (used by ALL 9 bundled frameworks) turned out much more sophisticated than A-07's evidence table implied — already 4 statuses, already caps non-code-testable controls, already discloses unmapped families. The real, narrow bug: the family-mapping "open finding" filter only counted critical/high severity, so any number of open MEDIUM findings on a mapped family rendered a vacuous "✓ clean" pass — the same failure mode `privacy-framework.js`'s own header comment already names. Fixed with a named severity-rank floor (medium+, not low+ — matches FR-502's own wording). 6 new tests; full `test:posture` (1641/1641) and full gate (4004/4004) green. NOT full FR-501 scope: the six-state model's `accepted_exception`/`stale` states remain unimplemented (separate, larger exception/expiry design surface).

**Cycle 7-8:** Implemented **FR-301** (ApplyFixService unification) — the highest-risk cycle so far. Found the CLI's `fix --apply` (`bin/agentic-security.js`) was WORSE than the PRD's A-08 evidence described: it WARNED (didn't refuse) on failed scan-integrity, and had NO path confinement or reserved-write check at all — a genuine traversal/symlink-escape exposure, not just a missing-verification gap. Built `scanner/src/fix/apply-fix-service.js` (matches the PRD's own proposed module path) with confinement logic extracted verbatim from `mcp/tools.js` (not rewritten, to avoid drift), and routed both the CLI and MCP's previously-unverified stored-replacement branch through it. Two existing tests that explicitly encoded the old unverified-write behavior (synthetic findings with no `stableId`) were deliberately updated to genuinely-verifiable fixtures — documented as a justified strengthening, not a weakened control. Rebuilt the bundle and confirmed it smoke-tests clean. Full gate green (4013/4013).

**Cycle 8-9 (new session, resumed from prior state per the loop's own "reconcile at start of every session" instruction):** Corrected a prioritization error from the prior session's handoff — the PRD's dependency order (§19: "E1 must precede final E2, E4, and E5 integration") means Milestone 1's canonical pipeline (FR-101-104) should come before Milestone 2's FR-401, not after. Implemented **FR-101/102/103/104** together as one tightly-coupled group — the highest-risk cycle of the whole session. Built a producer registry + collector (scoped to the 12 "late producers" A-03 evidenced — cross-language, IAM, container, business-logic, spec-drift, concurrency, privacy-taint — not the ~90 core detector call sites, a separate much larger undertaking documented as explicitly out of scope), an enrichment-completion gap-filling pass (proven via a real scan: every finding now has a stableId, where late producers previously had none), and `Object.freeze(finalFindings)` after the last append site (proven via a real scan: pushing after finalization throws TypeError).

Adversarial self-review (the loop's own Step 6) caught a real regression: freezing broke `posture/deterministic.js`'s in-place `.sort()` on `scan.findings` — found via the full test gate (11 failing determinism tests), root-caused by direct manual reproduction, fixed by building a new sorted array instead of sorting in place (decision D-0005). The self-scan gate (scanning the scanner's own source) then caught a second real issue in a *previous* cycle's file (`fix/apply-fix-service.js`, FR-301): a genuine TOCTOU (check-then-read race) plus one low-severity, codebase-convention-consistent finding, reviewed and accepted into the baseline. Final verification: full test gate clean (one recurring, isolation-confirmed-unrelated flake — D-0006), corpus gate 215/215 zero drift, self-scan gate clean, bundle rebuilt and smoke-tested.

**Cycle 9-10:** Implemented **FR-401** (privacy IR adapter). Read `dataflow/privacy-taint.js` in full before touching anything: its actual implementation is a self-contained, name-based heuristic (not calling into the real taint engine at all), needing only a flat per-file `{decls, calls}` shape — simpler than feared, but genuinely structurally mismatched from the real Layer-1 IR (per-function/CFG, not flat; structured expression trees, not text). Verified the real IR's shape empirically (`parseJsFile` on a live snippet, not just trusting the docs) before writing the adapter. Built `src/privacy/ir-adapter.js`, proven end-to-end against real parser output. Made a material, documented design decision: since privacy analysis runs by default but the real IR is normally only built for deep-mode scans, the adapter reuses the IR for free when deep mode already built it and stays honest (`irBacked` flag) about the degraded path otherwise, rather than forcing a new unbudgeted parsing cost onto every default scan or silently claiming equal coverage. Along the way, found and fixed an unrelated documentation gap from an earlier cycle (`artifact-registry.js` undocumented in `posture/CLAUDE.md`) and hit a 5th instance of the known combined-run flake class (D-0006) plus one genuine environment-fault corpus-gate retry (Python subprocess timeout under load, resolved on retry). Full gate, corpus gate (215/215), self-scan gate, build, and smoke all clean.

**Cycle 10-11:** Implemented **FR-801/FR-802/FR-804** (risk-dollars.js scenario honesty + confidence-weight bug). Rewrote `_loadConfig`'s fragile multi-line regex as a line-based parser after finding a genuine pre-existing bug (silently dropped every impactUSD/familyBaseProb entry after the first — found while testing the new `familyBaseProb` override with multiple entries). Added `_scenarioDisclosure`, wired into both the per-finding `riskDollars.scenarioStatus` field and `commands/labs.md`'s real CLI `--risk-dollars` output — the only actual rendering surface for this data. Fixed FR-804's exact described bug in the same function: `Math.max(0.4, f.confidence || 0.8)` inflated a real confidence of 0 to 0.8 (`||` treats 0 as falsy) and floored every other low confidence up to 0.4 unconditionally; replaced with `typeof f.confidence === 'number' ? f.confidence : 0.8`. 30 new tests across `risk-scenario-disclosure.test.js` (13) and `privacy-ir-adapter.test.js` (17, carried over from FR-401 verification). While closing out verification, found and fixed a doc-drift regression from an earlier cycle's `posture/CLAUDE.md` edit (5 runtime-state filenames referenced without the `.agentic-security/` prefix the doc-drift checker expects — one-line fix, no changes to the shared checker script). Full gate run: npm test 4065/4066 (the one failure, `test/posture-command.test.js`, confirmed standalone-clean 6/6 in 5.06s — ninth instance of the D-0006 combined-run flake class, one of the four files that decision originally named). Corpus gate 215/215 zero drift. Self-scan gate: one reviewed, intentional drift (`posture/risk-dollars.js` 4→3, confirmed via direct scan to be exactly the eliminated confidence-floor anti-pattern) plus one previously-uncaptured low-severity finding on an earlier cycle's new file (`fix/apply-fix-service.js`), baseline updated. Build and smoke both clean. FR-801 and FR-804 marked `verified`.

**Cycle 11-12:** Orient step re-evaluated FR-205 (the apparent next-smallest item) and found its recorded evidence understated its real scope: its PRD acceptance criterion needs the `advisory`/`standard`/`strict` assurance-mode machinery FR-204 introduces, which itself depends on FR-203→FR-201/202 — the same architecture chain already flagged as needing its own design pass. Corrected `dependsOn` rather than attempting a partial fix (decision D-0008) and selected **FR-601** (egress policy) instead. A dispatched research pass found the real scope was larger than A-09's single evidenced site: 7 fetch() call sites across 6 files in 4 independent code paths, only 2 with any pre-existing shared chokepoint — the same "scattered append sites" shape FR-101-104 solved for producers. Built `src/egress/policy.js` (`evaluateEgress`: mode allow/deny/local-only + provider allow/deny lists, default-allow to preserve existing behavior, decision object deliberately metadata-only per the PRD's "sanitized decision without retaining source" requirement) and wired all 7 sites through it, each gated before its own prompt-construction code runs. Built a completeness-guard test (`egress-policy-completeness.test.js`) directly modeled on FR-701's artifact-registry guard — the correct anti-drift mechanism for a scattered-call-site problem. 31 new tests total (policy unit tests, the completeness guard, and integration tests proving all 7 sites produce ZERO network calls when denied — the literal FR-601 acceptance criterion, not just a policy-function-in-isolation proof).

Two real issues surfaced during verification. First, a genuine TOCTOU in `policy.js`'s own config loader (found by self-scan, same bug class and fix pattern as `apply-fix-service.js` earlier this session). Second, a self-inflicted mistake: cleaning up widespread stray scan state that had accumulated in 66+ `test/fixtures/*/.agentic-security/` directories (from `npm run smoke` and full-suite runs scanning fixtures in place, corrupting "no prior scan" tests) via a bulk `find`/`rm`, which also deleted 2 GIT-TRACKED fixture files. Caught immediately by the next gate run, restored via `git checkout --`, documented in decision D-0009 with a standing lesson against pattern-based bulk deletes under `test/fixtures/`. That same investigation also found the true root cause of decision D-0007's previously "undiagnosed" test failures from earlier this same day — not a pre-existing code bug as originally concluded, but the identical stray-fixture-state mechanism (`git stash` cannot remove gitignored untracked files, which is exactly why D-0007's stash-based falsification never found it). D-0007 corrected in place. Final gate: npm test 4091/4091 + 26/26 with zero failures (not even a D-0006 flake), corpus gate 215/215 zero drift, self-scan gate clean after 3 reviewed drifts (one expected new-code finding, one confirmed-unrelated whole-tree cross-file count shift, one genuine reduction from the security fix itself), build clean, smoke exit 3 (correctly reflecting the vulnerable fixture's real findings). FR-601 marked `verified`.

## Known limitations / open items

- 15 of 73 FRs verified (`FR-101,102,103,104,105,106,206,301,401,501,601,701,703,801,804`). This is cycle 12 of up to 100.
- This PRD is scoped by its own authors at "two security/backend engineers + one data-flow engineer, several sprints per milestone." A single autonomous session will not complete all 73 FRs to `verified` status; expect this to span many sessions, checkpointed via these state files.
- FR-206's `analyzers` field is intentionally `null` (honest, not fabricated) until FR-201 (per-analyzer isolation) exists — see `scan-health.js`'s own comment.
- FR-802's fuller organizational-input schema (organizationScale/industry/recordCount/controlStrength) remains unimplemented; FR-801 makes its absence honestly visible rather than building it. FR-803/805/806 remain unimplemented.
- FR-601's egress chokepoint exists but FR-602 (constraint dimensions), FR-603 (payload redaction, only pre-existing for llm-validator's own path), FR-604 (persistent audit logging), and the fuller FR-605 anti-smuggling criteria beyond literal-loopback remain unimplemented — each is bounded future work building on `egress/policy.js` rather than greenfield.
- FR-205 is 'ready' but effectively blocked on the FR-201→202→203→204 chain (decision D-0008) — do not attempt a partial fix against its acceptance criterion.
- The D-0006 combined-run test-infrastructure flake has now recurred 9 times across 6+ distinct test files, always clean standalone. Recommended (not actioned, outside this session's authority) as a candidate for CI-infrastructure-owner investigation: longer timeouts for CLI-subprocess-spawning tests, or reduced default test-runner concurrency.
- `test/fixtures/**/.agentic-security/` accumulates stray scan state from `npm run smoke` and full-suite runs (decision D-0009, recurred a second time this same session in the FR-302/303 cycle) — wipe with `find test/fixtures -type d -name .agentic-security -not -path "*/custom-rules/*" -not -path "*/sca-suppression/*" -prune -exec rm -rf {} +` before trusting a "no prior scan" test result. NEVER bulk-delete under `test/fixtures/` without first running `git ls-files test/fixtures/ | grep '\.agentic-security/'` to get the authoritative tracked-file list, and `git status --short . | grep '^ D'` afterward to confirm nothing tracked was touched.

**Cycle 12-13:** Implemented **FR-302/FR-303** (ApplyFixService's fail-closed integrity + uniform confinement) as one tightly-coupled group, both landing in the same shared service built during FR-301. Traced both real apply entry points line-by-line and found the acceptance criteria were ALREADY behaviorally satisfied (CLI and MCP each independently re-implement their own fail-closed integrity check before calling the shared service) — but found a real, if minor, drift risk: `apply-fix-service.js`'s own `readVerifiedScan()` function (built during FR-301, docstring claiming fail-closed behavior) had ZERO real callers anywhere in the codebase, meaning the shared service was only safe because both of its current callers happen to check first, not because it enforces the property itself. Wired `readVerifiedScan` into `applyVerifiedFix` as a genuine defense-in-depth check (refuses unless status is exactly 'verified', evaluated before path confinement or any write), proven via 4 new direct unit tests that bypass both callers' pre-checks entirely. For FR-303, found the underlying `confinePath`/`isReservedWritePath` logic was already fully correct and exhaustively tested on the MCP path (including a contract-driven test walking `_CONFINEMENT.md`'s full reserved list) but the CLI path only had one reserved-path example (package.json) — added 3 new CLI tests for the specific classes the acceptance criteria name (.git/, .agentic-security/, node_modules/) plus 9 new direct unit tests for the primitives themselves in a new `test/apply-fix-service.test.js` (the module's first-ever direct unit test file — all prior coverage was indirect via CLI/MCP). Two of the new unit tests initially failed from test-setup bugs of my own (a macOS `/var` vs `/private/var` realpath mismatch from calling `isReservedWritePath` without first routing through `confinePath`, and a missing `package.json` project marker in the test fixture) — both diagnosed and fixed directly, not worked around. The D-0009 stray-fixture-state issue recurred a second time this same session during verification; wiped correctly using the lesson from its first occurrence. Full gate: npm test 4106/4106 + 26/26 zero failures, corpus gate 215/215 zero drift, self-scan gate zero drift (no findings added or removed), build/smoke clean.

## Known limitations / open items (updated)

**Cycle 13-14:** Implemented **FR-107** (preserve raw producer evidence separately from derived annotations), the last small item in E1/Milestone 1. Found `posture/why-fired.js`'s `annotateWhyFired` already wired into `engine.js`'s default pipeline and already capturing genuine detector-observed evidence (sink/source snippets, path steps, sanitizers) — but nothing formally connected that to FR-107's acceptance criterion, and a real finding carries ~90 flat fields with no structural way to tell observed from inferred without prior knowledge of every field name. Built `pipeline/evidence-provenance.js` (`describeEvidenceProvenance`) using a deliberate allowlist design: a small, stable set of detector-set identity/location fields is named explicitly; everything else non-null falls into `derived` by construction, so new annotator fields are correctly classified with zero future maintenance — the same anti-drift lesson this session has now applied to producers, artifacts, egress call sites, and finding evidence. Proved against a real scan (not synthetic) that confidence/severity/exploitability/riskDollars — the exact fields the PRD names as "inferred" — are never misclassified as observed. Additive only, same Milestone-1-observability posture as FR-105's `describeFindingCompleteness`, no engine.js pipeline changes. D-0009's stray-fixture-state pollution recurred a THIRD time this session (a new contributing source identified: this cycle's own real-scan test, following the same pre-existing pattern `finding-schema.test.js` already used) — wiped correctly again; flagged a third time in decisions.md, still correctly out of scope to fix unilaterally. Full gate: npm test 4115/4115 + 26/26, corpus 215/215, self-scan zero drift, build/smoke clean.

## Known limitations / open items (updated)

**Cycle 14-15:** Implemented **FR-304** (fresh rescan proving stableId gone + changed-file detection). Confirmed the "fresh rescan" half was already fully satisfied by `verifyFixCore`/`verifyPatch` (no verification-result caching exists anywhere — every apply always re-verifies fresh). Found the genuinely unaddressed half: verification takes real, measured wall-clock time (55-400ms+ even outside combined-suite load) during which the target file could be concurrently modified, and nothing detected that — a silent-clobber gap. Added `hashFileContentSync`/`detectConcurrentModification` to `apply-fix-service.js`: every file's content is hashed before verification starts, all files are re-checked against that baseline before any write (atomically — the whole batch refuses if any one file drifted, not a partial write), covering both an existing file being edited and a not-yet-existent file being concurrently created. A first draft of the test suite raced a real file edit against real verification timing and immediately proved itself flaky (measured 55ms vs 403ms for near-identical scenarios, 7x variance) — rather than tune a magic delay number, extracted the check into pure synchronous functions and tested those directly, 0.5-1ms per test, zero timing dependency (decision D-0010, a reusable lesson for testing any TOCTOU-style property in this codebase). The self-scan gate then caught a genuine TOCTOU in the FIRST draft of `hashFileContentSync` itself — the third instance this session of the exact same existsSync-then-readFileSync bug class — fixed using the same established read-first pattern. D-0009's stray-fixture-state pollution recurred a fourth time; wiped without incident using the by-now-routine safe recipe. Full gate: npm test 4121/4121 + 26/26, corpus 215/215, self-scan clean after one reviewed drift (a medium TOCTOU eliminated, replaced by one confirmed-benign low-severity false positive), build/smoke clean.

## Known limitations / open items (updated)

**Cycle 15-16:** Implemented **FR-305** (lint/test legs required; report skipped honestly). Found the lint and test legs already existed and already degraded gracefully (never fail-closed on a missing linter/runner) — but the overall `ok` boolean conflated "genuinely ran and passed" with "passed because it was silently skipped," with no field anywhere distinguishing them, and zero `verified_full`-style vocabulary existed in the codebase. Added `verifiedFull`/`degradedLegs` to `verifyFix`'s return (fix-verify.js), correctly treating "no linter configured at all" as N/A (nothing required) versus "linter configured but binary missing" as a genuine degradation, and wired the new field through `applyVerifiedFix` additively (without renaming the pre-existing `verified` field other tests already depend on). Found and fixed the most user-visible instance of the exact gap FR-305 describes: the CLI's `fix --apply` success message unconditionally claimed "lint" ran even when it was skipped or no test runner existed at all — now says "NOT fully verified" and names what was degraded. Same fix applied to MCP's `apply_fix` response and tool description. One test (`a configured-but-uninstalled linter degrades verifiedFull`) depends on `eslint` being absent from this environment's PATH — verified via `which eslint` before writing it, and asserted unconditionally rather than silently no-op'd if the premise ever breaks. Full gate: npm test 4126/4126 + 26/26 (after wiping D-0009's fixture state for a fifth time, and confirming a new D-0006-class flake in an unrelated file — `vuln-archaeology.test.js` — standalone-clean and unrelated to this cycle), corpus 215/215, self-scan zero drift, build/smoke clean.

## Known limitations / open items (updated)

- 20 of 73 FRs verified (`FR-101,102,103,104,105,106,107,206,301,302,303,304,305,401,501,601,701,703,801,804`). This is cycle 16 of up to 100.
**Cycle 16-17:** Implemented **FR-306** (transactional write with backup/rollback/post-write hash verification) — unlike the last four cycles, this one turned out to have REAL, substantial gaps, not just missing evidence. `fix-history.js`'s write was NOT atomic (opened the target directly in truncate mode — a crash mid-write left a partial file), had NO post-write hash verification (a write that didn't throw but produced wrong content went undetected), and a failed write only logged `status:'failed'` without restoring the file, with ZERO cross-file rollback for multi-file batches. Added `_writeAtomicAndSync` (temp-file + fsync + rename — the standard atomic-replacement pattern), post-write hash verification, automatic single-file rollback (correctly distinguishing "restore original content" from "delete a file that never existed before" via a new `fileExisted` parameter, verified as a genuinely distinct case with a dedicated test), and multi-file batch rollback in both real callers via a new `revertEntryById` targeted-revert primitive. Proved the actual FR-306 acceptance criterion — "injected write failure restores all files" — end-to-end through the real `applyVerifiedFix` path with a 2-file batch where the second file's write is fault-injected via a read-only subdirectory (chosen because it fails AFTER FR-304's own baseline read, unlike a directory-as-target trick which fails even earlier). Discovered and closed a genuine pre-existing gap while in this code: `undoLast`/`undoAll`/the CLI `undo` command had ZERO test coverage anywhere in the codebase — wrote 13 new direct tests. Full gate: npm test 4140/4140 + 26/26 (after wiping D-0009's fixture state a sixth time), corpus 215/215, self-scan zero drift, build/smoke clean.

## Known limitations / open items (updated)

- 21 of 73 FRs verified (`FR-101,102,103,104,105,106,107,206,301,302,303,304,305,306,401,501,601,701,703,801,804`). This is cycle 17 of up to 100.
**Cycle 17-18:** Implemented **FR-108** (compatibility adapter for legacy finding consumers), the last item in E1. Found no documented precedent for a finding-field rename in this project's history (CHANGELOG.md states "No breaking changes. All new capabilities are additive" three separate times) — but found a concrete, real, in-scope case rather than inventing one: this SAME session's own FR-804 cycle renamed `riskDollars.confidenceFloor` to `confidenceWeight`, breaking that additive-only convention with no backward-compat bridge. Built `pipeline/legacy-compat.js` (`LEGACY_FIELD_ALIASES` naming every known rename with a documented sunset date; `applyLegacyCompat` backfills the old field name from the new one's value without fabricating a phantom shell for findings that never had the new field at all; `legacyFieldDeprecationNotice` surfaces a single honest report-level notice, null in the steady state). Wired into `normalizeFindings()` — the one canonical shape-producing function every output format derives from — so every consumer gets the bridge in one place. Full gate: npm test 4152/4152 + 26/26 (after wiping D-0009's fixture state a seventh time), corpus 215/215, self-scan zero drift, build/smoke clean.

## Known limitations / open items (updated)

- 22 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,501,601,701,703,801,804`). This is cycle 18 of up to 100.
- **TWO full epics are now complete: E1 (Milestone 1, canonical pipeline — all 8 requirements FR-101-108) and E3 (ApplyFixService — all 6 requirements FR-301-306).**
- D-0009's stray-fixture-state pollution has now recurred 7 times in one session. The safe wipe recipe is documented in decisions.md D-0009 and the handoff file; the underlying fix remains correctly out of this PRD's scope.
- D-0010: prefer extracting a TOCTOU/race-window check into a directly-callable synchronous function over racing real async operation timing in a test.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-108. Candidates for the next Orient step: FR-602/603/604 (egress policy continuations, building on FR-601's chokepoint), FR-402-408 (E4 privacy items, unblocked by FR-401), or the FR-201-204 architecture chain (largest remaining item — with E1 and E3 both complete, this may be the natural next major undertaking).

## Known limitations / open items (updated)

**Cycle 18-19:** Implemented **FR-405** (missing/non-IR-backed privacy analysis → `not_assessed`, never vacuously `satisfied`). Root cause: FR-401 (earlier this session) built a real IR-backed PII taint path and set an `irBacked` flag per finding, but nothing downstream ever read it — `privacy-framework.js`'s bucketing only asked whether `evaluateFramework` found a mapped signal (e.g. `dpia.md`'s presence), never whether that signal came from a real IR walk or from the always-degrades-gracefully non-deep-mode path. Confirmed by direct execution (dispatched Explore agent) that `CT.DP-P4`/`CT.DP-P5` — both `codeTestable:'yes'`, both mapped ONLY to `module:privacy-taint` — read `satisfied` from a run with zero real IR-backed analysis. Fix: threaded a new `privacyIrBacked` scan-level flag from the annotator through `engine.js`'s `runFullScan()` return and `report/index.js`'s `toJSON()` (using `??`, not `||`, since `false` is a meaningful distinct value from `null`/absent), then added `_isPurelyPrivacyTaintDependent(control)` to `privacy-framework.js` to force exactly the controls whose ENTIRE mapping depends on privacy-taint into `engine-gap` when not IR-backed — deliberately NOT gating controls with an additional independent mapping (`ID.IM-P8`, `ID.RA-P3`, `CT.DP-P1`), since a mixed-mapping control's "present" verdict cannot be attributed to one specific signal. Own bug caught by the gate, not review: the first-draft `let _privacyIrBacked` declaration was placed textually after the `annotatePrivacyTaint` closure that assigns to it, causing a TDZ `ReferenceError` that made the annotator silently fail (`npm run test:report`'s `scan-health.test.js` caught it — `scanHealth.status` dropped from `complete` to `partial`); the second draft fixed the TDZ but placed the declaration inside a block that closes before the later `assessPrivacyFramework` call and final return, an out-of-scope bug that would have thrown `_privacyIrBacked is not defined` — caught by re-reading the actual block structure (`awk`-locating the closing brace) before trusting the fix, not by re-running tests first. Final placement: function-scope, before the `AGENTIC_SECURITY_NO_INTEGRATION` block starts. 4 new tests in `test/privacy-framework.test.js` (regression case, positive-control case proving the gate doesn't over-correct, absent-flag-degrades-safely case, mixed-mapping-not-gated case) — one initially failed for an unrelated reason (the positive-control fixture never created `dpia.md`, `module:privacy-taint`'s actual underlying signal per `auditor-walkthrough.js`), fixed by creating it in the test setup. Full gate: `npm test` exit 0, `test:posture` 1702/1702, `test:report` 250/250, `test/privacy-framework.test.js` 22/22, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- 23 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,405,501,601,701,703,801,804`). This is cycle 19 of up to 100.
- New decision D-0011 recorded: a `let` used inside a closure that runs immediately (via `_runAnnotator`) must be declared at a scope that is BOTH (a) already initialized before that closure runs, and (b) still in scope at every later read site — get either wrong and the failure mode differs (TDZ `ReferenceError` vs. out-of-scope `ReferenceError`), but both are silently swallowed by `engine.js`'s blanket per-annotator try/catch, so neither one crashes the scan — the annotator just silently no-ops. This class of bug is invisible without a test that checks a downstream *consequence* of the annotator having actually run, not just that the scan completed.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-405. Candidates: FR-402/FR-404 (E4's remaining smaller, evidenceable privacy items), FR-602/603/604 (egress policy continuations), or the FR-201-204 architecture chain (largest remaining item, needs its own design pass). FR-403 (real privacy dataflow rewrite) confirmed large and deferred.

## Known limitations / open items (updated)

**Cycle 19-20:** Implemented **FR-402** (versioned, source-free-customizable privacy data-classification taxonomy). `privacy-taint.js` carried a single hardcoded 4-bucket `PII_PATTERNS` const with no version and no extension mechanism — the PRD names 7 distinct classes by name (PII, PHI, PCI, financial, credentials, precise location, device identifiers, plus org-defined), and 3 of those (CREDENTIALS, GEOLOCATION, DEVICE_ID) didn't exist as distinct classes at all. Split the taxonomy into a new `dataflow/privacy-taxonomy.js` module: `BUILTIN_TAXONOMY_VERSION` + `DEFAULT_TAXONOMY` (all 7 classes, each with its own severity), and `loadPrivacyTaxonomy(scanRoot)` which merges an optional `.agentic-security/privacy-taxonomy.json` over the defaults — extend or replace an existing class's patterns, or add a brand-new organization-defined class, with zero source edits. `classifyField`/`annotatePrivacyTaint` both got optional parameters threading a taxonomy through, defaulting to prior behavior when omitted (verified via the 4 pre-existing tests passing unchanged). Wired the real `engine.js` call site to pass `scanRoot` so operator config is actually reachable on a real scan, and surfaced `privacyTaxonomyVersion` through to `report/index.js`'s `toJSON()` — making "versioned" genuinely inspectable, not just internal, mirroring the FR-405 cycle's `privacyIrBacked` pattern (including applying the same D-0011 scoping lesson: the new `_privacyTaxonomyVersion` variable was declared correctly at function scope on the first attempt this time). Own bug caught by the self-scan gate: the first-draft `loadPrivacyTaxonomy` used the exact `existsSync`-then-`readFileSync` TOCTOU pattern this session has now found and fixed 4 times total (3 previously, in `apply-fix-service.js` and `egress/policy.js`) — self-scanning the new file surfaced 4 findings (2 TOCTOU + 1 sync-I/O + a duplicate-detector TOCTOU), fixed using the established read-first pattern, which dropped it to 1 remaining finding (sync I/O, low severity) that was individually reviewed and confirmed to match the already-accepted precedent baselined for `network-policy-import.js`'s identical read pattern before accepting via `bench:self-scan:update-baseline`. Also caught my own exit-code-capture mistake mid-cycle: piping a gate command through `tail` and trusting the background task notification's paraphrased exit code silently hid a real gate failure (exit 1) behind an apparent "exit 0" — re-learned to always capture the real exit code directly (`> file 2>&1; echo $?`) for anything that gates, not just for the smoke test where this was first caught last cycle. Full gate: `npm test` exit 0 (4170/4170 + 26/26), `test:posture` 1716/1716, `test:report` 250/250, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift (1 reviewed/accepted new finding), build clean, smoke exit 3 (expected).

- 24 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,405,501,601,701,703,801,804`). This is cycle 20 of up to 100.
- Re-confirms D-0011's lesson held on a second, independent application (the `_privacyTaxonomyVersion` declaration this cycle) — declaring at function scope before the `AGENTIC_SECURITY_NO_INTEGRATION` block, on the first attempt, produced zero TDZ/out-of-scope issues, unlike the first attempt at `_privacyIrBacked` last cycle.
- Reinforces the TOCTOU lesson (now 4 occurrences across this session) strongly enough to be worth a permanent note: any new module that reads an optional operator-config file under `.agentic-security/` should be written read-first-in-try/catch from the start, not `existsSync`-then-read.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-402. Candidates: FR-404 (needs its `dependsOn: [FR-403]` re-verified against real code rather than trusted as written, per D-0008's lesson — FR-402 just landed some of what a full FR-403 would have needed), FR-602/603/604 (egress policy continuations), or the FR-201-204 architecture chain (largest remaining item, needs its own design pass). FR-403 itself confirmed large and deferred.

## Known limitations / open items (updated)

**Cycle 20-21:** Implemented **FR-404** (emit a privacy finding when regulated data reaches a prohibited sink under policy). Investigated the state.json `dependsOn: [FR-403]` before trusting it (per D-0008's lesson) and found it overcautious: the "emit a finding + prevent control satisfaction" half of the acceptance criterion already worked end-to-end (privacy-taint.js's `pii-exposure` finding flows through `auditor-walkthrough.js`'s `family:pii-exposure` resolution into `privacy-framework.js`'s `bucketOf`), with no FR-403 dataflow rewrite needed — corrected `dependsOn` to `[FR-402]`. The genuinely missing piece was "under policy": every sink match was unconditionally prohibited with zero override mechanism. Built `dataflow/privacy-sink-policy.js` (new): `loadPrivacySinkPolicy(scanRoot)` reads an optional `.agentic-security/privacy-policy.json` allowlist of `(sink, class?, reason?)` rules; `isSinkPermitted` requires EVERY matched class to be covered before exempting a finding (a mixed permitted+unpermitted class combination still fires in full — no partial bypass). Wired into `annotatePrivacyTaint`, with a `policyExemptions` array recording what was suppressed and why — never silent, same principle as the ignore-pragma suppression ledger — surfaced in the actual DPIA markdown artifact via a new "Policy-permitted flows" section, not just the internal return value. Wrote the new module read-first-in-try/catch from the very first draft this time (D-0012's lesson) and proactively self-scanned it BEFORE running the full gate — zero TOCTOU findings, only the same already-accepted sync-I/O pattern seen on `privacy-taxonomy.js` and `network-policy-import.js`, confirming the lesson held. The capstone test (`privacy-sink-policy.test.js`'s final case) proves the literal PRD acceptance-criteria example end to end against the REAL bundled NIST Privacy Framework 1.1: an unpermitted email→log flow flips `CT.DP-P1` from `satisfied` to `gap`, and a policy-permitted version of the identical flow leaves it `satisfied` — not a synthetic control, not an isolated unit test of one piece. Full gate: `npm test` exit 0 (4183/4183 + 26/26), `test:posture` 1729/1729, `test:report` 250/250, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift (1 reviewed/accepted finding), build clean, smoke exit 3 (expected). Every gate exit code this cycle was captured directly (`command > file 2>&1; echo $?`), continuing the discipline D-0012 re-established last cycle — including catching a python `-c` invocation's shell-parser warning ("redirection with no command") mid-cycle and verifying by direct re-read that the actual JSON write had completed correctly before trusting it, rather than assuming either "it worked" or "it's broken" from the warning text alone.

- 25 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,501,601,701,703,801,804`). This is cycle 21 of up to 100.
- E4 (Epic: Privacy data-flow and inventory) now has 4 of 8 requirements done (FR-401, FR-402, FR-404, FR-405) — the largest concentration of verified work in one epic outside E1/E3. FR-403 (real flow-tracking, confirmed large, deferred), FR-406/407/408 (P1, not yet investigated) remain.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-404. Candidates: FR-406/407/408 (E4's P1 remainder — data inventory/flow graph, expanded DPIA/RoPA fields, repository privacy policy — not yet investigated), FR-602/603/604 (egress policy continuations), or the FR-201-204 architecture chain (largest remaining item, needs its own design pass).

## Known limitations / open items (updated)

**Cycle 21-22:** Implemented **FR-407** (expand DPIA/RoPA scaffolds with governance fields, marked `manual_required` not inferred). All 11 PRD-named fields (purpose, lawful basis, subject, retention, residency, recipient, transfer, minimization, consent, access, deletion) are organizational/legal facts no code scanner can derive — this matches the bundled `gdpr.json` compliance-framework file's own stated philosophy ("lawful basis, data-subject rights, transfers and records of processing are organisational and are NOT represented here"), so the design goal was purely "never fabricate an answer, always disclose the gap by name," the same discipline this whole PRD is built around. Built `dataflow/privacy-governance.js`: `governanceRecordFor` returns all 11 fields per data class, each tagged `{value, source}` — `manual_required` unless an operator supplies a value via an optional `.agentic-security/privacy-governance.json` (per-class or scan-wide default), with an empty-string config value deliberately treated as unset rather than as a supplied blank answer. Added a new `emitRopaArtifact` (a proper GDPR Art. 30 register — one row per data class, all 11 fields as columns, plus an exact gap count) and expanded `emitDpiaArtifact` with a per-class governance subsection, so the same document a privacy officer already reads carries both halves. Wrote the new module read-first-in-try/catch from the start and proactively self-scanned it before the full gate for a second consecutive cycle — D-0012's lesson held again, zero TOCTOU findings, only the same already-accepted sync-I/O pattern (now baselined for 4 privacy-* modules this session). Hit a real shell-quoting failure this cycle writing the state.json entry via `python3 -c "..."` (a nested apostrophe inside a long evidence string broke out of the outer quoting, producing a `SyntaxError` several lines later that looked unrelated to its actual cause) — rather than fight the escaping, switched to writing the Python script to a scratch file via the Write tool and running that, which has zero shell-quoting exposure; verified the result was complete (not partially written) by reading the JSON back before trusting it, per the same discipline D-0012's earlier shell-warning incident established. Full gate: `npm test` exit 0 (4196/4196 + 26/26), `test:posture` 1742/1742, `test:report` 250/250, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift (1 reviewed/accepted finding), build clean, smoke exit 3 (expected).

- 26 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,407,501,601,701,703,801,804`). This is cycle 22 of up to 100.
- E4 now has 5 of 8 requirements done (FR-401, FR-402, FR-404, FR-405, FR-407) — the single most-complete epic outside E1/E3. FR-403 (large, deferred), FR-406 (not yet investigated), FR-408 (not yet investigated, but can likely build on FR-404's `privacy-sink-policy.js` allow-rule shape) remain.
- Writing a long evidence/decision string containing an apostrophe via `python3 -c "..."` is now a known trap (this cycle's own incident) — prefer writing the script to a file (Write tool) and running that file when the content has any non-trivial quoting, rather than debugging escape sequences inline.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-407. Candidates: FR-406 (code-derived data inventory and flow graph, not yet investigated), FR-408 (fuller repository privacy policy with environment/destination axes, can likely build on FR-404's work), FR-602/603/604 (egress policy continuations), or the FR-201-204 architecture chain (largest remaining item, needs its own design pass).

## Known limitations / open items (updated)

**Cycle 22-23:** Implemented **FR-408** (repository privacy policy defining prohibited and conditionally permitted flows, environment- and destination-specific). Rather than a new module, this extended FR-404's `dataflow/privacy-sink-policy.js` directly — that cycle's own evidence had already flagged the exact seam ("this module has no environment or destination-identity axis... FR-408 is the place to add those"). Added two OPTIONAL rule fields to the existing `(class, sink)` allow-rule shape: `environment` (this scan's deployment context, resolved ONLY from an explicit option or `AGENTIC_SECURITY_ENVIRONMENT` — deliberately never `NODE_ENV`, since this codebase's own SAST detector already flags `NODE_ENV` as "unreliable as a security boundary" for application code, and the same reasoning applies to a scanner's own policy decisions) and `destination` (a regex against the actual sink expression text, e.g. `stripe.track`, distinguishing it from a same-category sink like `sentry.track` that the coarser `sinkLabel` alone can't). Both axes fail closed — a rule naming a constraint the caller supplied no context for never matches, and an invalid destination regex never silently matches everything — matching the direction every other precision gate in this codebase takes when evidence is unavailable. A rule with neither field behaves exactly as an FR-404 rule always did, proven by the full pre-existing 13-test FR-404 suite passing completely unchanged after the extension. 9 new tests appended to the same `privacy-sink-policy.test.js` file (22 total). Proactively self-scanned both modified files before the full gate (D-0012's discipline, now routine) and correctly predicted zero drift, since this cycle only added pure functions with no new I/O — confirmed by the self-scan gate landing exactly as predicted. Full gate: `npm test` exit 0 (4205/4205 + 26/26), `test:posture` 1751/1751, `test:report` 250/250, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- 27 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,407,408,501,601,701,703,801,804`). This is cycle 23 of up to 100.
- E4 (Epic: Privacy data-flow and inventory) now has 6 of 8 requirements done (FR-401, FR-402, FR-404, FR-405, FR-407, FR-408) — the most complete epic outside E1/E3, and the only E4 items remaining are FR-403 (large, deferred) and FR-406 (not yet investigated).
- Continuing pattern: writing state.json entries via a scratch Python script file (Write tool), never `python3 -c "..."` for anything containing prose long enough to risk an apostrophe — this cycle's write completed cleanly on the first attempt using that approach.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-408. Candidates: FR-406 (code-derived data inventory and flow graph — the one remaining unstarted E4 item), FR-602/603/604 (egress policy continuations), or the FR-201-204 architecture chain (largest remaining item across the whole PRD, needs its own design pass).

## Known limitations / open items (updated)

**Cycle 23-24:** Implemented **FR-406** (generate a code-derived data inventory and flow graph). Built `dataflow/privacy-inventory.js`: `buildDataInventory` turns the raw signals `privacy-taint.js` already computes (piiFields, findings, policyExemptions) into per-source-flow records carrying the PRD's exact named fields (dataClass, source, transformations, storage, sinkRecipient, evidenceLocations) — an inventory of OBSERVED flows only, never a listing of every regulated-looking field name (a declared field that never reaches a recognized sink produces zero inventory records, proven directly). `transformations` is always the literal `not_modeled` sentinel, matching FR-405/FR-407's established discipline of honestly disclosing what this engine cannot see rather than fabricating partial credit — this engine has no sanitizer/transformation tracking for privacy flows at all (that's FR-403's deferred scope). `STORAGE_SINK_KINDS` (fileWrite, s3Upload) genuinely distinguishes the PRD's "storage" field from its "sink/recipient" field by sink category, disclosed as best-effort in the artifact's own schema note since most storage mechanisms (databases, queues, managed services) have no matching sink entry at all. Required one real, additive change to `privacy-taint.js` itself: findings and policyExemptions previously carried only the SINK location, with no way back to the SOURCE declaration — added `sourceName`/`sourceLine` via a small lookup built alongside the existing decls loop, confirmed non-breaking against the full 68-test pre-existing privacy-* suite. Also emits a Mermaid flow graph (`emitDataFlowGraph`) with source/sink nodes and red/green edges matching the FR-404 policy disposition. Wired into `engine.js` reusing the SAME already-computed findings/exemptions (no duplicate work), writing `data-inventory.json` and `data-flow-graph.md` alongside the existing `dpia.md`/`ropa.md`. Proactively self-scanned before the full gate and correctly predicted zero drift on both files (confirmed exactly). Full gate: `npm test` exit 0 (4216/4216 + 26/26), `test:posture` 1762/1762, `test:report` 250/250, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- 28 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,601,701,703,801,804`). This is cycle 24 of up to 100.
- **EPIC E4 (Privacy data-flow and inventory) IS NOW COMPLETE EXCEPT FR-403** (7 of 8 requirements done). This is the first epic besides E1/E3 to reach this level of completion. FR-403 itself (real flow-tracking through sanitization/masking/storage, aliased/interprocedural/cross-file) remains confirmed large and deferred, needing its own dedicated design pass — do not attempt it opportunistically as a "small connecting piece," per the repeated lesson this session that estimating scope requires reading the actual acceptance criterion first.
- With E4 essentially closed, the remaining live fronts are: FR-602/603/604 (E6 egress continuations), the FR-201-204 architecture chain (largest remaining item across the whole PRD), and epics E2/E5/E7/E8/E9 which have not had a fresh investigation pass this session — worth re-surveying state.json rather than assuming the only work left is in the epics touched recently.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-406. Candidates: FR-602/603/604 (egress policy continuations), the FR-201-204 architecture chain (largest remaining item, needs its own design pass), or a fresh survey of E2/E5/E7/E8/E9 (not touched recently) before assuming E4/E6 are the only live fronts.

## Known limitations / open items (updated)

**Cycle 24-25:** Implemented **FR-704** (make `--no-state` authoritative for all optional persistence). Per the prior handoff's own recommendation, surveyed E5/E7/E8/E9 for smaller unexamined items rather than defaulting straight to the large FR-201-204 chain — this investigation found FR-704 backed by extensive, mature existing infrastructure for the CLI path (state-dir.js's global kill switch, a write-seam guard with an EMPTY migration ledger, and `test/no-stray-state.test.js`'s 6-configuration zero-new-paths matrix), matching the FR-105-107 "small connecting piece" pattern — right up until it wasn't. Direct execution of a probe scan using MCP's `scan_diff` tool's EXACT call shape (`runScan(sessionRoot, {...})` with no opt-out) against a real fixture revealed a genuine, previously-undiscovered production bug: 11 state artifacts (dpia.md, ropa.md, data-inventory.json, threat-model.json, privacy-framework.json, and more) written into the session root on a single call — silently contradicting `scan_diff`'s own tool description ("Use BEFORE writing a Write/Edit to disk so the agent can self-correct") and `mcp/CLAUDE.md`'s own documented claim ("runs scan in memory"). The existing guard suite never caught this because its entire matrix drives the CLI's `--no-state` flag; MCP's separate `runScan()` call path was never exercised by it at all. Fixed with a new `withStateWritesDisabled(fn)` scoped wrapper in `state-dir.js` around the existing global kill switch — deliberately NOT threaded as a parameter through ~72 call sites (the module's own header already explains that design decision), instead saving and restoring the PRIOR flag value in a `finally` block so one caller's opt-out can never leak into every later write in the same long-running MCP server process. Explicitly disclosed, not silently absorbed: the global flag is still process-global, so genuinely concurrent overlapping calls carry a narrow, accepted race window — the SAME class of risk `mcp/CLAUDE.md` already documents for `fix-history.js`'s concurrent `apply_fix` calls, not a new one, and a strict improvement over the always-triggered bug it replaces. Confirmed via direct execution (not just unit tests) that the fix actually eliminates the state pollution, and that the flag correctly restores both directions (true→true and false→false) including through a thrown error. Full gate: `npm test` exit 0 (4223/4223 + 26/26), `test:posture` 1768/1768, `test:mcp` 119/119, corpus 215/215 zero drift, self-scan zero drift (both modified files matched their EXISTING baseline exactly — zero new detector-visible pattern), build clean, smoke exit 3 (expected).

- 29 of 73 FRs verified (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,601,701,703,704,801,804`). This is cycle 25 of up to 100.
- **The survey-before-defaulting approach paid off directly**: rather than assume FR-201-204 was the only substantive remaining work, checking the smaller epics first surfaced a real, live, previously-unknown bug affecting every agentic coding session using MCP's `scan_diff` tool. This reinforces the session-wide lesson (D-0008 and others) that "the largest remaining item" is not automatically "the next item" — investigate before assuming scope, including at the epic-selection level, not just the individual-requirement level.
- Flagged, not yet investigated: whether FR-507 (evidence-grade wording, E5), FR-606 (E6, model unavailability surfaced distinctly), or FR-605 (E6, unambiguous local-only mode) are similarly mature-but-unverified candidates worth the same treatment FR-704 just received.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-704. Candidates: continue the E5/E7/E8/E9 survey (FR-507, FR-605, FR-606 flagged as possible next "verify mature infrastructure" candidates), FR-602/603/604 (egress continuations), or the FR-201-204 architecture chain (still the largest remaining item, still needs its own design pass whenever it's eventually tackled).

## Known limitations / open items (updated)

**Cycle 25-26:** Implemented **FR-507** (replace certification-style wording with evidence-grade wording). Continuing the E5/E7/E8/E9 survey (per FR-704's own recommendation last cycle), found 4 independently hand-rolled compliance disclaimers (`compliance-policy.js`, `auditor-walkthrough.js`, `privacy-framework.js`, `report/oscal.js`) that had already drifted from each other — and 3 of the 4 contained a real terminology bug: "a licensed assessor is responsible for the final attestation" gets the PRD's own three-tier vocabulary BACKWARDS. A licensed/accredited assessor produces an INDEPENDENT CERTIFICATION (or a formal assessment opinion), not an attestation; an ATTESTATION is specifically the organization's OWN leadership formally asserting that stated controls are in place and operating (the SOC 2 "management assertion" model). Confirmed by cross-referencing `docs/OSCAL.md`'s own prose, which already used the term correctly in one place ("It is not an attestation that the control is implemented and operating") — proving this was a real, catchable inconsistency, not a matter of interpretation. Built `posture/evidence-grade-wording.js` as the single canonical source of truth (`ASSURANCE_TIERS` naming all three tiers distinctly, plus full/short disclaimer variants preserving the exact "does not certify compliance" substring two pre-existing tests already pinned), and rewired all 4 call sites to reference it instead of hand-rolling. Deliberately did NOT touch `posture/attestation.js`/`evidence-bundle.js` — those implement an unrelated cryptographic tamper-evidence concept that merely shares the English word "attestation" with the PRD's compliance vocabulary; renaming a working, already-tested feature to avoid a word collision would have been unjustified scope creep. Proactively self-scanned all 5 touched files before the full gate — every count matched the existing baseline exactly, confirming a pure wording change introduces no new detector-visible pattern. Full gate: `npm test` exit 0 (4231/4231 + 26/26), `test:posture` 1776/1776, `test:report` 250/250, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **30 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,701,703,704,801,804`). This is cycle 26 of up to 100 — a round milestone (41% of the PRD's requirement count, though weighted toward smaller items so far; the PRD's own author-scoped estimate of "several sprints per milestone" means this is not yet 41% of the actual EFFORT).
- **The survey approach has now found and fixed TWO real, previously-undiscovered defects across two consecutive cycles** (FR-704's MCP state-write bug, FR-507's terminology inversion) — both were things a routine "does the test suite pass" check would never have surfaced, because in both cases the existing tests were testing something narrower than the actual acceptance criterion (FR-704: only the CLI path; FR-507: only "is there a disclaimer" not "is the disclaimer terminology correct"). This reinforces continuing the survey before defaulting to FR-201-204.
- E5 now has 2 of 8 requirements done (FR-501, FR-507). FR-502/503/504/506 (E5's remainder) not yet investigated.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-507. Candidates: continue the E5/E7/E8/E9 survey (FR-502/503/504/506 in E5, FR-605/606 in E6, FR-702/705/706 in E7, FR-802/803/805/806 in E8 — none investigated this session), FR-602/603/604 (egress continuations), or the FR-201-204 architecture chain (still the largest remaining item).

## Known limitations / open items (updated)

**Cycle 26-27:** Implemented **FR-605** (enforce an unambiguous local-only provider mode — a remote URL cannot be smuggled into local-only configuration). Continuing the E5/E7/E8/E9 survey, traced every one of the 12 files referencing `AGENTIC_SECURITY_LLM_ENDPOINT` directly to confirm every real network-egress caller routes through `evaluateEgress`'s `mode:local-only` check, not just the ones already known to be wired — the same discipline that found FR-704's bug. Found a THIRD real defect in three consecutive cycles, and this one the codebase's own comment had already flagged as open: `discovery/llm-invoke.js`'s multi-endpoint consensus mode (`AGENTIC_SECURITY_LLM_ENDPOINTS`) constructed its caller with ZERO egress check, so `mode: local-only` was silently bypassable by using a comma-separated remote-URL list instead of the single-endpoint variable — the exact "remote URL smuggled into local-only configuration" scenario the acceptance criterion names. Fixed by giving each consensus endpoint its own `evaluateEgress` decision, excluding denied ones from the vote (matching the module's own pre-existing pattern for excluding unreachable endpoints, never counting exclusion as dissent). Separately, adversarially tested `isLoopbackUrl` (the mechanism the check relies on) against real-world localhost-bypass techniques — octal/hex/decimal IPv4 notation, IPv4-mapped IPv6, hostname lookalikes, userinfo-confusion URL tricks — and confirmed it genuinely holds, verified by printing the URL parser's own normalized hostname for each case rather than trusting the design comment. Updated `egress/policy.js`'s own module header, which had explicitly (and, as of this fix, correctly-at-the-time) flagged FR-605 as unfinished future work — corrected to describe the current, verified state rather than leaving it stale. Full gate: `npm test` exit 0 (4240/4240 + 26/26), `test:discovery` 88/88, `test:posture` 1776/1776 (unchanged), `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **31 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,605,701,703,704,801,804`). This is cycle 27 of up to 100.
- **The survey is now 3-for-3 on finding real, previously-undiscovered defects across three consecutive cycles** (FR-704's MCP state-write bug, FR-507's terminology inversion, FR-605's consensus-mode egress bypass). All three share a structural pattern worth naming explicitly: each was a real caller path that a "does the test suite pass" check would never surface, because the existing coverage tested something narrower than the actual acceptance criterion asked for (only the CLI path; only "does a disclaimer exist" not "is it worded correctly"; only the single-endpoint path). This is now a load-bearing lesson for the rest of this session, not a coincidence.
- E6 now has 2 of 7 requirements done (FR-601, FR-605). FR-602/603/604/606/607 not yet investigated (FR-606 flagged as the next E6 candidate).
- Disclosed, deliberately out-of-scope: `egress-policy-completeness.test.js`'s file-level granularity ("does this file import evaluateEgress at all") would NOT have caught this exact bug shape (a file that imports it but fails to call it on one branch) — a call-site-level AST guard would close that blind spot but is a larger undertaking than this cycle's scope.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-605. Candidates: FR-606 (E6, distinct model-unavailability surfacing — natural continuation of this cycle's egress-path tracing), FR-502/503/504/506 (E5's remainder), FR-702/705/706 (E7's remainder), FR-802/803/805/806 (E8's remainder).

## Known limitations / open items (updated)

**Cycle 27-28:** Implemented **FR-606** (surface model unavailability or refusal without converting it into a clean deterministic conclusion — reports distinguish model-disabled, policy-blocked, unavailable, malformed, and completed). Natural continuation of FR-605's egress-path tracing: every degrade branch in `llm-validator/index.js`'s `validateOne`/`validateMany` set the SAME `finding.validator_verdict` ('unvalidated'), with the actual reason scattered across an inconsistently-populated free-text field — the "nothing configured at all" branch didn't even set an error field. Built `llm-validator/model-status.js`'s `MODEL_STATUS`, a frozen 5-value vocabulary matching the PRD's own naming exactly, threaded through every real branch: DISABLED (nothing configured, or an explicit `VALIDATE=0` opt-out), POLICY_BLOCKED (local-preset loopback refusal, egress-policy denial, cost-ceiling cap), UNAVAILABLE (non-2xx HTTP or a network exception), MALFORMED (a 2xx response that couldn't be parsed/verified), COMPLETED (a real verdict, including a cache hit — proven via a genuine two-call round-trip, not asserted from reading the code). Deliberately did NOT stamp the taxonomy on findings skipped for SUITABILITY reasons (SCA locators, no-precise-location) — those are a different question from model availability and already have their own honest status. While verifying the cache-hit case, found and fixed a test-infrastructure bug in a NEW test (missing project marker silently disabling the disk cache — the same historical bug this codebase's own `state-dir.js` comment documents) — and, while diagnosing it, discovered an EXISTING, structurally-identical test elsewhere in the suite is itself order-dependent (passes only as part of its full file, fails in isolation). Documented, not fixed — flagged as separate remaining work, out of FR-606's own scope. Full gate: `npm test` exit 0 (4254/4254 + 26/26), `test:posture` 1790/1790, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **32 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,605,606,701,703,704,801,804`). This is cycle 28 of up to 100.
- **The user has instructed: keep going until the PRD is fully completed.** This supersedes the earlier "ask before continuing" checkpoint from mid-session — the loop should now continue autonomously through remaining FRs without pausing for confirmation between cycles, per the bounded-loop mandate (max 100 cycles) and safety boundaries (no commits/pushes, no destructive git ops, still established without exception).
- E6 now has 3 of 7 requirements done (FR-601, FR-605, FR-606). FR-602/603/604 (P1 remainder) and FR-607 (P2) not yet investigated.
- A newly-flagged, deliberately out-of-scope item: `test/llm-validator-default-on.test.js`'s "cache hit on second run: no network call" test is order-dependent (confirmed by direct isolation testing this cycle) — it should be root-caused in a future cycle, but doing so was not this cycle's job.

## Next action

Per state.json's `exactNextAction`: no FR selected yet for after FR-606. Candidates in priority order for continued investigation: FR-602/603/604 (E6 P1 remainder), FR-607 (E6 P2), FR-502/503/504/506 (E5 remainder), FR-702/705/706 (E7 remainder), FR-802/803/805/806 (E8 remainder) — then the larger items: FR-201-204 (architecture chain, needs its own design pass), FR-403 (privacy dataflow rewrite, large, deferred), and E9/E10 plus E2/E3's remaining requirements, none of which have been investigated this session at all.

## Known limitations / open items (updated)

**Cycle 28-29:** Implemented **FR-602** (support provider, model, role, region, repository, path, data class, and maximum-context constraints — tests cover allow, deny, and local-only decisions). FR-601's own module header had explicitly deferred this ("Deliberately NOT in scope here: the fuller constraint-dimension set — role/region/repository/path/data-class/max-context — FR-602"), so this cycle extended the SAME mechanism (`egress/policy.js`'s `evaluateEgress`) with the 7 remaining named dimensions rather than building something parallel. Design principle applied uniformly across every new dimension: evaluated ONLY when the caller supplies the corresponding context value AND the config restricts that specific dimension — a caller with no opinion on region must never be blocked by a region rule it has no way to satisfy. model/role/region/repository/dataClass share one list-membership code path (a `LIST_DIMENSIONS` table, avoiding 5 copy-pasted blocks); path uses glob matching (reusing `util/glob.js`'s existing `matchesAnyGlob`, not reimplementing one); max-context is a numeric cap against a caller-supplied estimate (the function runs before prompt construction, so it can't measure a prompt itself). Proven not purely theoretical by threading `role`/`model` through `llm-validator/index.js`'s real call path — the same wired-caller precedent FR-601/605/606 established — with an end-to-end test confirming a `deniedModels` rule genuinely blocks the real `validateOne()` fetch, not just the policy function in isolation. Confirmed `mode:local-only`'s own precedence still holds correctly against every new dimension in both directions, and that multiple dimensions supplied together on one call all must pass (a single failing dimension denies the whole call). Full gate: `npm test` exit 0 (4270/4270 + 26/26), `test:posture` 1806/1806, `test:lifecycle` 248/248, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **33 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,602,605,606,701,703,704,801,804`). This is cycle 29 of up to 100.
- E6 now has 4 of 7 requirements done (FR-601, FR-602, FR-605, FR-606). Only FR-603 (redaction), FR-604 (audit metadata), FR-607 (P2) remain in E6.
- Deliberately, consistently NOT retrofitted into every OTHER LLM-backed caller (discovery, adversary-agent, flow-narration, sca-llm-function-extract) — each would need to supply its own genuinely-known values for the new dimensions, which most don't currently have readily available (e.g. discovery callers don't classify content by privacy taxonomy at the call site). This mirrors FR-601/605/606's own established incremental-wiring precedent (prove one real caller per cycle, not a mass retrofit) rather than a gap in the mechanism itself.

**Cycle 30:** Implemented **FR-603** (add configurable secret, PII/PHI, customer-data, and proprietary-path redaction — redaction tests confirm prohibited content is absent from the final outbound payload). Survey (D-0014 method) found the codebase already had TWO independent `redactSecrets` implementations before any new code: `llm-validator/redact.js`'s (proven, secrets-only, wired into exactly one caller) and `util/untrusted.js`'s (fully tested but confirmed — by tracing every importer — to have ZERO production callers despite its own header claiming it protects LLM-bound and audit-log text; see D-0016). Built `scanner/src/egress/redact.js` as the general outbound-payload redaction module the PRD's four categories require: proprietary-path (whole-span replacement via a configured glob, reusing `util/glob.js`'s `matchesAnyGlob`), secrets (delegates to `llm-validator/redact.js`'s proven pass, not reimplemented), PII/PHI/PCI/FIN (reuses `dataflow/privacy-taxonomy.js`'s FR-402 field-name vocabulary as the single source of truth, CREDENTIALS class skipped since the secrets pass already owns it), and customer-data (operator-configured regex list, no built-in default). All four read the same `egress-policy.yml` FR-601/602 already use — `policy.js`'s private config loader was renamed/exported as `loadPolicyConfig` for this reuse rather than adding a second config file. A live bug (positional-backreference group-number shift, caused by two taxonomy field patterns — `email`/`phone` — carrying their own internal capturing groups) was caught by direct execution against a live example BEFORE writing tests, and fixed with named capture groups. Wired into the real, already-identified caller: `llm-validator/index.js`'s `renderPrompt`, the exact "last choke point before the prompt is assembled" its own R10 comment already named — proven end-to-end with a test that stubs `global.fetch` and inspects the actual outbound HTTP request body for a secret, an SSN-shaped PII value, and an operator-configured customer-data value together, confirming none reach the real network boundary while variable-name structure survives. Also surfaced and corrected a real gap in this session's own D-0009 fixture-wipe recipe: it silently deletes TRACKED fixture content (two `.agentic-security/rules.yml` fixtures), not just generated scan state — caught by the mandatory post-wipe `git status` check, restored via `git checkout --`, documented as D-0015. Full gate: `npm test` exit 0 (4290/4290 main + 26/26 cpp-dataflow + 5/5 + 4/4 python), `test:posture` 1826/1826, corpus 215/215 zero drift, self-scan zero drift (predicted correctly by a direct per-directory self-scan before the full gate), build clean, smoke exit 3 (expected).

- **34 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,602,603,605,606,701,703,704,801,804`). This is cycle 30 of up to 100.
- E6 now has 5 of 7 requirements done (FR-601, FR-602, FR-603, FR-605, FR-606). Only FR-604 (audit metadata) and FR-607 (P2) remain in E6.
- Deliberately NOT retrofitted into every OTHER LLM-backed caller (discovery, adversary-agent, flow-narration, sca-llm-function-extract) this cycle — same established incremental-wiring precedent (prove one real caller per cycle). `util/untrusted.js`'s unused `redactSecrets` was deliberately left in place rather than wired up or deleted as a drive-by fix (D-0016) — flagged for FR-604 or a future dead-code sweep.

**Cycle 31:** Implemented **FR-604** (record per-call audit metadata without retaining prohibited prompt content — audit includes purpose, provider, model, region if known, byte/token counts, policy, hashes, and outcome). Survey found `mcp/audit.js` already implements exactly this class of guarantee for MCP tool calls (append-only NDJSON, each entry's `prev` = sha256 of the previous line, so tampering breaks the chain from that point forward — OWASP MCP08); its public API and hash-chain helpers are private and shaped around `{tool, args}`, not egress's field set, so reshaping it into shared primitives was judged a separate refactor and the ~15-line hash-chain idiom was deliberately mirrored (documented as such) rather than imported. Built `scanner/src/egress/audit.js`: `recordEgressCall({scanRoot, decision, ctx, metrics})` appends one entry per `evaluateEgress()` decision to `.agentic-security/egress-audit.log`; `payloadMetrics(text)` computes `{byteCount, tokenCount, contentHash}` from an outbound payload WITHOUT ever returning or logging the text itself (verified directly — `JSON.stringify(payloadMetrics(secret))` does not contain the secret). Wired into `llm-validator/index.js`'s `validateOne()`: the deny branch records immediately (byte/token/hash all `null` — nothing was ever built); the allow branch records right after `renderPrompt()` produces the final, already-redacted (FR-603) prompt, the only point real metrics exist. Proven end-to-end: an integration test drives `validateOne()` through a real `mode: deny` denial and a real allowed call with a live secret in the finding's content, reads the actual persisted log file both times, confirms the two entries chain, and asserts the raw log text never contains the secret. A proactive self-scan before the full gate caught a real TOCTOU defect in the new `verifyEgressAuditLog()` (mirroring the SAME pre-existing pattern `mcp/audit.js`'s own verify function still carries) — fixed to read-first per this session's own D-0012 discipline rather than accepting the same latent pattern, confirmed by re-scanning (2 TOCTOU findings → 0). Full gate: `npm test` exit 0 (4300/4300 + 26/26 + python suites), `test:posture` 1836/1836, corpus 215/215 zero drift, self-scan zero drift (baseline updated +1 for `egress/audit.js`, confirmed via direct before/after self-scan), build clean, smoke exit 3 (expected).

- **35 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,602,603,604,605,606,701,703,704,801,804`). This is cycle 31 of up to 100.
- E6 is now 6 of 7 done (FR-601, FR-602, FR-603, FR-604, FR-605, FR-606) — only FR-607 (P2) remains.
- Deliberately NOT retrofitted into the other 5 `evaluateEgress` call sites (discovery/hunter, discovery/disprove, adversary-agent, flow-narration, sca/llm-function-extract) this cycle — same established one-real-caller-per-cycle precedent, matching how FR-602/603 also deferred their own mechanisms for those same callers.

**Cycle 32:** Implemented **FR-607** (support approved-provider metadata such as DPA/BAA status and retention policy — regulated profiles can require approved contractual and retention attributes), **closing Epic E6 entirely** (all 7 requirements: FR-601–FR-607). Reused `dataflow/privacy-governance.js`'s (FR-407) "state it, never infer it" discipline for organizational/legal facts — DPA/BAA/retention are never derivable from code. Extended `evaluateEgress` with a final gate: `regulatedProfile.requireApprovedProviders` (default off — a no-op unless configured, the same default every other dimension follows); when set, every provider must have a matching `approvedProviders[provider]` config entry carrying a non-empty value for each of `regulatedProfile.requiredAttributes` (defaults to `[dpaStatus, baaStatus, retentionPolicy]`) or the call is denied, naming the specific missing attribute(s). `'none'`/`'not_signed'` placeholder values are explicitly treated as absent, closing an accidental-bypass path. Runs as the LAST gate, after every other FR-601/602/605 check — confirmed `mode: deny` still takes precedence, and approval for one provider does not leak to another. The allow decision now optionally carries `approvedProviderMetadata` for downstream use (e.g. compliance reporting). Proven end-to-end through the real `llm-validator` call path — a regulated profile with no approved-provider entry blocks the real `validateOne()` before any fetch. Full gate: `npm test` exit 0 (4311/4311 + 26/26 + python suites), `test:posture` 1847/1847, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **36 of 73 FRs verified** (`FR-101,102,103,104,105,106,107,108,206,301,302,303,304,305,306,401,402,404,405,406,407,408,501,507,601,602,603,604,605,606,607,701,703,704,801,804`). This is cycle 32 of up to 100.
- **Epic E6 is now fully closed.**

**Cycle 33:** Implemented **FR-502** (make control thresholds policy-specific rather than globally high/critical — a framework mapping can fail on medium/low, not just high/critical). Survey found `auditor-walkthrough.js`'s own header comment on `OPEN_FINDING_MIN_SEVERITY` already documented that the GLOBAL half of this requirement (raising the floor from critical/high-only to medium+) was done in an earlier cycle — with a pre-existing, already-registered 6-test file proving it — but explicitly named the remaining scope: "a future per-framework/per-policy threshold... is a one-line change here." Built that config surface: `.agentic-security/compliance-severity-policy.json` (`{default, byFramework: {frameworkId: severity}}`), read by a new `_resolveOpenFindingMinSeverity(scanRoot, frameworkId)`, resolved once per `evaluateFramework()` call via the framework's own `id`. An unrecognized/malformed override value always falls back to the safe `'medium'` default rather than reaching the severity-rank lookup with an invalid key — which would silently make the `>=` comparison always false, recreating the exact vacuous-pass bug this threshold exists to prevent. Confirmed both real callers (`privacy-framework.js`, and the CLI's `--walkthrough`/`--report` flag — a direct, unmodified `scanRoot` passthrough) are covered. In passing, discovered that `artifact-registry-completeness.test.js`'s literal-scanning regex cannot see a `statePath()` call that uses a named constant instead of an inline string — the same shape `egress/policy.js`'s own `POLICY_FILE` already uses — so it silently missed FR-604's `egress-audit.log` too; registered both retroactively. Full gate: `npm test` exit 0 (4319/4319 + 26/26 + python suites), `test:posture` 1855/1855, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **37 of 73 FRs verified.** This is cycle 33 of up to 100.
- E5 is now 3 of 8 done (FR-501, FR-502, FR-507).

**Cycle 34:** Implemented **FR-307** (require human approval for high-impact change classes — auth/authZ/crypto/PII/schema/infrastructure-privilege/public-API changes cannot auto-apply without approval evidence), deliberately breaking this session's repeated pattern of defaulting back to E5/E6/E7/E8 (E2/E3/E9/E10 had gone unexamined for 6+ handoffs running). Survey found `posture/material-change.js` already implements a pattern-based diff-risk classifier covering 4 of the 7 named categories (auth, authZ, infra-privilege, public-API — built for a different purpose, PR-review triage), missing crypto/PII/schema. Added 3 new pattern kinds for those (PII reuses `dataflow/privacy-taxonomy.js`'s field-name vocabulary rather than a parallel list) plus an explicit `HIGH_IMPACT_CATEGORY_OF_KIND` map. Added `classifyFixMaterialRisk(files)`, operating on the same before/after content shape `apply-fix-service.js` already has — no git invocation needed. Wired into `applyVerifiedFix()`: a high-impact candidate is refused before verification even runs (fail-fast) unless `fixMeta.approval: {approvedBy, reason}` is present with both fields non-empty; every other candidate is unaffected. `dryRun:true` deliberately previews past the gate without approval (nothing is written either way) so a caller can learn approval will be needed before going to collect it. Proven end-to-end: a real auth-removal fix is refused with zero bytes written, the same candidate succeeds once approval evidence is supplied, an incomplete approval object still fails, and PII/crypto/schema are each independently proven through the real write path. Full gate: `npm test` exit 0 (4332/4332 + 26/26 + python suites), `test:posture` + `test:report` both 0 failures, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **38 of 73 FRs verified.** This is cycle 34 of up to 100.
- E3 is now 7 of 8 done (FR-301 through FR-307) — only FR-308 remains.

**Cycle 35:** Implemented **FR-308** (record fix completeness and residual risk from mechanical evidence — a mitigation or workaround cannot be represented as a full fix), **closing Epic E3 entirely** (all 8 requirements: FR-301–FR-308). Survey found `fix-honesty-gate.js`'s FULL/MITIGATION/WORKAROUND tier classifier was already built and tested — but entirely SELF-REPORTED: the module's own header comment and both real MCP call sites already admitted "nothing here is server-computable." Identified the one piece of genuinely mechanical evidence already available in the real pipeline: `fix-verify.js`'s PoC-execution leg, which actually runs the finding's proof-of-concept against the candidate patch inside the sandbox. Added `checkMechanicalTierEvidence(tier, pocLeg)`: a self-reported FULL tier is now REFUTED (not merely internally-checked) when the PoC still demonstrates the vulnerability against the patch — deliberately one-directional, a non-FULL tier is unaffected (a mitigation is allowed to leave the flaw partially reachable; that's what "mitigation" already honestly means). Reordered `verifyFix()` so the PoC leg computes before the honesty gate, then threaded `pocLeg` through as a new, backward-compatible optional parameter. Both real MCP callers already pass `fixMeta` and `poc` together, so this reaches production with zero MCP schema changes. Proven end-to-end: a self-reported FULL claim is refuted when a real PoC still works, and correctly accepted once the same PoC no longer does. Full gate: `npm test` exit 0 (4339/4339 + 26/26 + python suites), `test:posture` 1869/1869, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **39 of 73 FRs verified.** This is cycle 35 of up to 100.
- **Epics E1, E3, E4 (except FR-403), and E6 are all fully closed.**

**Cycle 36:** Implemented **FR-901** (re-run independent evaluation for every candidate release — published results identify engine version, corpus version, commit, scope, and date), the first requirement examined in Epic E9. Survey confirmed the ENFORCEMENT half already existed: `scripts/release-check.mjs`'s scorecard-freshness check is wired into `prepublishOnly` and `release.yml`, failing when the committed `docs/scorecard.json`'s `engineVersion`/`corpus.totalEntries` don't match the current state — a version bump without a fresh scorecard cannot pass. The gap was the acceptance criterion's literal 5-field list: `provenance` already had engine version, commit, and date, but no `corpusVersion` or `scope` field existed anywhere in the pipeline. Added `corpusVersion` (a sha256 hash of `bench/cve-replay/corpus-baseline.json`'s raw bytes — a reproducible identifier independent of the engine's own commit) and `scope` (a description of what the scorecard run actually measures) to `scripts/scorecard.mjs`'s provenance object, threaded through `posture/accuracy-scorecard.js`'s already-pass-through `buildScorecard` and rendered in `renderScorecardMarkdown`'s Provenance table. Proven via a REAL execution of the production driver (`npm run scorecard`), not a mocked test: the regenerated `docs/SCORECARD.md`/`docs/scorecard.json` were read back directly to confirm both new fields are genuinely present and correctly formed, the documented determinism invariant was independently re-verified (two runs, identical apart from the timestamp), and `npm run scorecard:check` still passes. Full gate: `npm test` exit 0 (4342/4342 + 26/26 + python suites), `test:posture` 1872/1872, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **40 of 73 FRs verified.** This is cycle 36 of up to 100.
- E9 is now 1 of 7 done (FR-901).

**Cycle 37:** Implemented **FR-903** (keep independent labels isolated from detector development — detection code and prompts cannot access labels or expected-answer files during scan execution). Survey traced every reference to the specific label/expected-answer filenames this repo's benchmarking infrastructure uses (`corpus-baseline.json`, `RESULT.json`, `calibration-holdout`, `labels.jsonl`) across the entire `scanner/src/` tree — found exactly 2 files with any reference at all, both confirmed by direct inspection to be reporting-only string labels, never a file read. Distinguished this from two things it is NOT: `sast/bench-shape/`'s source-embedded marker reading (a different, already-gated, already-tested mechanism — opt-in only, stripped under blind evaluation) and `calibration-seed.json` (a legitimate aggregate statistical prior for confidence calibration, not per-entry ground truth). Built the missing piece: `test/label-isolation.test.js`, a structural completeness guard (same shape as `artifact-registry-completeness.test.js`) proven — both directions, via direct execution — to catch a real violation: an injected throwaway reference to `corpus-baseline.json` made the guard fail with a precise, correctly-attributed message; removing it restored a clean pass. Full gate: `npm test` exit 0 (4346/4346 + 26/26 + python suites), `test:posture` 1876/1876, corpus 215/215 zero drift, self-scan zero drift (no production `src/` file changed this cycle), build clean, smoke exit 3 (expected).

- **41 of 73 FRs verified.** This is cycle 37 of up to 100.
- E9 is now 2 of 7 done (FR-901, FR-903).

**Cycle 38:** Implemented **FR-904** (maintain a hidden or externally governed holdout set — rule authors cannot optimize against the full scored population). `bench/calibration-holdout/labels.jsonl` does not exist — confirmed this is genuine, already-honestly-waived (`.calibration-waiver.json`) data-collection work, not a code gap, and deliberately left untouched (fabricating labels would be measurement fraud). Found the requirement's real answer already built: `bench/independent/runner.mjs`'s T0.7 — a deterministic (FNV-1a hash of entry id) 20% held-out slice of the externally-governed independent population, scored separately and explicitly "never tuned against," with its own pre-existing direct tests (determinism, fraction, monotonicity). The underlying source is gitignored, so it's not sitting visibly in a working tree either. The real gap: `accuracy-scorecard.js`'s published `committedInputs.independent` copied `overall`/`wide`/`byLanguage` from `RESULT.json` but never `heldOut`/`development` — the one number this requirement is about was computed and committed but never reached the published, for-every-release scorecard. Extended `buildScorecard`/`renderScorecardMarkdown` with a null-safe pass-through and a new "Held-out slice — never tuned against" section, distinct from the merged figures. Proven via two real `npm run scorecard` runs (determinism re-verified) reading back the actual committed 205-entry held-out figures. Full gate: `npm test` exit 0 (4349/4349 + 26/26 + python suites), `test:posture` 1879/1879, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **42 of 73 FRs verified.** This is cycle 38 of up to 100.
- E9 is now 3 of 7 done (FR-901, FR-903, FR-904).

**Cycle 39:** Implemented **FR-902** (gate on advisory-local precision, recall, and F1 plus per-language floors — a material regression blocks release unless a signed exception explains it). Confirmed `bench/independent/`'s own README explicitly states it is "not gated in CI — a measurement, not a gate," and no existing check (`layer-recall`, `mutation`) compares its precision/recall/F1 against any prior state. Built `scripts/independent-population-gate.mjs` + `bench/independent/gate-baseline.json`, following the same committed-artifact-vs-committed-floor pattern `bench/cve-replay/corpus-baseline.json`/`bench/layer-recall/baseline.json` already use — a FLOOR gate (not exact equality, given this population's small, noisy per-language counts), covering overall precision/recall/F1 plus a floor for every tracked language, correctly treating a language vanishing entirely as a regression while a brand-new language is not gated. Signed-exception mechanism built to the same shape as `.calibration-waiver.json`/`.dependency-holds.json` (same anti-rot rules: short reason, missing/expired reviewBy all fail). Wired into `release-check.mjs`, proven via a real `node scripts/release-check.mjs --fast` execution showing the new check genuinely PASSes in the real gate run. Full gate: `npm test` exit 0 (4363/4363 + 26/26 + python suites), `test:posture` 1893/1893, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **43 of 73 FRs verified.** This is cycle 39 of up to 100.
- E9 is now 4 of 7 done (FR-901, FR-902, FR-903, FR-904).

**Cycle 40:** **Investigated FR-905** (publish false-positive adjudication and coverage methodology) and correctly left it NOT verified (D-0020) — after 4 consecutive E9 wins of the "mechanism exists, needs to reach the published surface" shape, this one only partially fits it. 3 of 4 named categories are already genuinely published: "unsupported cases" via `population.unscored` (rendered in both `docs/SCORECARD.md` and `docs/scorecard.json`), and the qualitative methodology for "true false positives"/"unlabeled output" via `bench/independent/README.md`'s detailed "Honest limits" section. The 4th, "missed findings," is different in kind: `bench/independent/why-missed.mjs` is a real, working diagnostic (determines the exact suppression mechanism per false negative, computes an aggregate `summary.byBucket`) but its output — confirmed by reading the script — is gitignored per-entry and stderr-only in aggregate, never committed. ~94 already-materialized local output files were found in the working tree from a prior run, evidence the tool has been run but never published. Closing this properly needs a real ~32-minute-class measurement run to produce a first committed artifact, then wiring — correctly scoped as its own future cycle rather than rushed. Full investigation trail recorded in `state.json`'s FR-905 entry and D-0020.

- **43 of 73 FRs verified — unchanged this cycle** (a deliberate non-closure, not a stall).

**Cycle 41:** Implemented **FR-906** (add performance, memory, determinism, and fault-injection gates). Surveyed all 4 dimensions independently: determinism was already fully gated via CI (`determinism-compare` job, confirmed BLOCKING in `.github/required-checks.json`, enforced transitively via `remote-ci-green` — no work needed); performance had a real, working measurement (`bench/ttff/runner.mjs`, PRD F11.2) that was simply never wired to any release gate; memory had zero existing infrastructure anywhere in the repo — built `bench/memory/runner.mjs` from scratch, deliberately mirroring `ttff`'s exact baseline/regression-factor/`--check` shape; fault-injection had a solid unit-level contract (`runAnnotatorAsync`, FR-106) but no proof the real wiring across engine.js's ~90 annotator call sites actually holds. Wired both `ttff-gate` and `memory-gate` into `release-check.mjs` (same `runNpmGate` pattern as `corpus-gate`/`layer-recall-gate`), proven in all 3 states (pass/regression-fail/no-baseline-fail) via direct execution for both. Built `test/fault-injection.test.js` using Node's `mock.module()` to inject a real throw into a real posture module before `engine.js`'s own static import resolves it — genuine end-to-end proof, not simulation; confirmed the technique's real constraint (one module per process) by direct experiment before committing to a one-scenario design. Along the way: the repo's own advisory-scan hook caught a real TOCTOU in the new memory runner, traced to the *same* pattern already present in `ttff`'s runner (its model) and fixed in both; and a real D-0009-class fixture-pollution bug in the new fault-injection test was caught by reading actual failing-test output on a full `npm test` re-run, not assumed away, and fixed. Full gate: `npm test` exit 0 (4364/4364 + 26/26 + 1/1 fault-injection + python suites, re-verified clean twice), `test:posture` 1894/1894, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected), `bench:ttff:check`/`bench:memory:check` both exit 0.

- **44 of 73 FRs verified.** This is cycle 41 of up to 100.
- E9 is now 5 of 7 done (FR-901, FR-902, FR-903, FR-904, FR-906). FR-905 remains pending (D-0020); FR-907 blocked on it.

**Cycle 42:** Implemented **FR-1005** (produce privacy-preserving fleet summaries), the first requirement examined in the previously entirely-unexamined Epic E10. `posture/fleet.js` already exists and its own header states "no server, no account, no multi-tenancy" as a hard design line. Confirmed by direct code reading (not the comment alone) that all 3 layers — the per-repo result entry, the aggregate rollup, and the rendered HTML — structurally exclude raw finding snippets/source, carrying only counts, stableIds, and owner routing. No existing test guarded this property, so added one (same shape as FR-903's label-isolation guard), proven through a real `runFleet()` call with a secret-shaped snippet asserted absent from all 3 output layers. **Investigated FR-1006** alongside it (same file) and correctly left it pending: 2 of its 4 named views were already present (repo inventory, risk-vs-coverage-gap distinction); found and fixed a real bug along the way — `state.completed[repo].at` (scan freshness) was hardcoded `null` and never read anywhere, confirmed by grep — now stamped with a real timestamp on both success and failure, surfaced in the rendered HTML, proven by 4 new tests; but "policy drift" is a genuine, unbuilt gap needing its own design decision, and "assurance-health views" naturally depends on FR-204 (not built). Full gate: `npm test` exit 0 (4370/4370 + 26/26 + 1/1 fault-injection + python suites), `test:posture` 1900/1900, corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **45 of 73 FRs verified.** This is cycle 42 of up to 100.
- E10 opened (1/6, FR-1005). FR-1006 investigated, real progress made, correctly left pending.

**Cycle 43:** Implemented **FR-1002** (bind approvals, exceptions, and suppressions to verified identities and roles — anonymous or unauthorized high-risk exceptions fail policy). Survey (D-0014 method) found FR-307's own `remainingWork` field, written earlier this session, had already named this exact gap: `fixMeta.approval.approvedBy` is checked only for non-empty-string presence, never against any registered identity. Established up front what "verified identity" can honestly mean inside a tool whose own `fleet.js` states "no server, no account, no multi-tenancy" as a hard design line — no SSO/session exists to authenticate against, so inventing one would contradict the product's own stated architecture. Chose the same operator-authored-registry pattern this session has now used repeatedly for other policy surfaces (egress providers, privacy sinks, compliance severity floors): built `fix/approver-registry.js` (`loadApproverRegistry`/`verifyApprover`/`requiredRolesFor`), a no-op until an operator opts in by creating `.agentic-security/authorized-approvers.json`, at which point an anonymous or unregistered `approvedBy` is refused, and a per-category `requiredRolesByCategory` map (any-of-many semantics) can further require the registered approver to hold a specific role. Wired into the *existing* FR-307 gate in `apply-fix-service.js` immediately after its pre-existing presence check, not as a parallel mechanism. Registered the new config file in `artifact-registry.js` by hand (D-0018's lesson applied proactively — the completeness test's literal-scanning regex cannot see a `statePath()` call using a named constant). Proven end-to-end through the real `applyVerifiedFix()` call path in all 6 states: no-registry (FR-307 behavior provably unchanged), unregistered approver refused (file provably untouched), registered approver accepted, registered-but-missing-required-role refused, role-qualified approver accepted, and a category with no configured role requirement unaffected by another category's requirement in the same registry. Deliberately scoped to *approvals* only — extending the same identity-binding concept to *exceptions* and *suppressions* (also named in the acceptance criterion) was left as real, separate future work, since neither has an existing caller to bind to yet (matching this session's "prove one real caller per cycle" discipline rather than retrofitting speculatively). Full gate: `npm test` exit 0 (4391/4391 + 26/26 + 1/1 fault-injection + python suites), `test:report` 277/277, corpus 215/215 zero drift, self-scan zero drift (scanner/src 435/435, +1 baseline update for the new config-loader module, same accepted "Synchronous Blocking I/O" pattern as every other module of this shape this session), build clean, smoke exit 3 (expected).

- **46 of 73 FRs verified.** This is cycle 43 of up to 100.
- E10 is now 2 of 6 done (FR-1005, FR-1002).

**Cycle 44 (remediation, not a new FR):** While surveying FR-1003 (separation-of-duties) per D-0014's discipline — "what currently records who approved/authored a fix, through the REAL call paths" — found that FR-307's/FR-1002's high-impact-change approval gate was unreachable from BOTH of its real production callers. Confirmed by direct execution, not assumption: (1) `mcp/tools.js`'s `apply_fix` tool schema has `additionalProperties: false` on `fixMeta` and never declared `approval` as a property — running the real schema validator against a payload with `fixMeta.approval` threw `unexpected property "approval"`, meaning a real MCP caller's approval evidence was rejected before the handler ever ran; (2) that same tool's OTHER branch — the caller-supplied `patch` path, which the tool's own description calls the one covering "~100% of findings that ship only a template" — writes via `applyFixHistory()` directly and never calls `applyVerifiedFix()` at all, so it had NO high-impact gate whatsoever for any input; (3) the CLI's `fix --apply` had no flag of any kind to supply approval evidence, so a CLI user hitting the gate could never approve a fix, ever. Fixed all three: added the missing schema property; added the same material-risk-classify-then-gate logic directly into the patch branch (dry_run still previews past it, matching FR-307's established behavior); added `--approved-by`/`--approval-reason` CLI flags. While wiring the schema fix, a FOURTH, coupled bug surfaced: `fix-verify.js`'s honesty-gate (FR-308) fired on ANY non-empty `fixMeta`, so an approval-only `fixMeta` (no completeness self-report) was silently gated on an unrelated FR-308 check demanding a `residual` field nobody was asked to supply — narrowed the trigger to fire only when a completeness-adjacent field (`residual`/`verdict`/`evidence`/`signals`) is actually present. Proven end-to-end through the REAL MCP schema (a direct `validate()` call against the real `apply_fix.inputSchema`) and the REAL CLI subprocess (`spawnSync` against `bin/agentic-security.js`) — not `applyVerifiedFix()` called directly, which is what the ORIGINAL FR-307/FR-1002 tests proved and is exactly why the gap was missed: a unit-level "proven end-to-end" claim did not, in fact, reach the true end. FR-307's and FR-1002's `state.json` evidence extended with this correction (their verified status is unchanged — the underlying requirement logic was always correct; only its real-world reachability was broken). New/extended tests: `test/mcp.test.js` (+5), `test/cli-fix-apply.test.js` (+2). Full gate: `npm test` exit 0 (4397/4397 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift (`mcp/tools.js` and `posture/fix-verify.js` both unchanged at their existing baselines), build clean, smoke exit 3 (expected).

- **46 of 73 FRs verified — unchanged this cycle** (a correction cycle, not a closure — see D-0024).
- A real, previously-undetected reachability gap in 2 already-"verified" requirements found and fixed before building anything new on top of it.

**Cycle 45:** Implemented **FR-1003** (add separation-of-duties rules — a configurable policy can prevent the patch author from self-approving a high-impact fix). Survey confirmed no "patch author" concept existed anywhere in the codebase. Applying cycle 44's D-0024 lesson directly this time: built the schema property, the service-layer check, the MCP caller-patch-branch check, AND the CLI flag together in ONE pass, with real-entry-point reachability tests from the start, rather than the service-first-schema-later sequence that caused D-0024's gap. Added `fixMeta.author` (a second self-reported identity, parallel in kind to `approvedBy` — this tool has no VCS-blame ground truth, so both are policy checks over claimed identities, documented as such). Built `checkSeparationOfDuties(registry, author, approvedBy)` in `fix/approver-registry.js` — a no-op unless the same `authorized-approvers.json` registry opts in via a new `separationOfDuties: {enabled: true}` key, matching every other dimension in this registry; when enabled, a case-insensitive, trimmed match between author and approver is refused, and a missing author claim passes (nothing to compare against). Wired into the existing FR-307/FR-1002 gate in `apply-fix-service.js`, the MCP `apply_fix` tool's caller-patch branch (built in cycle 44), and a new `--author` CLI flag — all three real call paths in this same cycle. Proven end-to-end through all three: `applyVerifiedFix()` directly, the real MCP JSON-RPC handler (including a schema-validation regression guard), and a real CLI subprocess — same-identity refused with the file/disk provably untouched in every case, different-identity accepted in every case. Full gate: `npm test` exit 0 (4411/4411 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **47 of 73 FRs verified.** This is cycle 45 of up to 100.
- E10 is now 3 of 6 done (FR-1005, FR-1002, FR-1003).

**Cycle 46:** Implemented **FR-1004** (exception owner, reason, scope, compensating control, and expiry — expired exceptions automatically reopen findings or fail the gate). Survey found the closer, more directly on-point precedent than the gate-level waiver pattern: `posture/suppressions.js`'s "pro" schema (`.agentic-security/suppressions.yml`) already implements a per-FINDING exception record with `reason`, `expires_at`, and a two-person rule — genuinely wired into the real scan pipeline (`bin/agentic-security.js`), not a dead module — missing only 3 of FR-1004's 5 named fields (owner, scope, compensating_control). Also discovered, by direct code reading, that "expired exceptions automatically reopen findings" was ALREADY TRUE in production (`applySuppressions`'s expiry branch returns the finding to the open set) but had ZERO test coverage anywhere, despite being live since before this session began. Added the 3 missing fields as required, alongside the existing checks, in `validateProSuppression` — a deliberate tightening (an existing suppressions.yml lacking them will now fail validation and reopen its findings, same as any other invalid entry already does). Deliberately did NOT make `scope` a functional matching-widening mechanism — the criterion asks the exception to document its scope, not to suppress more broadly, and widening would be an unrequested security-control weakening. Confirmed "or fail the gate" is already satisfied transitively (a reopened finding is an ordinary open finding by the time `ci --fail-on` evaluates it). Proven end-to-end at 3 levels: `validateProSuppression`'s new required fields, `applySuppressions` at the unit level (valid/expired/invalid), and a REAL CLI end-to-end proof against a genuine detector-triggering finding (scan once to learn its real shape, then prove an expired suppression reopens it and a valid one suppresses it). A first full `npm test` run correctly caught a real side effect — `test/no-dead-modules.test.js`'s ALLOWLIST had a stale entry for `validateProSuppression` (previously dead from the test suite's perspective; no longer, once the new test file imports it directly) — fixed and re-verified clean on a second full run. Full gate: `npm test` exit 0 (4419/4419 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **48 of 73 FRs verified.** This is cycle 46 of up to 100.
- E10 is now 4 of 6 done (FR-1005, FR-1002, FR-1003, FR-1004).

**Cycle 47:** Implemented **FR-1001** (support signed portable policy bundles with organization, repository, and environment inheritance — tampered or expired policy is rejected; effective policy is explainable). Survey confirmed no policy-inheritance concept existed anywhere in the codebase, and compared the two candidate signing primitives named in the prior handoff: `posture/integrity.js`'s per-install symmetric HMAC (unusable — tamper-evidence for the SAME install only, but a policy bundle is authored by an org and verified by many OTHER installs that never had the signing key) vs. `posture/evidence-bundle.js`'s Ed25519 scheme (verify with only a public key — exactly the portability needed). Reused the Ed25519 primitive directly rather than building a third mechanism. Built `posture/policy-bundle.js`: sign/verify with the same canonicalisation shape as evidence-bundle.js, shallow top-level-key-override inheritance (organization → repository → environment, most-specific-wins) with per-key `provenance` (the "explainable" half). Applied evidence-bundle.js's own hard-won EA-03 lesson (allowlist-signed-fields, reject any top-level key outside it) from the FIRST write rather than discovering the same gap independently a cycle later. Wired into two new real CLI commands built together (D-0024's lesson): `policy-sign` (operator-facing signing, generating/reusing a keypair the same way `cmdAttest` already does, but in a separate key directory — different trust domains must not share a key) and `policy-explain` (verifies whichever bundles exist, prints accepted scopes, every REJECTED bundle's scope+reason, and the effective policy with provenance). The dead-module guard correctly caught an initially-unwired `ensurePolicyKeyPair` convenience export — rather than deleting or allowlisting it, built `policy-sign` as its real, tested caller, closing the operator loop end-to-end. Proven end-to-end: 6 distinct rejection modes (wrong key, tampered, EA-03 stapled-key, expired, no-public-key, unsigned), 3-scope inheritance with correct precedence and provenance, a tampered-or-expired bundle excluded from the merge while others still apply, and a real CLI round trip (`policy-sign` → `policy-explain`). Full gate: `npm test` exit 0 (4441/4441 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift (scanner/src 436/436, +1 baseline update — same accepted "Synchronous Blocking I/O" pattern as every other config-loader module this session), build clean, smoke exit 3 (expected).

- **49 of 73 FRs verified.** This is cycle 47 of up to 100.
- E10 is now 5 of 6 done (FR-1005, FR-1002, FR-1003, FR-1004, FR-1001) — only FR-1006 remains to close the epic.

**Cycle 48:** Implemented **FR-1006** (repository inventory, policy drift, scan freshness, and assurance-health views — fleet output distinguishes risk findings from scan and governance coverage gaps), **closing Epic E10 entirely** (all 6: FR-1001–FR-1006). Repository inventory and scan freshness were already built in cycle 42. Survey found "assurance-health" was already computed per-scan (`computeScanHealth`, FR-206) but never surfaced at the fleet level — zero references in `fleet.js`. "Policy drift" had no prior concept, but FR-1001 (built the immediately preceding cycle) supplies exactly the missing primitive. Added `scanHealth` pass-through and a new `computePolicyDrift(repo)` (in `posture/policy-bundle.js`, reusing `resolveEffectivePolicy`) to every fleet per-repo entry — reporting repository/environment-scope overrides of an organization key (naming the key, both values, and which scope won) and any tampered-or-expired bundle rejection (the policy an operator believes is enforced is silently not). Satisfied the literal acceptance criterion structurally, not just by field presence: `rollupFleet` counts governance gaps SEPARATELY from the risk rollup, `renderFleetSummary` reports them in their own "GOVERNANCE:" clause, and `renderFleetHtml` puts risk findings and governance/coverage gaps in two separate `<h2>`-headed sections rather than one merged table. Deliberately did not force a design for a richer "assurance-health view" depending on FR-204 (P0, still unbuilt) — surfacing the real signal that already exists is the honest reading of the criterion, not a substitute for the fuller per-analyzer ledger FR-204 would eventually add. Along the way, found and fixed a real, latent gate-ordering gap: this session's own gate recipe runs `npm test` before `npm run build`, so a brand-new dist chunk from cycle 47's POST-test build sat untracked for a full cycle before `test/dist-chunks-tracked.test.js` finally caught it this cycle. Fixed via a minimal, deliberate `git add` (staging ONLY — verified via `git diff --cached --name-only` that no commit was created) plus a rebuild confirming no further chunk drift — see D-0027. Full gate: `npm test` exit 0 (4451/4451 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **50 of 73 FRs verified.** This is cycle 48 of up to 100.
- **Epic E10 is now fully closed** (E1, E3, E4-except-FR-403, E6, and now E10 — 5 of 10 epics fully closed).

**Cycle 49:** Implemented **FR-905** (publish false-positive adjudication and coverage methodology — the 4th, previously-unclosed category). Closed the exact 3-step path D-0020 recorded: (a) modified `bench/independent/why-missed.mjs` to persist its aggregate summary to a new committed file (`why-missed-summary.json`, schema-tagged, scope-disclosed); (b) actually RAN it for real across the full population — timed a small sample (2.2s/entry) to confirm the ~34-minute estimate, then ran all 920 current false negatives as a real, monitored background job to completion; (c) wired the committed summary into `accuracy-scorecard.js`'s `committedInputs`, rendering a new "### Missed findings" subsection, mirroring FR-901/FR-904's own established committed-file pattern. The real, honest result: 737/920 (80%) no-finding-at-all, 162/920 (18%) finding-present-but-suppressed, 21/920 (2%) wrong-file-or-CWE — exactly the "shape doesn't occur" vs "masked downstream" distinction the PRD names as the point of the exercise. A real `npm run scorecard` execution regenerated `docs/SCORECARD.md` with these figures, confirmed by reading the actual output. Along the way, a first full-gate attempt hung for **over 2 hours** mid-run on `test/verifier.test.js` — reproduced as passing cleanly (22/22, <5s) when run standalone with zero code changes, confirmed as a transient environmental stall (killed the process tree, verified zero stray processes/ports/resource exhaustion) rather than a regression, and the full gate then completed clean in 4 minutes on a second attempt (within the session's "max 2 identical attempts" budget). Full gate: `npm test` exit 0 (4473/4473 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean (zero new untracked dist chunks, checked per D-0027), smoke exit 3 (expected).

- **51 of 73 FRs verified.** This is cycle 49 of up to 100.
- E9 is now 6 of 7 done — only FR-907 remains, previously noted as blocked on FR-905.

**Cycle 50:** Implemented **FR-504** (bind conclusions to repository, commit, scope, engine, ruleset, analyzer health, and mapping version) and **FR-506** (evidence freshness, owner, reviewer, exception, expiry), both in `posture/compliance-policy.js` — the customer-DSL "compliance-as-code" evaluator (confirmed as the real target over `auditor-walkthrough.js`, since its own header already states its output is "consumable by Vanta/Drata/SecureFrame/auditors," matching FR-508's own "external GRC ingestion" wording). FR-504: built `computeEvidenceDigest` — a sha256 of an explicit allowlist, mirroring `attestation.js`'s own canonicalisation shape rather than a fourth scheme; wired into every real `verifyPolicy()` call, with commit auto-resolved via `git rev-parse HEAD` (matching `sbom-diff.js`'s established pattern). Proven independently sensitive to each of 5 named bound inputs plus control outcome, deterministic across identical inputs, and insensitive to control array ordering. FR-506: extended the DSL with 5 new optional per-control fields — `owner`/`reviewer`/`reviewed-at`/`review-interval-days` (opt-in staleness: exceeding the interval demotes an otherwise-compliant control to a new `stale` status) and a richer structured `not-applicable` exception form (`{reason, owner, expires-at}` alongside the legacy bare `true`, which never expires) — an EXPIRED structured exception reopens the control as a new `gap` status rather than silently reverting, splitting the acceptance criterion cleanly: expired evidence → `stale`, expired exception → `gap`. Both requirements' summary counts and per-control metadata surface in both the JSON-LD and markdown evidence artifacts, present only when actually set. Full gate: `npm test` exit 0 (4473/4473 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift (`compliance-policy.js` unchanged at its existing baseline of 5), build clean, smoke exit 3 (expected).

- **53 of 73 FRs verified.** This is cycle 50 of up to 100.
- E5 is now 5 of 8 done (FR-501, FR-502, FR-504, FR-506, FR-507).

**Cycle 51:** Implemented **FR-505** (sign evidence manifests when signing is configured — signature verification detects altered findings, scope, policy, or evidence references). Built `posture/compliance-evidence-signing.js`, deliberately REUSING `evidence-bundle.js`'s existing Ed25519 keypair rather than a third or fourth key domain — a compliance evidence manifest and a finding evidence bundle make the same kind of claim (self-attestation by the running install), unlike `policy-bundle.js`'s org-distributed trust domain (D-0026's distinction, applied a second time, correctly). Applied D-0026's own EA-03 lesson from the first write: `canonicalComplianceEvidenceBytes` signs an explicit allowlist of every field `emitEvidenceJsonLd()` can produce, and verification rejects any unrecognised top-level key. Deliberately signed the FULL rendered document, not just FR-504's narrower `evidenceDigest` (which binds only `{id,status}` per control) — proven concretely by a test that edits a control's `narrative_evidence` bullet with its status unchanged and confirms the full-document signature catches it where the digest alone would not. "When signing is configured" implemented as a genuine no-op-until-opted-in: `loadSigningKeyIfConfigured()` only ever READS an existing key (never generates one), deliberately different from `agentic-security attest`'s own auto-generate-on-first-use default, since compliance evidence is emitted automatically on every routine scan and silently creating key material as a side effect would be unwanted. Extended the existing `verify-attestation` CLI command to auto-detect a compliance evidence manifest as a third recognizable shape (alongside run-attestation and finding-evidence-bundle), reusing one operator-facing verification entry point. The dead-module guard correctly caught 2 speculative convenience-wrapper exports with no real purpose once the key-reuse decision was made — deleted rather than kept or allowlisted. Proven end-to-end through the real CLI: sign → write → `verify-attestation` succeeds → tamper → `verify-attestation` fails. Full gate: `npm test` exit 0 (4486/4486 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift (scanner/src 437/437, +1 baseline update — same accepted "Synchronous Blocking I/O" pattern), build clean (zero new untracked dist chunks), smoke exit 3 (expected).

- **54 of 73 FRs verified.** This is cycle 51 of up to 100.
- E5 is now 6 of 8 done (FR-501, FR-502, FR-504, FR-505, FR-506, FR-507) — only FR-503 and FR-508 remain.

**Cycle 52:** Implemented **FR-503** (require positive evidence for satisfaction where the control demands implementation proof — mere artifact existence or absence of findings is insufficient unless the mapping explicitly defines it). Survey confirmed `compliance-policy.js`'s `file-exists`/`documented` primitive treats mere existence as sufficient with no way to demand content proof. Built a NEW, additive `file-contains: {path, pattern}` primitive rather than weakening or replacing `file-exists` — a mapping author now has a way to explicitly opt into stronger proof, while every control that genuinely only needs existence (e.g. "an incident response plan exists") keeps working unchanged. Deliberately did NOT attempt to audit or upgrade the bundled compliance framework JSON files to decide which existing controls should switch to the new primitive — that curation is separate, fuzzy-scoped work, left honestly for a future cycle per D-0020's discipline; this cycle built the capability, not its rollout. In passing, while touching the same verifier function, the session's own advisory-scan hook surfaced a real, pre-existing `existsSync`-then-`readFileSync` TOCTOU in the (previously completely untested) `sca-policy-has-entry` primitive — fixed with the established read-first-in-try/catch pattern, with 5 new tests added proving the fix changed no observable behavior; the file's self-scan count genuinely went DOWN (5→4) as a direct, verified result, not just an accepted new pattern. Full gate: `npm test` exit 0 (4497/4497 + 26/26 + 1/1 fault-injection + python suites, ~4.3 minutes, no repeat of the D-0030 hang), corpus 215/215 zero drift, self-scan settled at 436/436 (net -1 after individually reviewing a +1-then-fix sequence), build clean, smoke exit 3 (expected).

- **55 of 73 FRs verified.** This is cycle 52 of up to 100.
- E5 is now 7 of 8 done — only FR-508 remains to close the epic entirely.

**Cycle 53:** Implemented **FR-508** (export evidence in a stable API suitable for external GRC ingestion — exported records retain source references, status semantics, and schema version), **closing Epic E5 entirely** (all 8: FR-501–FR-508). The acceptance criterion is concrete, not vague — confirmed the existing signed JSON-LD artifact had none of the three explicitly: no machine-readable schema version (only an implicit `/v1/` inside a URL), no documented meaning for any status value, no per-export pointer to the policy file that produced it. Added `schemaVersion` (exported as `EVIDENCE_SCHEMA_VERSION`, with an explicit bump discipline documented: only for an incompatible meaning change, never for a genuinely additive field — matching how every field this session has added to this artifact has stayed backward-compatible), `statusSemantics` (an exported `STATUS_SEMANTICS` dictionary defining all 5 real status values so a consuming GRC tool never needs this codebase's own source to interpret one), and `policySource` (the relative path to the compliance policy mapping that produced the export). Kept `compliance-evidence-signing.js`'s (FR-505) EA-03-safe allowlist in sync with the 3 new fields, proven by 2 new tamper-detection tests. Along the way, a first full `npm test` run correctly caught a real violation of this repo's own state-path discipline: the new `policySource` field hardcoded a `.agentic-security/` string literal, which `test/no-stray-state.test.js`'s detector flags regardless of whether the literal is used for an actual file operation or just a display string. Fixed properly — not allowlisted around, which the guard's own error message explicitly forbids — by exporting the previously-private `STATE_DIR_NAME` constant from `state-dir.js` and consuming it. Full gate: `npm test` exit 0 (4500/4500 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3 (expected).

- **56 of 73 FRs verified.** This is cycle 53 of up to 100.
- **Epic E5 is now fully closed.** 6 of 10 epics now fully closed: E1, E3, E4 (except FR-403), E5, E6, E10.

## Next action

Per state.json's `exactNextAction`: with E5 closed, the largest genuinely unexamined territory is **E7** (FR-702/705/706) and **E8** (FR-802/803/805/806) — neither has been surveyed even once this entire session. Survey both fully (per D-0014) before picking the next target. Also outstanding: **FR-907** (E9 — needs a real new aggregation module across 5 already-separate mechanisms, likely its own dedicated cycle), **E2** (FR-201-205/207/208, deferred as a unit per D-0008/D-0028 — do not attempt without a deliberate multi-cycle plan), and **FR-403** (large, deferred).
**Cycle 54:** Implemented **FR-702** (enforce default and maximum TTL by artifact class — expired caches, scans, evidence, tickets, and backups are purged or archived according to policy). Extended `artifact-registry.js` (FR-701's registry) with a `retentionClass` field on ~30 of its 47 generated entries, deliberately conservative — only artifacts unambiguously fitting one of 5 named categories (cache/scan/evidence/ticket/backup) were classed; ongoing self-managing state was deliberately left classless rather than forced into a category. Built `posture/retention-policy.js`: two numbers per class (a `defaultDays` that applies unconfigured, and a `maxDays` CEILING an operator override cannot exceed — the literal "default AND maximum" wording asks for both a floor-free default and a hard cap, not just a knob), an optional `.agentic-security/retention-policy.yml` override (no-op-until-configured, never throws on missing/malformed input), and `findExpiredArtifacts()`. Implements PURGE, not archive — the acceptance criterion allows either, and archive implies a defined export format/destination that is FR-706's own separate scope; documented explicitly as a deliberate scoping choice. Wired a new `--expired` flag into `cmdReset()`, reusing its existing confirm/keep/preserve safety machinery rather than a second deletion path; operator-config is never touched regardless of age. Proven end-to-end via 17 new tests including a real-CLI case showing an operator-configured SHORTER TTL correctly expires an artifact the built-in default would not, and 3 real-subprocess `reset --expired` tests (dry-run, --yes, nothing-expired). Full gate run twice (before and after staging the new dist chunk from cmdReset's new dynamic import): `npm test` exit 0 both times (4517/4517 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift (baseline updated 436→437 for the new module), build clean (new `dist/730.index.js` staged per D-0027), smoke exit 3 (expected).

- **57 of 73 FRs verified.** This is cycle 54 of up to 100.
- E7 is now 4 of 7 done (FR-701, FR-703, FR-704, FR-702) — FR-705, FR-706, FR-707 remain.

## Next action

Per state.json's `exactNextAction`: with E7 partially progressed, the largest genuinely unexamined territory is **E8** (FR-802/803/805/806 — FR-801/804 already verified, E8 itself never surveyed this session) and **E7's remainder** (FR-705 encrypt confidential state classes, FR-706 manifest-based export/deletion reports — natural next step given FR-702's purge-only scoping explicitly defers "archive" to it, FR-707 legal hold). Also outstanding: **FR-907** (E9 — needs a real new aggregation module across 5 already-separate mechanisms, likely its own dedicated cycle), **E2** (FR-201-205/207/208, deferred as a unit per D-0008/D-0028 — do not attempt without a deliberate multi-cycle plan), and **FR-403** (large, deferred).

**Cycle 55:** Implemented **FR-802** (require organization-specific inputs before presenting a "likely organizational loss"), **FR-803** (present ranges, assumptions, source versions, and confidence), and **FR-805** (support scenario comparison — conservative/base/severe — without changing finding severity), all in `posture/risk-dollars.js`, advancing **E8 to 3/6** (FR-801, FR-804 already verified; FR-802/803/805 this cycle; FR-806 remains). A background survey confirmed FR-802 was only half-built: FR-801's scenario disclosure already refused to claim organizational-loss language, but 4 of its 5 named inputs (organizationScale/industry/recordCount/controlStrength) had NO config-collection path at all — the label could never even theoretically advance. Extended `risk-config.yml`'s tiny parser to collect all 4 as flat top-level scalars, added a `REQUIRED_FOR_ORGANIZATION_SPECIFIC_LOSS` gate tracked separately from the existing broader `ORG_SPECIFIC_DIMENSIONS` list (so the bonus `familyBaseProb` knob can never substitute for a real required input), and added a third scenario status — `scenario_organization_specific` — reached only when all 5 are configured, the only status permitted to use the "likely organizational loss" phrase. This directly realizes the `--risk-config <path>` CLI option the PRD names in §12.1. For FR-803/FR-805, built a single coherent mechanism rather than two divergent ones: `SCENARIO_MULTIPLIERS` (conservative/base/severe) perturb only probability and impact — the two GENERIC assumption inputs FR-801 already discloses as scenario defaults — while reachability discount and confidence weight (both measured facts about the specific finding) stay fixed across scenarios; `range: {low, base, high}` is literally the same three numbers as `scenarios`, not a second uncertainty model. Every `riskDollars` object now also carries `assumptions[]` (naming the actual source of each factor — EPSS vs. operator override vs. built-in table, which impact/reachability tier), `modelVersion` (a bumped-only-on-formula-change semver string), and `confidence` (a coarse low/medium/high tier derived from scenario completeness folded with detection confidence). `ev` remains numerically identical to before (the base scenario, multiplier 1.0), proven by test rather than asserted. One pre-existing test's assertion was intentionally updated — not weakened — to reflect the new, more precise disclaiming wording (see D-0034). Full gate: `npm test` exit 0 (4531/4531 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean (no new dist chunks this cycle), smoke exit 3 (expected).

- **60 of 73 FRs verified.** This is cycle 55 of up to 100.
- E8 is now 3 of 6 done (FR-801, FR-804, FR-802, FR-803, FR-805 — only FR-806 remains, confirmed genuinely greenfield with zero existing precedent).

## Next action

Per state.json's `exactNextAction`: with E8 nearly closed (only FR-806 — customer opt-in calibration against realized/accepted incidents, greenfield, privacy-preserving aggregation — remains), the highest-value next steps are: **FR-806** itself (P2, but closes E8 entirely), **E7's remainder** (FR-705 encrypt confidential state classes, FR-706 manifest-based export/deletion reports, FR-707 legal hold), **FR-907** (E9's last item — needs a real new aggregation module across 5 already-separate mechanisms), **E2** (FR-201-205/207/208, deferred as a unit per D-0008/D-0028 — do not attempt without a deliberate multi-cycle plan), and **FR-403** (large, deferred).

**Cycle 56:** Implemented **FR-706** (manifest-based export and deletion reports — "operators can prove what was exported, deleted, retained, or failed"), advancing **E7 to 5/7**. A background survey of E7's remainder (FR-705/706/707) confirmed FR-706 was the most tractable: `cmdReset` already computed exactly the right target/preserved data, it just never wrote any of it to a durable file, and no `export` command existed at all. Built `posture/state-lifecycle-report.js` (a single-overwritten "last action" file per kind, same precedent as `last-scan.json` — documented explicitly so it isn't mistaken for an audit LOG). Wired a `deletion-report.json` into every `reset` invocation (dry-run writes `planned` items, `--yes` writes `deleted`/`failed` items with real error messages — and `cmdReset` now genuinely returns 1 on a real deletion failure, a previously-silent gap since failures were only ever `console.error`'d). Built a new `agentic-security export --out <dir>` command that copies every CURRENTLY-PRESENT registered artifact — generated AND operator-config, unlike reset, since an export is a snapshot for migration/legal-preservation rather than a deletion decision — computing a real sha256 per file and writing both an `export-manifest.json` at the destination and a mirrored `export-report.json` under `.agentic-security/`. Both new report writers route through the existing `safeWriteState` seam, so FR-704's `--no-state` guarantee applies for free with no new wiring. Registered both new artifacts in the registry as `evidence`-class. Hit the well-documented D-0004/D-0006/D-0009 "S7 no prior scan" combined-run flake twice across 3 full-gate attempts (2 failed with the identical 4-test signature unrelated to this cycle's diff, 1 passed clean) — confirmed via standalone reruns (32/32 clean) and did not chase a 4th attempt, per D-0006's own established precedent (see D-0035). Full gate: one clean `npm test` run (4544/4544 + 26/26 + 1/1), corpus 215/215 zero drift, self-scan zero drift, build clean (one new dist chunk, `dist/552.index.js`, staged per D-0027), smoke exit 3 (expected).

- **61 of 73 FRs verified.** This is cycle 56 of up to 100.
- E7 is now 5 of 7 done (FR-701, FR-702, FR-703, FR-704, FR-706 — only FR-705 encryption and FR-707 legal hold remain).

## Next action

Per state.json's `exactNextAction`: with E7 down to 2 remaining items (FR-705 confidential-state encryption, confirmed genuinely greenfield — no crypto-provider abstraction exists anywhere in `src/posture/`; FR-707 legal hold, confirmed to need a third instance of the `{reason, owner, expires_at}` shape already used twice elsewhere), and E8 down to 1 (FR-806, also greenfield), the remaining unclosed epics are E2 (deferred as a unit, D-0008/D-0028), E7, E8, and E9's FR-907. Next candidate: FR-707 (legal hold) is likely more tractable than FR-705 (encryption, which needs a new provider abstraction plus fail-closed enforcement) since it has two directly reusable exception-shape precedents (`suppressions.js`, `compliance-policy.js`) and one clear integration point (`retention-policy.js`'s `findExpiredArtifacts`).

**Cycle 57:** Implemented **FR-707** (legal hold and policy-authorized retention exceptions — "legal hold is identity-bound, reasoned, time-bounded where applicable, and auditable"), **closing Epic E7 entirely** (all 7: FR-701–FR-707). Built `posture/legal-hold.js` as a third instance of this codebase's recurring `{owner, reason, expires_at}` exception shape (already used by `suppressions.js` for findings and `compliance-policy.js` for compliance controls) — confirmed via D-0025's own precedent that a hold on a STATE ARTIFACT is a distinct subject, not reusable code from either. `expires_at` is optional: null means an INDEFINITE hold, directly matching the acceptance criterion's own "time-bounded where applicable" phrasing for genuine open-ended legal matters. `addLegalHold` validates all three required fields, refuses an unregistered artifact name (almost always a typo) and an already-past `expires_at` (a hold that protects nothing is refused, not silently accepted). The real design finding this cycle: a hold must be consulted at TWO points, not one — `retention-policy.js`'s `findExpiredArtifacts()` (already existed, defense in depth) AND `cmdReset`'s own PLAIN (non-`--expired`) path, which deletes every registered generated artifact unconditionally and would otherwise blow straight through a hold that only gated TTL expiry. Held items are pulled from the deletion targets in both modes and surface in FR-706's own preserved/deletion-report machinery with the hold's owner and reason named. `legal-holds.json` itself is registered as `operator-config` (not `generated`) despite being CLI-written, deliberately: a plain reset must never be able to delete the very record protecting other artifacts. New CLI surface: `legal-hold add/remove/list`. Full gate: `npm test` run twice (once before staging the new `dist/144.index.js` chunk, once after) — run 1 clean (4562/4562), run 2 hit the well-documented D-0004/D-0006/D-0009 "S7 no prior scan" flake again (unrelated to this cycle, standalone-confirmed 32/32 clean, not chased further per D-0035), corpus 215/215 zero drift, self-scan zero drift (baseline updated 437→438 for the same accepted "Synchronous Blocking I/O" pattern as every other config-loader module this session), build clean, smoke exit 3 (expected).

- **62 of 73 FRs verified.** This is cycle 57 of up to 100.
- **Epic E7 is now fully closed.** 7 of 10 epics now fully closed: E1, E3, E4 (except FR-403), E5, E6, E7, E10.

## Next action

Per state.json's `exactNextAction`: with E7 now fully closed, the remaining open territory is **E8's last item** (FR-806, greenfield calibration-against-realized-incidents work), **E9's last item** (FR-907, needs a new aggregation module across 5 already-separate mechanisms), **E2** (FR-201-205/207/208, deferred as a unit per D-0008/D-0028 — do not attempt without a deliberate multi-cycle plan), and **FR-403** (large, deferred). Only 3 epics remain not-fully-closed (E2, E8, E9), and both E8 and E9 are down to exactly one requirement each.

**Cycle 58:** Implemented **FR-907** (longitudinal production feedback measurement — "metrics separate user suppression, accepted risk, invalid finding, fixed finding, and verification outcome"), **closing Epic E9 entirely** (all 7: FR-901–FR-907). A design survey confirmed the 5 named categories already existed separately across 5 unrelated mechanisms with zero aggregation. Built `posture/production-feedback.js` as a pure read-only aggregator — introduces no new storage of its own, only a unified event shape over the 5 existing sources (`suppressions.js`, `triage-memory.js`, `sca-policy.js`, `fix-history.js`, `fix-metrics.js`). Handled "longitudinal" honestly rather than glossing over a real schema gap: 3 of the 5 sources are genuinely dated and support real day-bucketed trending (reusing `triage.js`'s own established cutoff-window pattern rather than inventing a new one), while 2 schemas (pro suppressions, SCA accept-risk entries) have no per-entry creation timestamp by design — those events are counted in their category total but reported separately as `undated`, the same disclosed-gap discipline used elsewhere in this codebase, rather than silently smeared across the window or dropped. Wired into the existing `triage trend` CLI surface (adds a "Production feedback" section) rather than inventing a parallel command. The dead-module guard correctly caught a real, useful side effect: `loadSoftAccepted`/`loadProSuppressions` had been sitting in the allowlist as previously-dead exports — removed per the guard's own shrinking-ledger discipline now that they have a real caller. Full gate: `npm test` run twice (run 1 clean, 4578/4578; run 2 hit the well-documented D-0004/D-0006/D-0009 "S7 no prior scan" flake again, standalone-confirmed unrelated, not chased per D-0035), corpus 215/215 zero drift, self-scan zero drift (baseline updated 438→439 for a genuine regex-heuristic false positive — a variable literally named `total` compared against 0, same accepted class as prior cycles), build clean (one new dist chunk, `dist/920.index.js`, staged per D-0027), smoke exit 3 (expected).

- **63 of 73 FRs verified.** This is cycle 58 of up to 100.
- **Epic E9 is now fully closed.** 8 of 10 epics now fully closed: E1, E3, E4 (except FR-403), E5, E6, E7, E9, E10.

## Next action

Per state.json's `exactNextAction`: only **E2** and **E8** (1 item) remain not fully closed, plus the standalone deferred **FR-403**. **FR-806** (E8's last item) is the only remaining requirement outside E2/FR-403 — closing it would leave exactly 2 items outstanding in the entire 73-requirement PRD: E2 (7 FRs, deferred as a unit per D-0008/D-0028) and FR-403 (large, deferred). FR-806 needs real design work before implementation (confirmed genuinely greenfield — no existing mechanism reports a realized incident or accepted-risk outcome tied to a dollar estimate anywhere in the codebase); read the PRD's own privacy-preservation language (E1/dataflow modules) for precedent on what "aggregated and privacy-preserving" should concretely mean before building.

**Cycle 59:** Implemented **FR-806** (validate model calibration against accepted and realized incidents where customers opt in), **closing Epic E8 entirely** (all 6: FR-801–FR-806) — **9 of 10 epics are now fully closed.** Confirmed genuinely greenfield: no existing mechanism anywhere reports a real-world outcome tied to a confidence/dollar estimate. Scoped "aggregated" deliberately, since this codebase has no SaaS control plane and takes no runtime cloud calls: aggregation happens WITHIN one installation across every feedback event recorded over time, documented explicitly as an interpretation rather than an oversight (this codebase's architecture and stated philosophy both reject a cross-organization aggregation server). Built `posture/calibration-feedback.js`: genuinely opt-in (`recordCalibrationFeedback` is the ONLY way a record is ever created — never auto-populated by a scan), privacy-preserving AT THE SOURCE (a record snapshots only the model's own prediction signals plus the outcome and an optional truncated note — never file path, line, or vulnerability text, proven by a dedicated test), and a `possibleMiscalibration` signal that only ever fires when both outcome buckets reach a reliable sample size, never asserting a verdict from too few samples. New CLI: `calibration-feedback record`, `calibration-report`. Along the way, this session's own advisory-scan discipline caught a real bug in the FIRST implementation: an `existsSync`-then-`readFileSync` TOCTOU before a whole-file rewrite — the exact anti-pattern this session has now fixed 5 times (D-0012/D-0022 precedent) — fixed by switching to a single `fs.appendFileSync` with explicit safety checks first, matching `fix-metrics.js`'s/`triage-memory.js`'s own established JSONL-append pattern; self-scan genuinely dropped from +3 to +1 as a direct, verified result. Full gate: `npm test` run twice (run 1 clean, 4597/4597; run 2 hit the well-documented D-0004/D-0006/D-0009 "S7 no prior scan" flake again, standalone-confirmed unrelated, not chased per D-0035), corpus 215/215 zero drift, self-scan settled at 440 (net +1 after the TOCTOU fix), build clean (one new dist chunk, `dist/1.index.js`, staged per D-0027), smoke exit 3 (expected).

- **64 of 73 FRs verified.** This is cycle 59 of up to 100.
- **Epic E8 is now fully closed.** 9 of 10 epics now fully closed: E1, E3, E4 (except FR-403), E5, E6, E7, E8, E9, E10.

## Next action

Per state.json's `exactNextAction`: **only 2 items remain in the entire 73-requirement PRD**: **E2** (FR-201/202/203/204/205/207/208, deferred as a unit per D-0008/D-0028 — do NOT attempt without a deliberate multi-cycle plan; see D-0028 for the exact decomposition) and **FR-403** (large, confirmed deferred, never yet surveyed in this session with the same depth as E2). The next cycle should either (a) begin E2's decomposition per D-0028's plan (small isolation helper first, migrate call sites in small gate-verified batches, never one mechanical pass) or (b) survey FR-403 fresh with D-0014's three-step method to confirm it is still accurately described as "large" before committing to (a) or (b). Given E2 was deferred twice already with a concrete decomposition plan already on record, and FR-403 has never been freshly re-surveyed at current HEAD, surveying FR-403 first is the lower-risk next step.

**Cycle 60 — CORRECTION, no code changes:** the cycle-57, 58, and 59 entries above (and their "N of 10 epics closed" summary lines) incorrectly claimed **"Epic E7 is now fully closed."** It is not — **FR-705** (encrypt confidential state classes) was surveyed as genuinely greenfield back in cycle 56 (see that cycle's own entry: "no `confidential`/sensitivity marker exists in the artifact registry, and no encryption-provider abstraction exists anywhere in `src/posture/`"), deliberately set aside that same cycle to prioritize the more tractable FR-706, and was never circled back to before FR-707 closed the OTHER two remaining E7 items and got mistakenly narrated as closing the whole epic. E7 is **6 of 7**, not 7 of 7. Caught this cycle by cross-checking every epic-closure claim directly against `state.json`'s per-FR `status` fields (`FR-705` reads `"pending"`) rather than trusting the running cycle-to-cycle narrative — the state.json itself was never wrong, only the prose summaries layered on top of it in status.md/handoff.md. `docs/implementation/assurance-hardening-handoff.md` has been corrected directly (it is a live snapshot, not a log); this entry is the correction for status.md, which stays append-only rather than silently editing the cycle-57/58/59 text above.

Corrected count: **64 of 73 FRs verified**, unchanged (FR-705 was never counted as verified — the state.json ledger was accurate throughout; only the epic-level narrative was wrong). **7 of 10 epics fully closed: E1, E3, E4 (except FR-403), E5, E6, E8, E9, E10.** E7 is 6/7. **3 items remain in the entire PRD: E2 (7 FRs), FR-403, and FR-705** — not 2 as previously claimed.

Also this cycle: **surveyed FR-403 fresh** at current HEAD with concrete file:line evidence (see D-0039). Verdict: genuinely large, NOT a "mostly built, needs evidence" case like most of this session's other deferred-then-closed items. `privacy-taint.js` covers 7 of 9 real sink categories via its own `SINK_PATTERNS` regex list (missing: generic storage/DB, queues — a bounded, same-shape addition) but does ZERO safe-transformation recognition (sanitization/masking/hashing/encryption/serialization never clear taint — confirmed by `privacy-inventory.js`'s own in-code admission, "this engine does not track sanitization/masking/encoding for privacy flows (see FR-403)") and does NOT plug into the general k=1 taint engine's catalog/SummaryCache/callGraph machinery at all — it is a standalone, shallower, per-file name-matcher, confirmed by it importing none of `summaries.js`/`catalog.js`/any call-graph object. That means real aliased/interprocedural/cross-file flow tracking (all three explicitly named in FR-403's own acceptance criterion) do not exist for privacy specifically, even though the GENERAL engine already has this capability for non-privacy findings. Closing FR-403 for real means wiring privacy sources/sinks into that general machinery (privacy classes become catalog `source` entries, sinks become catalog `sink` entries, sanitizer-gate plumbing gets reused) — a genuine architectural rework on the same scale as E2's own deferred decomposition, not a bounded patch. Per D-0028's own precedent ("do not attempt a large, structurally-coupled epic in an unplanned cycle just because 'keep going' is standing — deferring with a clear, evidence-based reason IS keeping going"), FR-403 is NOT attempted this cycle. No code changes this cycle — investigation and documentation-correction only.

- **64 of 73 FRs verified** (corrected count, unchanged from cycle 59 — only the epic-narrative was wrong). This is cycle 60 of up to 100.
- **7 of 10 epics fully closed: E1, E3, E4 (except FR-403), E5, E6, E8, E9, E10.** E7 is 6/7 (FR-705 remains). E2 remains untouched (deferred as a unit).

## Next action

**3 items remain in the entire 73-requirement PRD**, all requiring deliberate, non-rushed work: **FR-705** (E7's last item — needs a new confidentiality marker + encryption-provider abstraction + fail-closed write gate, genuinely greenfield but more tractable in scope than the other two), **FR-403** (E4, P0 — the highest-priority remaining item; needs the general-engine catalog/SummaryCache/callGraph integration described above, a multi-cycle architectural effort), and **E2** (7 FRs, deferred as a unit per D-0008/D-0028, with an existing decomposition plan). None of the three should be attempted in an unplanned single cycle. A future cycle picking one up should: for FR-705, design the confidentiality-marker + provider-interface shape before writing code (similar rigor to this cycle's FR-403 survey); for FR-403 or E2, follow their respective existing decomposition plans exactly rather than re-deriving a new approach.

**Cycle 61:** Implemented **FR-705** (encrypt confidential state classes when an encryption provider is configured or required — required encryption absence fails before sensitive state is written), **genuinely closing Epic E7 entirely this time** (all 7: FR-701–FR-707 — cross-checked directly against every FR's own `status` field in state.json, per the cycle-60 correction's own new discipline, before writing this claim). Designed deliberately conservatively (Phase 1 of a staged rollout, matching FR-503's build-the-mechanism-first precedent): added a new `confidential: true` axis to `artifact-registry.js` (separate from classification/retentionClass), marked on exactly `compliance-evidence.json`/`.md` — the two candidates with one well-isolated writer function each and no other production readers. Every other candidate from the earlier survey (`mcp-audit.log`/`egress-audit.log` — hash-chained NDJSON, chaining plaintext vs. ciphertext is a real open design question; `dpia.md`/`ropa.md`/`data-inventory.json` — written via `engine.js`'s internal helper, larger blast radius; `last-scan.json`/`findings.json` — read directly as plain JSON by dozens of commands, a migration on the scale of E2's own deferred work) is explicitly named and deferred in the registry's own header, not silently dropped. Built `posture/encryption-provider.js`: one real provider (`local-key`, AES-256-GCM, a per-install key generated via the same atomic hard-link publish pattern `integrity.js` already established — a SEPARATE key file, never reusing a key across cryptographic purposes), a `loadEncryptionPolicy` that is inert by default (no file = not configured, matching every other policy surface in this codebase), and the fail-closed gate itself: `maybeEncryptForWrite` returns `{ok:false}` with NO content field at all when encryption is required but unavailable, so a careless caller cannot accidentally write plaintext anyway. Both real call sites (`emitEvidenceJsonLd`/`emitEvidenceMarkdown`) check this before writing and skip entirely on failure, with an always-visible (not debug-gated) stderr message. Wired transparent decrypt-on-read into `verify-attestation`'s CLI path too, so an encrypted compliance evidence manifest still verifies end-to-end automatically. Self-scan drift (+3) was reviewed carefully given the security-sensitive nature of new crypto code: one TOCTOU finding was confirmed IDENTICAL in shape to `integrity.js`'s own already-accepted key-read pattern (deliberately mirrored from that module), one was the familiar accepted "Synchronous Blocking I/O" pattern — both verified by direct comparison, not rubber-stamped. Full gate: `npm test` exit 0 on the FIRST attempt (4614/4614 + 26/26 + 1/1 fault-injection + python suites — no D-0006 flake recurrence this cycle), corpus 215/215 zero drift, self-scan settled at 443, build clean with zero new dist chunks, smoke exit 3 (expected).

- **65 of 73 FRs verified.** This is cycle 61 of up to 100.
- **Epic E7 is now genuinely fully closed** (verified by reading every one of its 7 FRs' status fields directly from state.json, not by narrative count). **8 of 10 epics now fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 is NOT fully closed (FR-403 remains open, so E4 does not count as closed despite every other FR in it being verified). E2 remains untouched, deferred as a unit.

## Next action

**Only 2 items remain in the entire 73-requirement PRD, both already deliberately deferred with concrete, evidence-based decomposition plans**: **E2** (FR-201/202/203/204/205/207/208, deferred as a unit per D-0008/D-0028 — a ~150-200-call-site `engine.js` refactor) and **FR-403** (E4's last item, P0 — the highest remaining priority, needs the general taint engine's catalog/SummaryCache/callGraph machinery wired into privacy-taint.js, per D-0039). Neither should be attempted without a dedicated, careful multi-cycle plan. This is very likely the natural stopping point for this session's bounded autonomous loop: every other requirement has a justified terminal status, and the two remaining items are both large, well-understood, and correctly NOT rushed.

**Cycle 62 — planning only, no code changes:** produced a CONCRETE, technically-grounded 4-step decomposition plan for **FR-403** (D-0041), the last remaining P0 item, per the user's explicit request to plan it rather than begin implementation immediately. A focused technical-grounding pass read the actual `catalog.js`, `summaries.js`, `ir/callgraph.js`, and `sanitizer-gate.js`/`sanitizer-proof.js` code (not just architectural prose) and found the underlying machinery is ALREADY family-agnostic — the call graph and `SummaryCache` know nothing about SQL/XSS/privacy, only `catalog.js`'s flat sink/source/sanitizer entries differ per family. Concrete proof found: XXE (CWE-611) was added across 5 languages as PURE catalog-entry additions with zero changes to `engine.js`/`summaries.js`/`sanitizer-gate.js`/`callgraph.js` — the exact same shape FR-403's privacy sink categories need. The plan: (1) add a `privacy-leak` sink family to `catalog.js` mirroring `privacy-taint.js`'s existing 7 sink categories — zero engine changes, proven-safe shape, genuinely low-risk even done alone; (2) add a `declared-as` catalog matcher so a PII/PHI/PCI-classified DECLARATION becomes a source (the one real catalog-shape gap, since every existing source is an expression pattern); (3) wire privacy sources/sinks into the real CFG walk as an opt-in second lattice, bench-compared against the current shallow pass before ever becoming default — this is where real interprocedural/cross-file/alias tracking and sanitizer-clearing actually get proven; (4) cut the default path over only after 1-3 are independently gate-verified, reusing the already-existing `irBacked` flag rather than inventing a new one. Ordered by risk, not just dependency — step 1 is genuinely zero-risk and worth attempting even in isolation as real progress; step 4 is a one-way door on the default scan path and should never be bundled with step 3.

- **65 of 73 FRs verified** (unchanged — this was a planning-only cycle). This is cycle 62 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 not closed (FR-403 open, now with a real plan). E2 untouched.

## Next action

**Only 2 items remain in the entire 73-requirement PRD, both now carrying concrete, evidence-based decomposition plans**: E2 (D-0028) and FR-403 (D-0041). FR-403's D-0041 step 1 (a pure `catalog.js` sink-family addition, zero engine changes, matching the shipped XXE precedent exactly) is the lowest-risk, most tractable next real implementation step across either remaining item — genuinely worth attempting on its own even before steps 2-4. Awaiting explicit direction on whether to proceed with implementation.

**Cycle 63:** Implemented **D-0041 step 1 of FR-403** (add a `privacy-leak` sink catalog covering all 9 sink categories, zero engine changes) — genuine, gate-verified progress, though FR-403 as a whole remains correctly NOT verified (steps 2-4 are the acceptance criterion's actual substance: interprocedural/cross-file/alias tracking and safe-transformation clearing). **A real correction was found DURING implementation, not anticipated by the original grounding pass — D-0042**: the plan's "zero behavior change" framing for step 1 assumed the shipped XXE precedent (pure catalog additions, no engine changes) would generalize directly. It does not: `dataflow/engine.js`'s taint state is a SHARED boolean per variable with no per-family filtering, so appending privacy sinks straight into `catalog.js`'s shared `CATALOG` export would make every already-active general security source (`req.body`, `req.query`, etc.) immediately trigger spurious CWE-359 "privacy leak" findings the moment tainted data reached an ordinary `console.log`/`fetch`/`sendMail` call — a real, immediate false-positive class XXE's sinks never risked (XML-parsing calls aren't already reached by unrelated general sources the way log/response/fetch calls are). Caught this BEFORE writing the catalog entries by actually reading `dataflow/engine.js`'s worklist and `matchSinkOrSanitizer`'s implementation, not just trusting the grounding survey's architectural summary. Fixed by keeping the new catalog (`dataflow/privacy-catalog.js`) in a genuinely separate, currently-unconsumed file — inert by construction, not by a flag that could be forgotten. Verified the isolation is real, not just designed: corpus 215/215 zero drift, self-scan zero drift, full `npm test` clean, proving today's scan behavior is provably unchanged. The dead-module guard's own logic confirmed a test file counts as a real caller (no allowlist entry needed — an initial assumption to the contrary was wrong and self-corrected within the same cycle). Full gate: `npm test` exit 0 (4620/4620 + 26/26 + 1/1 fault-injection + python suites), corpus 215/215 zero drift, self-scan zero drift, build clean with zero new dist chunks (the module has no production importer yet, by design, so it isn't even in the shipped bundle until step 3 wires it in), smoke exit 3 (expected).

- **65 of 73 FRs verified** (unchanged — FR-403 correctly remains pending; this cycle's progress is real but partial, 1 of 4 planned steps). This is cycle 63 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 not closed (FR-403 open, now 1/4 steps done). E2 untouched.

## Next action

**Only 2 items remain in the entire 73-requirement PRD**: E2 (D-0028's decomposition, untouched) and FR-403 (D-0041's decomposition, step 1 of 4 done and gate-verified). FR-403's step 2 (a `declared-as` catalog matcher so a PII/PHI/PCI-classified declaration becomes a taint source) is the natural next increment — bounded, unit-testable against `privacy-taxonomy.js`'s existing compiled-taxonomy fixtures, and does not yet touch `engine.js`. Step 3 (the real CFG-walk wiring) is where genuine regression risk begins and needs its own before/after recall comparison per D-0042's isolation requirement — do not rush into it without that comparison in hand.

**Cycle 64:** Implemented **D-0041 step 2 of FR-403** (a `declared-as` source matcher so a PII/PHI/PCI-classified DECLARATION becomes a taint source) — the second of 4 planned steps, genuine gate-verified progress. Added `matchPrivacyDeclSource`/`matchPrivacyDeclSources` to `dataflow/privacy-catalog.js`, calling directly into `privacy-taxonomy.js`'s existing, already-tested `classifyFieldAgainst` rather than reimplementing classification logic. Modeled deliberately after `catalog.js`'s own `matchAnnotationParams` precedent — confirmed via `dataflow/CLAUDE.md`'s own documentation that annotation-style matching runs once per function against a side-channel, not mid-CFG-walk against an expression, which is exactly the shape a declaration-name classifier needs (there is no expression to match against; the fact being tested is "this name classifies as PII," not "this expression touches a specific callee"). Preserved D-0042's isolation guarantee throughout — these are pure, pre-walk functions with no engine.js touch and no reference to the general `CATALOG`. Proven against real regex-boundary edge cases, not just happy-path names: confirmed camelCase compound identifiers (`medicalRecordNumber`, `creditCardNumber`) correctly match their taxonomy patterns because those specific patterns have no internal `\b` boundary between sub-words, while a single-word pattern (`\bdiagnosis\b`) would NOT match a compound like `diagnosisCode` — caught and corrected this distinction while writing the test, not after a failure. Also proved the matcher honors a caller-supplied custom taxonomy (not just the built-in default), meaning step 3's eventual wiring will automatically respect FR-402's own organization-defined classes with zero extra work. Full gate: `npm test` exit 0 (4627/4627 + 26/26 + 1/1 fault-injection + python suites, clean on the FIRST attempt), corpus 215/215 zero drift, self-scan zero drift, build clean with zero new dist chunks, smoke exit 3 (expected).

- **65 of 73 FRs verified** (unchanged — FR-403 correctly remains pending; 2 of 4 planned steps now done). This is cycle 64 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 not closed (FR-403 open, now 2/4 steps done). E2 untouched.

## Next action

**Only 2 items remain in the entire 73-requirement PRD**: E2 (D-0028's decomposition, untouched) and FR-403 (D-0041's decomposition, 2 of 4 steps done and gate-verified). FR-403's step 3 (wiring privacy sources/sinks into the real CFG walk as an opt-in second lattice, bench-compared against the current shallow pass before ever becoming default) is where genuine regression risk begins — it needs its own before/after recall comparison on a real corpus, not just "tests still pass," and must preserve D-0042's isolation guarantee throughout. This is a meaningfully larger and riskier step than 1-2; it deserves its own careful, unhurried cycle rather than being rushed to keep momentum.

**Cycle 65:** Implemented **D-0028 step (a) of E2/FR-201** (a small, isolated detector-runner helper) — genuine, gate-verified progress on E2, the last remaining item in the entire PRD alongside FR-403. Built `src/pipeline/detector-runner.js`'s `runDetector(detectorErrors, file, analyzer, fn)`, modeled directly on the already-shipped FR-106 `runAnnotatorAsync` precedent's exact isolation contract (return value on success, capture-and-continue on any thrown exception) but deliberately synchronous, not async — confirmed all ~128 of `engine.js`'s per-file detector call sites are synchronous, so an async wrapper would add real Promise-scheduling overhead to the engine's single hottest loop for a capability nothing needs (D-0043). Error records carry `{file, analyzer, err}`, using the PRD's own literal "analyzer" vocabulary (FR-201's "per-analyzer error capture," FR-203's future per-analyzer coverage ledger) rather than "detector." Confirmed the real call-site count directly (128 via grep, consistent with D-0028's original 150-200 estimate) and the exact try/catch boundaries in `engine.js`'s per-file loop. Deliberately did NOT touch any of the 128 call sites this cycle — per D-0028's own explicit phasing, migrating them is step (b), a separate, larger, batched effort requiring its own gate run after every batch (not just once at the end). `pipeline/` is outside the dead-module guard's scanned directory set, matching `annotator-runner.js`'s own precedent of living there with no allowlist entry needed. Full gate: `npm test` exit 0 (4634/4634 + 26/26 + 1/1 fault-injection + python suites, clean on the FIRST attempt), corpus 215/215 zero drift, self-scan zero drift, build clean with zero new dist chunks, smoke exit 3 (expected).

- **65 of 73 FRs verified** (unchanged — FR-201 correctly remains `ready`, not `verified`; the acceptance criterion needs the real 128 call sites migrated, which is step (b), not yet started). This is cycle 65 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E2 now has its step (a) done (1 of D-0028's 3 named steps). E4 not closed (FR-403 open, 2 of 4 D-0041 steps done).

## Next action

**Only 2 items remain in the entire 73-requirement PRD**, both now with real, gate-verified partial progress: **E2** (D-0028 step (a) done; step (b) — migrating the ~128 detector call sites in small, gate-verified batches of ~20-30 — is the natural next increment) and **FR-403** (D-0041 steps 1-2 done; step 3 — the CFG-walk wiring with its own before/after recall comparison — is where real regression risk begins). Both are legitimate next targets. E2's step (b) first batch is likely the lower-risk pick: each batch is small, mechanical in shape (once the pattern is established), and independently gate-verifiable, versus FR-403 step 3's need for a dedicated recall-comparison harness before any wiring can be trusted.

**Cycle 66:** Implemented **D-0028 step (b) of E2/FR-201 IN FULL** — the last remaining step for FR-201, and the requirement is now `verified`. Migrated all 128 of `engine.js`'s real detector call sites (confirmed via grep before and after: 128 bare `aX.push(...scanY(...))` sites before, 0 after, 128 `runDetector(...)` occurrences after) to the isolation wrapper built in cycle 65, across 5 gate-verified batches (9, 25, 30, 30, 23 sites respectively, applied via a verified mechanical Python transform — each `old_string` confirmed unique via `content.count()==1` before replacement, `node --check` run after every batch) plus 2 hand-edited special cases and 8 cross-file/aggregate calls given a project-level or manifest-path file label:
- `scanGraphQL` — preserved its pre-existing `if(/\.(graphql|gql)$/i.test(p))` single-statement guard, wrapped only the push call.
- `scanRegexReDoS` — had a pre-existing nested `try{...}catch(_){}` that silently swallowed errors; converting it to `runDetector` is a deliberate behavior IMPROVEMENT (these errors now land in `_detectorErrors` instead of vanishing), not a pure refactor, and was called out as such rather than silently changed.
- 9 cross-file/aggregate calls (`scanConventionDeviationProject`, `scanApiBrokenAuthz`, `scanCrossService`, `scanRbacConsistency`, `scanConfigFiles`, `scanInstallScripts`'s cross-file call, `scanDbTaintCrossFile`, `scanStoredPromptInjectionCrossFile`, `scanDeserializationGadgets`) don't have a single per-file `p` in scope; given `'<project>'` as an honest file label (or the real manifest path for `scanInstallScripts`, which does iterate per-manifest).

Ran the FULL gate (`npm test` + corpus + self-scan) after EVERY one of the 5 batches, not just once at the end, per D-0028's explicit instruction — every single one passed clean on the first attempt (npm test 4634/4634, corpus 215/215 zero drift, self-scan zero drift, each time).

After the full migration, closed out FR-201's remaining acceptance-criterion gap: surfaced `_detectorErrors` on the scan result as `scan.detectorErrors` (`engine.js`'s return object) AND through `toJSON()` (`src/report/index.js`) — mirroring the existing `annotatorErrors`/`scan.annotatorErrors` precedent exactly. This second wiring step mattered: `test/annotator-errors.test.js`'s own file history documents that a field computed by the engine but dropped by `toJSON`'s allowlist is a real, recurring bug class in this codebase (licenseGraph, sbomDiff, entrypointInventory, several per-finding fields all had this exact gap found and fixed in earlier cycles) — checked for it here rather than assuming the new field would just flow through.

Added real END-TO-END proof, not just unit-level: `test/detector-fault-injection.test.js` uses `node:test`'s `mock.module()` to inject an actual throwing fault into a REAL sast module (`src/sast/secret-concat.js`) wired through a REAL `runScan()` over `test/fixtures/vulnerable-js` — proving FR-201's literal acceptance criterion ("one detector exception does not prevent subsequent independent detectors from running") against the real 128-call-site engine, not a synthetic stand-in. Mirrors `test/fault-injection.test.js`'s FR-906/annotator precedent exactly, including its documented one-scenario-per-process constraint — wired as its own step in `package.json`'s top-level `test` script. Also added `test/detector-errors.test.js` (3 tests) mirroring `annotator-errors.test.js`'s cheap always-run regressions (clean scan → `[]`, pre-populated round-trips through `toJSON`, default is `[]`).

Full final gate: `npm test` exit 0 (4637/4637 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection[new] + 5/5 python), corpus 215/215 zero drift, self-scan zero drift, build exit 0 with zero new untracked dist chunks, smoke exit 3 (expected).

- **66 of 73 FRs verified.** FR-201 now verified; E2 (FR-201..FR-208) has 1 of 8 done, 6 remain (FR-202/203/204/205/207/208) — E2 itself is NOT yet closed. This is cycle 66 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E2 now has real, gate-verified progress (FR-201 done) but is not closed. E4 not closed (FR-403 open, 2 of 4 D-0041 steps done).

## Next action

**7 requirements remain across 2 items**: E2 (FR-202/203/204/205/207/208 — FR-201 now done, and per D-0028's own sequencing, FR-202's real interruptible-deadline work is now unblocked since it depends on FR-201's isolation existing for real) and FR-403 (D-0041 steps 1-2 done; step 3 — the CFG-walk wiring with its own before/after recall comparison — is where real regression risk begins). FR-202 (worker/subprocess/interruptible deadline enforcement) is the natural next increment per the PRD's own dependency chain, but is a materially different and larger design problem than FR-201's mechanical migration — it needs its own grounding pass (what execution boundary is actually available/safe for ~128 heterogeneous detectors, many of which are synchronous CPU-bound regex/AST work that cannot be interrupted mid-statement without a real OS-level boundary like a worker thread or subprocess) before implementation starts.

**Cycle 67:** Grounded **FR-202** (E2's next item per D-0028's sequencing) via a dedicated investigation, recorded as **D-0046**. Confirmed both existing timeout-shaped mechanisms in the engine are non-preemptive: `engine.js`'s `_perFileTimeoutMs` measures elapsed time AFTER a file's whole 128-detector pass already completed (a hang never reaches the check at all), and the deep-mode `AGENTIC_SECURITY_DEEP_TIMEOUT_MS`/`deadlineMs` is a cooperative clock-poll every 128 worklist iterations that cannot rescue a hang inside a call with no iteration counter of its own — true of essentially all 128 pattern-matching detectors. This is a structural fact (JavaScript is single-threaded; nothing in the same event loop as a synchronous hang can preempt it), not a code-quality gap. Found `src/engine-parallel.js` is dead, unwired `AGENTIC_SECURITY_PARALLEL=1` scaffolding with zero real callers — a design sketch, not working infrastructure. Recorded a 4-phase decomposition mirroring D-0028's exact shape for FR-201: (1) an isolated, tested worker-termination primitive; (2) a work-unit data-contract design; (3) real per-file wiring gated behind an opt-in flag with a full accuracy/determinism/self-scan/corpus comparison; (4) default-cutover, explicitly deferred as out of scope for the literal acceptance criterion.

Implemented **phase 1 for real**: `src/pipeline/analyzer-supervisor.js`'s `runWithDeadline(spec, {timeoutMs, graceMs})` — a real `worker_threads`-based primitive that races a worker against a genuine `setTimeout` and calls `worker.terminate()` (an OS-level preemptive kill, unlike anything checked-clock-based) when the deadline elapses. Proven against a REAL, unmocked infinite loop (`test/fixtures/analyzer-supervisor/synthetic-analyzers.mjs`'s `hangForever()` — a genuine busy-wait with no I/O and no clock checks anywhere in it) — the literal FR-202 acceptance-criterion scenario, terminating within the configured 300ms+200ms window (~501ms measured). Also proved: a well-behaved call resolves with its real value; a thrown error is captured as `{ok:false,error}` rather than propagated or left hanging; a nonexistent export is reported cleanly; the primitive validates its own inputs rather than hanging on a malformed call. Zero `engine.js` changes — this is an isolated, currently-unwired primitive, exactly matching D-0028's own step (a) precedent for FR-201.

Full gate: `npm test` exit 0 (4643/4643 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 zero drift, self-scan zero drift, build exit 0 with zero new untracked dist chunks (the new modules have no importer yet, so ncc's static bundler correctly excludes them — noted for phase 3: `analyzer-worker.js` is referenced via a runtime string path to `new Worker()`, not a static import, and will need explicit bundling attention when real wiring happens), smoke exit 3 (expected).

- **66 of 73 FRs verified** (unchanged — FR-202 correctly remains `ready`, not `verified`; 1 of 4 D-0046 phases done). This is cycle 67 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E2 now has 1 of 8 FRs verified (FR-201) plus real, gate-verified partial progress on FR-202 (1/4 phases) — not closed. E4 not closed (FR-403 open, 2/4 D-0041 steps done).

## Next action

**6 requirements remain across 2 items**: E2 (FR-202 1/4 phases done — phase 2, the work-unit data-contract design, is the natural next increment, since it's a design task that clarifies exactly what phase 3's real wiring needs to serialize/broadcast before any engine.js code is touched; FR-203/204/205/207/208 remain fully unstarted, downstream of FR-202) and FR-403 (2/4 D-0041 steps done, step 3 — the CFG-walk wiring — is where real regression risk begins). Both remain large, well-understood, and correctly not rushed.

**Cycle 68:** Grounded **FR-403's step 3** (the CFG-walk wiring, flagged since cycle 62 as "where real regression risk begins") via a dedicated investigation, recorded as **D-0047** — mirroring D-0046's approach for FR-202. Confirmed exactly where the general engine's CFG walk consults `catalog.js` (`matchSource`/`matchSinkOrSanitizer`, called through `_matchCallCatalog`/`_sinkFindingsForCall` from `step`'s node-kind switch), and confirmed the per-variable taint state is a bare `Set<string>` with no per-path metadata — meaning a privacy dimension cannot piggyback on the existing Set's elements, only run as a genuinely parallel structure. Confirmed `_unionAnnotationTaint` is the exact, already-proven precedent for turning `matchPrivacyDeclSource(s)`'s pre-walk classification into an entry-state contribution.

Faced a real design choice: thread a second Set through `step`/`exprTaint`'s existing signature (more code-efficient, but requires touching the single hottest, most heavily-relied-upon function in the entire taint engine — ~455 lines, dozens of internal `state`-as-bare-Set references) versus building a genuinely separate walker module with zero shared code paths. **Chose the separate walker (D-0047)** — extending D-0042's isolation mandate from catalog DATA to the WALKER CODE PATH itself, on the principle that a shared representation is a shared failure surface, and the general engine's already-corpus/self-scan-proven correctness should not be put at risk for an orthogonal, still-opt-in capability.

Implemented the smallest safe increment toward step 3: `matchPrivacySink(calleeExpr, file, receiverType)` in `privacy-catalog.js`, mirroring `matchSinkOrSanitizer`'s exact contract via an independent linear scan over `PRIVACY_SINK_CATALOG` (no shared index machinery — only the pure `_languageFamilyExtensions` helper is reused). Proven against both real IR callee shapes (structured member-expression and flat dotted-string), receiver-type gating in both directions, language gating, and full catalog coverage. Still zero `engine.js` changes — the actual standalone walker construction remains the concrete next step, deliberately deferred rather than rushed within this cycle.

Full gate: `npm test` exit 0 (4649/4649 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 zero drift, self-scan zero drift, build exit 0 with zero new untracked dist chunks, smoke exit 3 (expected).

- **66 of 73 FRs verified** (unchanged — FR-403 correctly remains `pending`, this cycle's progress is design + one small pure addition, not a step-3 completion). This is cycle 68 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E2 has 1/8 FRs verified plus FR-202 at 1/4 phases. E4 not closed (FR-403 open, 2/4 D-0041 steps done, step 3 grounded+designed but not built).

## Next action

**6 requirements remain across 2 items**: FR-403's step 3 (the actual standalone intra-procedural privacy walker construction, per D-0047's design — a genuinely new module, not a modification of `step`/`exprTaint`) and E2's FR-202 phase 2 (the work-unit data-contract design/audit of all 128 detectors for side-channel mutable state). Both are real, bounded, well-understood next steps; neither should be rushed given each was explicitly flagged as carrying real regression risk if done carelessly.

**Cycle 69:** Built and wired FR-403 step 3's v1 privacy taint walker for real, per D-0047's design. `dataflow/privacy-deep-walker.js` implements a genuinely separate, intra-procedural CFG walker (`analyzePrivacyFunction`/`runPrivacyTaintEngine`) reusing only `access-paths.js`'s pure Set utilities — zero shared code paths with `dataflow/engine.js`'s `step`/`exprTaint`. Seeds entry state from `matchPrivacyDeclSources(fn.params)` (mirroring `_unionAnnotationTaint`'s exact precedent), propagates taint through assignment including the declaration-name-itself-is-a-source path (D-0041 step 2's whole purpose), and checks `matchPrivacySink` at call/return nodes. Wired into `dataflow/index.js`'s `runDeepAnalysis` behind `AGENTIC_SECURITY_PRIVACY_DEEP=1` (default off), mirroring the existing `AGENTIC_SECURITY_IFDS=1` wiring exactly — an additional, independently-failing pass whose findings merge in, never replacing the primary engine's output.

Proved this two ways: 9 hand-constructed-fn unit tests (mirroring `test/annotation-taint-engine.test.js`'s established pattern) covering source classification, propagation, true-negative clean-reassignment, and error isolation; and a real end-to-end test (`test/privacy-deep-e2e.test.js`, mirroring `test/deep-taint.test.js`'s pattern) scanning a real fixture through the real parser — a declaration named `socialSecurityNumber` (classified by NAME alone; its RHS does not independently classify, proving the declaration-source path specifically) fires a real `IR-PRIVACY-TAINT`/`CWE-359` finding with the flag on, and — the critical safety proof — the same fixture produces ZERO such findings with deep mode on but the flag unset, and zero with deep mode off entirely.

This is real, substantial progress on FR-403's acceptance criterion (direct-flow, declaration-based privacy taint now genuinely exists and is proven), though FR-403 as a whole correctly remains `pending` — aliasing, interprocedural flow, cross-file flow, and safe-transformation recognition are all still unmodeled in v1, and the before/after recall-comparison harness against `annotatePrivacyTaint` still needs to be built.

While the privacy walker work was underway, a background investigation completed **FR-202's D-0046 phase 2** (the full 128-detector side-channel audit, recorded as **D-0048**): corrected phase 1's "dozens of detectors write to `_suppressionLog`" estimate down to 4 real in-loop writers (the rest are post-loop orchestration code); confirmed `_projectIndex` and `SOURCE_PATTERNS`/`SINK_PATTERNS`/`SANITIZER_PATTERNS` as additional broadcast-once candidates; confirmed no hidden non-per-file detector and no order dependency between detectors within one file's dispatch; and found one genuine NEW bug independent of FR-202 — `src/sast/cpp.js`'s `_entropyMod` is an async module-load-time singleton whose current single-process timing happens to resolve before use, but a worker-per-file design would race it, potentially disabling the entropy false-positive filter for early C/C++ files. Not yet fixed — flagged as a small, independent, low-risk correction for a future cycle.

Full gate for the walker wiring: `npm test` exit 0 (4662/4662 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 ZERO drift (confirms zero behavior change to the 6 deep-tier corpus entries), self-scan ZERO drift, build exit 0 (the new modules are now bundled into the main `dist/index.js` via the real static import chain — size grew from 3876kB to 3887kB as expected, zero new untracked dynamic-import chunks), smoke exit 3 (expected).

- **66 of 73 FRs verified** (unchanged — FR-403 correctly remains `pending`; this cycle's step-3 v1 is real, substantial, gate-verified progress but not the full acceptance criterion). This is cycle 69 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E2 has 1/8 FRs verified plus FR-202 at 2/4 D-0046 phases (audit done, wiring not started). E4 not closed (FR-403 open, 2/4 D-0041 steps fully done, step 3 v1 substantially built).

## Next action

**6 requirements remain across 2 items**: FR-403's step 3 continuation (aliasing/interprocedural/cross-file/safe-transformation, plus the recall-comparison harness — each a real, separate increment) and E2's FR-202 phase 3 (real per-file worker wiring into `engine.js`'s main loop, now fully scoped by D-0048's checklist, plus the independent `_entropyMod` race fix it surfaced). Both remain large; neither should be rushed.

**Cycle 70:** Fixed the `_entropyMod` async-singleton race D-0048 flagged as FR-202 phase 3's prerequisite. `src/sast/cpp.js`'s `cpp-hardcoded-secret` rule resolved `_secret-entropy.js` via a fire-and-forget `import(...)` into a module-level `let`, racing the first `scanCpp()` call — today's single-process design always wins that race, but a worker-per-file boundary would give each worker no equivalent warm-up guarantee, potentially disabling the entropy false-positive filter for an early C/C++ file. The stated justification ("avoids a circular dep") did not hold up: `_secret-entropy.js` has zero imports of its own, and `engine.js` already imports the same function statically with no issue. Fixed to a plain static import — now synchronous and race-free by construction. Added `test/cpp-hardcoded-secret.test.js` (3 tests) — this rule had zero direct test coverage before the fix, a real gap closed alongside the bug, matching this session's established precedent.

Full gate: `npm test` exit 0 (4665/4665 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3.

- **66 of 73 FRs verified** (unchanged — this was a prerequisite bug fix for FR-202 phase 3, not a phase completion itself). This is cycle 70 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E2 has 1/8 FRs verified plus FR-202 at 2/4 D-0046 phases done, its phase-3 prerequisite now also closed. E4 not closed (FR-403 open).

## Next action

**6 requirements remain across 2 items**: FR-403's step 3 continuation (aliasing/interprocedural/cross-file/safe-transformation, or the recall-comparison harness — pick one bounded increment) and E2's FR-202 phase 3 (real per-file worker wiring, now fully scoped by D-0048 with its prerequisite bug fixed). Both remain large; neither should be rushed.

**Cycle 71:** Built FR-403 step 3's before/after recall-comparison harness at `bench/privacy-recall/` — the item D-0041/D-0047 both explicitly named as required "before this walker could ever be considered for anything beyond opt-in." Mirrors `bench/self-scan/check.mjs`'s exit-code contract (0 clean / 1 drift / 2 unmeasurable); `bench/layer-recall` was not reusable (always forces deep mode on with no on/off diff axis, and scores the CVE-replay corpus, which has no privacy-shaped fixtures).

One scan per fixture with both `AGENTIC_SECURITY_DEEP=1` and `AGENTIC_SECURITY_PRIVACY_DEEP=1` set measures both sides at once — the shallow `annotatePrivacyTaint` always runs whenever deep mode is on, and the new walker's findings are distinguishable by `parser:'IR-PRIVACY-TAINT'`. Three hand-written fixtures empirically confirmed a **real, concrete recall improvement**, not just parity: `same-name-direct` (declaration name and sink in the same call) scores shallow=1/deep=1 — a parity case, since the shallow engine's name-in-text matching already covers it; `renamed-before-sink` (the classified value is reassigned to a differently-named variable before reaching the sink) scores shallow=0/deep=1 — the shallow engine's pure text matching structurally cannot see this, while the new walker's real Set-based taint propagation does; `clean-negative` scores 0/0 — no false positive introduced. Proved the gate fails on deliberately injected drift (exit 1) and passes clean when restored (exit 0), both directions.

Found and fixed a real state-pollution issue while building this: `disableStateWrites()` does not cover every writer (threat-model/sbom-history annotators write regardless of the flag) — added explicit `purgeScanState()` calls before and after every run, both at the fixtures directory and at the `measure.mjs` subprocess's own working directory (where the same writers land when spawned via `execFileSync`). Confirmed zero stray `.agentic-security` dirs after repeated runs.

Wired as `bench:privacy-recall` / `:check` / `:update-baseline` npm scripts, deliberately left standalone — not wired into `pre-push-gate.mjs` or `release-check.mjs`, since it gates an opt-in, still-incomplete capability, matching the precedent of other non-hard-gated benches (`iac-coverage`, `secrets-precision`, `fix-correctness`) rather than the hard-gated ones (`layer-recall`, `self-scan`, `mutation`, `cve-replay`), which gate default-path behavior.

Full gate: `npm test` exit 0 (4665/4665 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python — unaffected by the new bench files, which live outside `scanner/test/`/`scanner/src/`), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3.

- **66 of 73 FRs verified** (unchanged — this closes item (e) of FR-403's step-3 remaining list, not the requirement itself). This is cycle 71 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 not closed (FR-403 open — steps 1-2 done, step 3 v1 built+wired+tested+gate-verified+recall-harnessed, items (a)-(d) remain). E2 not closed (FR-201 verified; FR-202 at 2/4 D-0046 phases with its prerequisite fixed).

## Next action

**6 requirements remain across 2 items**: FR-403's step 3 continuation — pick ONE of (a) aliasing, (b) interprocedural flow, (c) cross-file flow, (d) safe-transformation recognition, each extendable via the new `bench/privacy-recall` harness — and E2's FR-202 phase 3 (real per-file worker wiring, now fully unblocked). Both remain large; neither should be rushed.

**Cycle 72:** Implemented FR-403 step 3's item (d) — safe-transformation recognition. Added `PRIVACY_TRANSFORM_CALLEES` (a named list — hash/hmac/bcrypt/scrypt/pbkdf2/argon2/encrypt/mask/redact/anonymize/pseudonymize/tokenize — mirroring `engine.js`'s own `_UNSANITIZER_CALLEES` precedent of "a named list, not exhaustive") and `isPrivacyTransformCallee` to `privacy-catalog.js`. Wired a per-function side-map into the walker, mirroring `engine.js`'s own `_sanitizersByVar` shape exactly: recorded on assignment from a recognized transform call, cleared on any other reassignment. At the sink, a value that passed through a recorded transform demotes confidence (0.6→0.18, `confidenceTier` medium→low) and records `_privacyTransformsOnPath` — the finding is **never suppressed**, matching `dataflow/CLAUDE.md`'s own documented recall-preserving philosophy for the general engine's sanitizers exactly ("RECORDED, never trusted to kill taint... a blanket rule silently drops a real vulnerability").

Caught and fixed a real dead-modules violation during this cycle's own gate run: `PRIVACY_TRANSFORM_CALLEES` was initially exported but had no external call site — made it private, since only `isPrivacyTransformCallee` needed to be public. Also reviewed a genuine self-scan drift (+1, `scanner/src` 443→444): `OWNERSHIP-AUTHZ`'s `CWE-863` "lifecycle-gate-missing" heuristic false-fired on the new file, pattern-matching generic vocabulary (the file's own taint-tracking `state` variable) against its lifecycle-field heuristic — confirmed as a genuine false positive (this file has no notion of database records/resources/ownership at all) and accepted into the baseline after direct review, matching this session's repeated precedent for new files.

Full gate: `npm test` exit 0 (4668/4668 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 zero drift (confirmed twice — once before, once freshly after the baseline update), self-scan clean against the reviewed, updated baseline, build clean, smoke exit 3.

- **66 of 73 FRs verified** (unchanged — this closes item (d) of FR-403's step-3 remaining list, not the requirement itself). This is cycle 72 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 not closed (FR-403 open — steps 1-2 done, step 3 v1 now covers direct flow + declaration sources + transform recognition + a working recall harness; items (a)-(c) remain). E2 not closed (FR-201 verified; FR-202 at 2/4 D-0046 phases with its prerequisite fixed).

## Next action

**6 requirements remain across 2 items**: FR-403's step 3 continuation — (a) aliasing or (b) interprocedural flow or (c) cross-file flow, each a real, separate increment — and E2's FR-202 phase 3 (real per-file worker wiring, fully unblocked). Both remain large; neither should be rushed.

**Cycle 73:** Implemented FR-403 step 3's item (a) — simple same-function identifier aliasing. Added a per-function `ctx.aliasesByVar` side-map (symmetric `Map<varName, Set<varName>>`) to the walker: a bare `let a = obj;` records `a`/`obj` as mutual aliases. A later member write through either name applies to both; a later read through either name (via `exprPrivacyTaint`'s new optional `ctx` parameter) resolves through the alias. Reassigning the alias variable severs all its existing alias relationships first, so a stale alias can't keep propagating taint after the variable moved on. Deliberately not a general points-to analysis — the general engine's own `points-to.js` solves that far more generally for security taint but is deliberately not reused here, for the same D-0047 isolation reason `step`/`exprTaint` are not reused.

Two real bugs were found and fixed during test-writing, not after: (1) the first alias-recording implementation added a new alias relationship on reassignment without removing the stale one, so `a = obj; a = other;` left `a` aliasing both simultaneously — caught by a dedicated reassignment test, fixed by severing existing entries first; (2) all three alias tests initially used a PII-shaped container variable name, which meant the container's own declaration-based classification tainted the whole object independent of aliasing — every test passed for the wrong reason. Rewrote all three to use a neutral container name and a genuinely PII-shaped parameter as the real source, correctly isolating the mechanism.

Reviewed one more self-scan false positive of the same class already accepted in cycle 72 (`OWNERSHIP-AUTHZ`'s lifecycle-gate heuristic firing on two more generic `ctx`-checking `if()` statements) — confirmed as the identical pattern, not a new issue, and accepted.

Full gate: `npm test` exit 0 (4671/4671 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 zero drift confirmed twice (before and after the baseline update), self-scan clean against the reviewed baseline, build clean, smoke exit 3.

- **66 of 73 FRs verified** (unchanged — this closes item (a) of FR-403's step-3 remaining list, not the requirement itself). This is cycle 73 of up to 100.
- **8 of 10 epics fully closed: E1, E3, E5, E6, E7, E8, E9, E10.** E4 not closed (FR-403 open — steps 1-2 done, step 3 v1 now covers direct flow + declaration sources + transform recognition + same-function aliasing + a working recall harness; items (b)-(c) remain). E2 not closed (FR-201 verified; FR-202 at 2/4 D-0046 phases with its prerequisite fixed).

## Next action

**6 requirements remain across 2 items**: FR-403's step 3 continuation — (b) interprocedural flow or (c) cross-file flow, each a substantially larger increment than what's been done so far — and E2's FR-202 phase 3 (real per-file worker wiring, fully unblocked). Both remain large; neither should be rushed.

**Cycle 74 — MAJOR MILESTONE: FR-403 VERIFIED, CLOSING EPIC E4 (the 9th of 10 epics).** Implemented items (b) interprocedural flow and (c) cross-file flow, then closed a real acceptance-criterion gap and marked the requirement complete.

**Item (b), interprocedural flow**: `dataflow/privacy-deep-walker.js`'s `exprPrivacyTaint` 'call' case now also checks `_nestedCallReturnPrivacyTainted`, which resolves a bare-identifier callee via `callGraph.resolveKnownCallee()`/`functionRecord()` — both pure, read-only lookups reused for the same reason `_languageFamilyExtensions` is reused (shared, stateless infrastructure, not the general engine's mutable taint state — D-0047's isolation requirement is about `step`/`exprTaint`'s data shape and the SummaryCache, not call-graph name resolution). Deliberately as conservative as the general engine's own resolution: refuses every member-expression/dotted-string callee. Extracted the worklist core into a shared `_runWorklist` so a new `_computeReturnTaintUnderEntry` probe and the real per-function analysis cannot diverge. A per-scan shared `{callGraph, summaryCache, stack}` gives real cross-call-site caching and a genuine recursion guard, proven by dedicated self-recursion and mutual-recursion termination tests.

**Item (c), cross-file flow, came essentially free**: `callGraph.resolveKnownCallee()` is built once for the WHOLE SCAN across every file, not per-file, and nothing in the item (b) code restricted resolution to the caller's own file. Verified empirically with a real two-file fixture BEFORE writing any new code — it resolved correctly on the first attempt, zero additional implementation needed.

**Closed a real acceptance-criterion gap**: re-reading the PRD literally, FR-403's acceptance criterion requires "INTEGRATION TESTS" (not just unit tests) covering direct/aliased/interprocedural/cross-file flows plus safe transformations — aliasing and safe-transformation recognition had only unit-level proof until this cycle. Added 3 new real end-to-end fixtures and tests, bringing `test/privacy-deep-e2e.test.js` from 4 to 11 tests, each scanning real source through the real parser. The safe-transformation e2e test surfaced a real, expected fact: the shared downstream pipeline recalibrates confidence on every finding (all detector families alike), so the test asserts the relative demotion and the `_privacyTransformsOnPath` marker rather than raw unit-test values.

**Result**: every named flow shape in FR-403's acceptance criterion now has a real, gate-verified integration test. The broader requirement text is covered too: all 9 sink categories (step 1), sanitization/masking/hashing/encryption (item d), and serialization is deliberately NOT treated as a safe transform (since it doesn't remove sensitive content — taint correctly flows through it unaffected, the correct reading of "track flows through serialization"). The capability remains genuinely opt-in (`AGENTIC_SECURITY_PRIVACY_DEEP=1`, default off) — D-0041 step 4 (default-path cutover) remains explicitly deferred and out of scope, since the acceptance criterion is about test coverage, not default-on status, matching this session's established precedent (FR-705's conditional encryption, and the broader no-op-until-configured pattern used throughout this codebase).

Extended `bench/privacy-recall` with a 4th fixture (interprocedural), re-baselined: a second concrete, measured recall improvement (shallow=0/deep=1) beyond cycle 71's finding.

Full gate: `npm test` exit 0 (4682/4682 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3.

- **67 of 73 FRs verified.** This is cycle 74 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** Only **E2** remains — 2/8 FRs done (FR-201 verified; FR-202 at 2/4 D-0046 phases, prerequisite fixed). All 6 remaining unverified requirements in the ENTIRE PRD are in E2: FR-202, FR-203, FR-204, FR-205, FR-207, FR-208.

## Next action

**6 requirements remain, ALL in Epic E2.** FR-202 phase 3 (real per-file worker wiring into `engine.js`'s main loop, fully unblocked, D-0048's checklist ready) is the natural next step, since FR-203/204/205/207/208 are all downstream of it in some way. This is now the ENTIRE remaining scope of the PRD — a large, architecturally significant piece that should not be rushed.

**Cycle 75:** Grounded **FR-202 phase 3** (D-0049) by reading `engine.js`'s actual per-file loop directly before writing any code — the same discipline D-0046/D-0047 established for hot-path work. Confirmed the D-0048 broadcast-once assumption is correct (`_loadCustomRules` runs once, before the loop, and is the only writer to `SOURCE_PATTERNS`/`SINK_PATTERNS`/`SANITIZER_PATTERNS`), and found a real complication neither D-0046 nor D-0048 had surfaced: the loop is checkpoint/resume-coupled — `_ckptRecord` marks ~10 shared accumulator arrays' lengths before each file and slices the delta after, so a future worker cannot just "return some findings"; it must reproduce this exact delta shape. Recorded **D-0049**, splitting phase 3 into **3a** (pure data-flow change: cascade returns a delta instead of mutating shared arrays, zero worker involvement) and **3b** (real worker wiring using that delta shape). **Implemented 3a**: extracted the ~130-detector-call-site cascade into a new `_runFileCascade(p, c)` inner function that accumulates into fresh local arrays and returns one delta object, matching `_ckptRecord`'s own existing payload shape exactly. Deliberately left the two skip-checks (oversized/dense file) and the checkpoint-replay check in the OUTER loop rather than folding them into the cascade — moving them would have changed which iterations skip the trailing cooperative-yield check, a real behavioral subtlety caught by reading the code closely (not assumed away). `_suppressionLog` handling was deliberately left untouched (still mark-and-slice) since it is written to transitively inside 4 detectors' own internals, not at the cascade's top level — converting those 4 detectors' output to a return value remains phase 3b's job per D-0046's original checklist. Verified via a scripted, assertion-gated transform (exact boundary text asserted byte-for-byte before touching anything, all 9 array-rename counts checked before/after) rather than hand-editing ~140 dense minified-style lines by eye. Full gate: `npm test` exit 0 (4710 pass, 0 fail across all suites), corpus 215/215 zero drift, self-scan zero drift (`scanner/src` 445=445 — the exact file that changed), build clean (zero new untracked dist chunks), smoke exit 3.

- **67 of 73 FRs verified** (unchanged — FR-202 correctly remains `ready`; phase 3a is real, gate-verified progress but the acceptance criterion still needs phase 3b's actual worker dispatch). This is cycle 75 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** Only **E2** remains — FR-201 verified, FR-202 now at D-0046 phases 1-2 done + D-0049 phase 3a done. All 6 remaining unverified requirements in the ENTIRE PRD are in E2: FR-202, FR-203, FR-204, FR-205, FR-207, FR-208.

## Next action

**FR-202 phase 3b is the natural next step**: make `_runFileCascade` callable inside `analyzer-worker.js` via `runWithDeadline`, broadcasting `SOURCE_PATTERNS`/`SINK_PATTERNS`/`SANITIZER_PATTERNS`/`_projectIndex`/`_GLOBAL_JAVA_TAINTED_METHODS` once per worker at startup, converting the 4 `_suppressionLog`-writing detectors' output to an explicit return value, and adapting `_ckptRecord`/`_ckptReplay` to accept a worker-returned delta. This is where genuine regression risk begins (concurrency, serialization boundaries, bundling of a runtime-string-path worker module) and needs its own full accuracy/determinism/self-scan/corpus comparison before ever going beyond opt-in — it deserves its own careful, unhurried cycle, not a rushed continuation of this one.

**Cycle 76:** Continued **FR-202 phase 3b** (D-0049) — the first concrete sub-step from D-0046's own checklist: converting the 4 real `_suppressionLog`-writing detectors' output to an explicit return value. `scanStructuralVulns`/`scanLogicVulns`/`scanExtraStructural`/`scanEntropySecrets` each gained an optional third parameter (`_suppOut`) with a backward-compatible fallback (`const _supp = _suppOut || _suppressionLog;`) — every existing 2-arg call site (confirmed via grep: exactly one per function, all inside `_runFileCascade`) is unaffected, purely additive. `_runFileCascade` now passes a local `_aSupp` accumulator into all 4 calls instead of letting them close over the module-level `_suppressionLog` global, and returns it as `delta.suppressions`; the outer loop pushes it onto the real global immediately after applying the other 9 delta arrays and before `_ckptRecord`'s mark-and-slice call, preserving identical global content, order, and checkpoint-capture timing. `_runFileCascade` now has **zero direct writes to shared module-level state** — every one of its 10 outputs flows through its single return-value delta, closing the concrete blocker phase 3b needed before a worker boundary could carry this cascade's full output back. Full gate: `npm test` exit 0 (4710 pass, 0 fail), corpus 215/215 zero drift, self-scan zero drift (`scanner/src` 445=445), build clean (zero new untracked dist chunks), smoke exit 3.

- **67 of 73 FRs verified** (unchanged — FR-202 correctly remains `ready`; the cascade is now fully worker-ready in terms of its data contract, but no actual worker dispatch exists yet). This is cycle 76 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** Only **E2** remains, same 6 requirements as before: FR-202, FR-203, FR-204, FR-205, FR-207, FR-208.

## Next action

**FR-202 phase 3b's remaining piece is the actual worker-pool dispatch** — a genuinely new architectural design question not yet grounded: whether to spawn one worker per file or reuse a pool of N workers, the warm-start cost of re-importing `engine.js`'s ~10K-line module per worker, and how the broadcast-once globals (`SOURCE_PATTERNS`/`SINK_PATTERNS`/`SANITIZER_PATTERNS`/`_projectIndex`/`_GLOBAL_JAVA_TAINTED_METHODS`) get into a worker's `workerData` cleanly. This deserves its own grounding pass (mirroring D-0046/D-0047/D-0049's precedent) before any code is written, given it is genuinely the highest-regression-risk piece remaining in the entire PRD.

**Cycle 77:** Continued **FR-202 phase 3b** (D-0049) with its second sub-step: promoting `_runFileCascade` from an inner closure of `runFullScan` to a genuine top-level, exported module function — `export function _runFileCascade(p, c, scanRoot, _detectorErrors)`. This closed a real, previously-unaddressed blocker: a function nested inside another function cannot be reached by a worker thread's `import()` (a worker gets a fresh module instantiation with none of a specific invocation's closure state), so phase 3b's actual dispatch could not have proceeded without this. Direct code reading confirmed the cascade's only two closure dependencies were `scanRoot` (3 uses, the MCP rug-pull block) and `_detectorErrors` (119 uses, one per `runDetector(...)` call) — both promoted to explicit parameters with identical identifier names, so the ~135-line function body needed zero internal changes; only its signature and physical location moved. Verified live via `import('./src/engine.js')` showing `_runFileCascade` as a real, callable export — exactly the shape `runWithDeadline`'s `{modulePath, exportName, args}` contract needs. Full gate: `npm test` exit 0 (4710 pass, 0 fail), corpus 215/215 zero drift, self-scan zero drift (`scanner/src` 445=445), build clean (zero new untracked dist chunks), smoke exit 3.

- **67 of 73 FRs verified** (unchanged — FR-202 correctly remains `ready`; the cascade is now a real, standalone, worker-callable export, but no actual worker dispatch exists yet). This is cycle 77 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** Only **E2** remains, same 6 requirements: FR-202, FR-203, FR-204, FR-205, FR-207, FR-208.

## Next action

**FR-202 phase 3b's last piece is the actual worker-pool dispatch design and wiring.** Every prerequisite is now in place: `_runFileCascade` is a pure, top-level, exported, zero-closure-dependency function returning a self-contained delta with no direct writes to any shared state. What remains is genuinely new architecture, not yet grounded: one worker per file vs. a reused pool of N workers (accounting for the warm-start cost of a worker re-importing `engine.js`'s ~10K-line module), getting the broadcast-once globals into `workerData`, wiring `runWithDeadline` calls into the main loop behind a new opt-in flag, adapting the checkpoint mark/slice mechanism to accept a worker-returned delta, and verifying `analyzer-worker.js`'s bundling once it imports the real `engine.js` instead of the phase-1 synthetic fixtures. This is the single highest-regression-risk piece left in the entire 73-requirement PRD and deserves its own dedicated grounding pass before any dispatch code is written.

**Cycle 78:** Completed **FR-202 phase 3b's remaining piece** — the actual worker-pool dispatch primitive (**D-0050**). Built `src/pipeline/cascade-worker-pool.js`/`cascade-worker.js`: a pool of long-lived workers, each performing whole-project setup ONCE at creation (a new exported `_initCascadeWorkerState(fileContents, scanRoot)` in `engine.js`, mirroring `runFullScan`'s own pre-loop sequence), then servicing many per-file `_runFileCascade` tasks over its lifetime — chosen over one-shot-per-file (the existing `runWithDeadline` model) because redoing whole-project setup per file would be O(n²) across a scan, a real scaling defect grounding surfaced, not a style call. A task exceeding deadline+grace gets its own worker terminated (mirroring `analyzer-supervisor.js`'s preemptive `worker.terminate()`) with a background replacement keeping the pool healthy. Proven standalone via 8 tests including a REAL hung-task termination-and-recovery proof (~537ms) and a dedicated "setup runs once per worker, not once per task" test. Found and fixed two real, unrelated bugs while grounding this: (1) `test/sca-route-reachable.test.js`'s fake, never-created scanRoot caused `state-dir.js` to silently fall through and leak a fake dependency into the REAL repo's `sbom-history`, producing a spurious self-scan finding on the new pool file — fixed via the established `AGENTIC_SECURITY_NO_STATE=1` per-test isolation idiom already used in 4 other test files, plus deleting the one corrupted snapshot; (2) once cleared, a genuine "fire-and-forget" self-scan finding on the pool's background worker-replacement call (a bare `.catch(() => {})` that would silently swallow a real failure mode) was fixed by tracking the failure into `spawnFailureCount`/`lastSpawnError`, without changing the correct fire-and-forget design itself. The 2 remaining self-scan findings (a return-propagated promise chain the heuristic can't trace, and the now-error-tracked fire-and-forget call) were reviewed and confirmed as the same pattern-matcher false-positive class already accepted elsewhere this session — baseline updated (`scanner/src` 445→447). Full gate: `npm test` exit 0 (4718 pass, 0 fail), corpus 215/215 zero drift, self-scan clean against the reviewed baseline, build clean (zero new dist chunks), smoke exit 3.

- **67 of 73 FRs verified** (unchanged — FR-202 correctly remains `ready`; every phase 3b primitive now exists, tested and gate-verified in isolation, but nothing is wired into `runFullScan`'s real loop yet). This is cycle 78 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** Only **E2** remains, same 6 requirements: FR-202, FR-203, FR-204, FR-205, FR-207, FR-208.

## Next action

**FR-202's last remaining step is wiring `createCascadePool` into `runFullScan`'s real per-file loop behind a new opt-in flag** — replacing the direct synchronous `_runFileCascade` call with `pool.runFile(...)` when set, adapting `_ckptRecord`/`_ckptReplay` to accept a worker-returned delta the same way `_ckptReplay` already does for a resumed file, deciding pool lifecycle (create once per `runFullScan` call, shut down at scan end), and running a full accuracy/determinism/self-scan/corpus comparison WITH the flag on (not just proving off is unchanged) before FR-202 can be marked `verified`. This is the final piece of the highest-priority remaining item in the entire PRD.

**Cycle 79: MAJOR MILESTONE — FR-202 IS NOW `verified`.** Completed the final wiring: `createCascadePool` is now dispatched from `runFullScan`'s real per-file loop behind `AGENTIC_SECURITY_WORKER_CASCADE=1` (default off) — each file's cascade call becomes `await pool.runFile(p,c,scanRoot,_detectorErrors,{timeoutMs:_perFileTimeoutMs})` instead of the direct synchronous call; a pool timeout produces a new `_emptyCascadeDelta(c)` (correctly distinct from the existing cooperative post-hoc check — a genuine preemptive kill loses all in-flight analysis, unlike a merely-slow-but-completed file); a real cascade error re-throws into the existing per-file `try/catch` unchanged. **No changes were needed to `_ckptRecord`/`_ckptReplay`** — phase 3a's unified delta-application design (apply the delta to the shared arrays, then checkpoint) already works identically regardless of which code path produced the delta.

**A real scoping bug was caught immediately by a purpose-built end-to-end test, before it ever reached the full suite**: wrapping the loop in `try{}` accidentally shrank `let i`'s scope, breaking post-loop cross-file code that reads the final counter (`ReferenceError: i is not defined`) — this would have broken EVERY scan, flag or no flag. Fixed by hoisting `let i=0;` above the `try`. Added `test/cascade-pool-wiring.test.js` (3 tests through the REAL `runFullScan()`): flag-off unchanged; flag-on with a generous timeout produces IDENTICAL real findings to the sync path; flag-on with an impossibly low timeout still completes without crashing and marks the file with a real `file-timeout` finding — the literal FR-202 acceptance criterion, now proven through the actual production engine.

**Then, manually testing the REAL SHIPPED BUNDLE (`dist/agentic-security.mjs`, not `src/`) with the flag on found it produced ZERO findings** — a serious, genuine bug the `src/`-only test suite structurally could not have caught. Root cause: `fileURLToPath(import.meta.url)` computed inside `engine.js` resolves to `dist/agentic-security.mjs` once bundled — the CLI ENTRYPOINT itself (`bin/agentic-security.js`'s code), which has no `_runFileCascade`/`_initCascadeWorkerState` exports and runs CLI-usage dispatch as a side effect of merely being `import()`ed (confirmed directly: importing the bundle prints the CLI help text). Fixed with `_resolveEngineModulePathForWorkers()` (detects unbundled-from-`src/` vs bundled-from-`dist/`, and in the latter case walks to the real sibling `src/engine.js` — stable in both a dev checkout and the published npm package, since `package.json`'s `"files"` ships `src/` and `dist/` as siblings) and an equivalent `_resolveDefaultWorkerScript()` fix in `cascade-worker-pool.js` for the same problem affecting the worker script path. Re-verified against the real bundle after the fix: identical 6 real findings with the flag on vs off, and a genuine `file-timeout` marker under an impossibly low timeout.

**Final verification, with the flag genuinely enabled, not just proving default-off is unchanged**: `AGENTIC_SECURITY_WORKER_CASCADE=1 npm run bench:cve-replay:check` → 215/215 pass, zero drift, across the full multi-language, multi-CWE, deep-mode-inclusive corpus. Full gate (flag off, the default path): `npm test` exit 0 (4721 pass, 0 fail), corpus 215/215 zero drift, self-scan zero drift (`scanner/src` 447=447), build clean (`dist/265.index.js` — the new `cascade-worker-pool` chunk, real and expected since `engine.js` now dynamically imports it, staged per D-0027, now 7 chunks total), smoke exit 3.

**No known bypass defeats the deadline enforcement**: `worker.terminate()` is OS-level preemptive termination of the V8 isolate — not a cooperative check a hung synchronous detector could ever observe, skip, or race around.

- **68 of 73 FRs verified.** This is cycle 79 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** **E2 is now 2/8** (FR-201, FR-202 both verified). **5 requirements remain in the ENTIRE PRD: FR-203, FR-204, FR-205, FR-207, FR-208 — all in Epic E2, all downstream of FR-202 to varying degrees.**

## Next action

**5 requirements remain, all in Epic E2, all now genuinely unblocked** since FR-202's real deadline-enforcement mechanism exists and is proven. FR-203 (per-analyzer coverage ledgers) and FR-204 (assurance modes) both need a real terminal-status signal per analyzer — FR-202's `_detectorErrors`/timeout-marking machinery is the natural foundation for this. A fresh grounding pass against each of the 5 remaining FRs' literal PRD text (mirroring D-0046/D-0049/D-0050's own discipline) is the right next step before implementing any of them — do not assume their scope from the FR titles alone.

**Cycle 80:** Grounded **FR-203** (D-0051) by auditing every one of `_runFileCascade`'s ~130 detector call sites for their real applicability rules. Found: ~119 always-applicable (no gate), 2 file-extension-gated (`scanRegexReDoS`, `scanGraphQL`), 9 operator-policy-gated via individual `AGENTIC_SECURITY_NO_*` env vars — mapping directly onto PRD §10.2's named `skipped_by_policy` status. **Decided**: FR-203 does not require refactoring every detector into a full `AnalyzerResult`-returning function (§10.2's canonical shape, which FR-201 itself never implemented either, and which the literal FR-203 acceptance criterion doesn't demand) — a lightweight `computeCoverageLedger(...)` pure function over data already collected by FR-201/FR-202 (`_detectorErrors`, file-timeout markers, the env-var gate table) is the right, smallest-correct-primitive scope, mirroring D-0041/D-0046/D-0050's precedent. Not yet implemented — this cycle was grounding only.

**While grounding, found and fixed a real, unrelated FR-201 completeness gap**: 2 of the original 128 detector call sites — `scanWeb3Advanced` and `scanK8sAdmission` — were never wrapped in `runDetector` during cycle 66's migration (they were called directly, `_aF.push(...scanK8sAdmission(p,cc))`), meaning an exception from either would propagate out of the cascade entirely and silently discard every OTHER detector's results for that file — a real violation of FR-201's own already-verified acceptance criterion. The original migration's grep-based verification (128 sites found = 128 expected) couldn't catch this because a count match doesn't distinguish a wrapped call from an unwrapped one containing the same callee-name substring. Fixed by wrapping both identically to the other 126 sites; a corrected sweep (grep for the pattern specifically NOT preceded by `runDetector`) now confirms zero remaining gaps. Proven via a new `test/detector-fault-injection-k8s-admission.test.js` (mirroring the existing fault-injection test's exact `mock.module()` technique, in its own file per that file's own documented binding constraint). FR-201's `verified` status is unchanged — the isolation mechanism was always correct, only its coverage was incomplete, mirroring the FR-307/FR-1002 precedent (D-0024) of correcting a verified requirement's evidence without re-litigating its status. Full gate: `npm test` exit 0 (4722 pass, 0 fail), corpus 215/215 zero drift, self-scan zero drift (`scanner/src` 447=447), build clean, smoke exit 3.

- **68 of 73 FRs verified** (unchanged — FR-203 correctly remains `ready`, not `verified`; this cycle was grounding plus a completeness fix, not FR-203's actual implementation). This is cycle 80 of up to 100.
- **9 of 10 epics fully closed: E1, E3, E4, E5, E6, E7, E8, E9, E10.** **E2 remains at 2/8** (FR-201, FR-202 verified). 5 requirements remain in the ENTIRE PRD: FR-203, FR-204, FR-205, FR-207, FR-208.

## Next action

**Implement FR-203's `computeCoverageLedger(...)` as an isolated, pure, tested module** — mirroring D-0028/D-0046/D-0050's "build and test the primitive before wiring it into engine.js" precedent. Taking the applicability table grounded this cycle (the 2 extension gates, the 9 env-var gates) plus `_detectorErrors`/file-timeout data, produce a per-file × per-analyzer status map. Wire into `scan-health.js`'s `analyzers` field as a separate, later step, with full accuracy comparison before considering FR-203 verified — a wrong coverage claim is a false-assurance regression, not a neutral one.

**Cycle 81 (INDEPENDENT AUDIT, at the user's explicit request to verify the PRD is truly complete):** Dispatched 16 parallel agents to independently re-verify a risk-weighted sample of the 68 requirements marked `verified`, each working from the PRD's literal acceptance criterion and the code only — not this session's own tracking files. **12 of 16 held up genuinely. 4 of 16 (25%) had real gaps** that violated their literal PRD acceptance criteria, all found, fixed, and re-verified in this cycle (see **D-0053** for full detail):

- **FR-206**: `scanHealth.status` was computed correctly but never surfaced in the actual human-facing verdict (`toShipVerdict`) — a broken scan with zero findings still said "Safe to deploy." Fixed.
- **FR-401**: only 2-3 of the 8 named IR elements were genuinely, distinctly supplied (`types` a hardcoded stub, `assignments`/`parameters` merged, `returns`/`storage` absent). Fixed the 2 cheap, real, zero-risk gaps (parameter/assignment tagging, real `returns` extraction); honestly **downgraded from `verified` to `ready`** since real type inference and storage detection remain genuinely unimplemented, not force-closed.
- **FR-806**: the "privacy-preserving" promise was broken — a caller-supplied `findingId` was persisted verbatim, and a real finding's displayed `.id` embeds its exact file path. Fixed by substituting the finding's `stableId` or a genuine hash.
- **FR-1002**: only the "approvals" third of "approvals, exceptions, and suppressions" was wired to identity verification. `posture/suppressions.js` never checked `justification_signed_by` against the approver registry at all. Fixed by threading the same registry FR-307 already built into the suppression path.

All 4 fixes came with dedicated regression tests proving the specific failure mode the audit demonstrated, and a full gate re-run: `npm test` exit 0 (4738 pass, 0 fail), corpus 215/215 zero drift, self-scan zero drift (447=447), build clean, smoke exit 3.

**Net tally: 67 verified + 3 ready (FR-203, FR-205, FR-401) + 3 pending (FR-204, FR-207, FR-208) = 73.** Same 6 remaining un-verified requirements as before this audit, minus FR-401 now correctly counted among them instead of falsely verified.

**The honest answer to "is the PRD truly completed": NO — not yet.** 67/73 genuinely verified (now with an independent audit behind that number, not just this session's own self-assessment), 6 requirements remain real, unimplemented-or-incomplete work: FR-203 (grounded, not built), FR-204, FR-205, FR-207, FR-208 (not started), and FR-401 (partially implemented, real gaps remain).

## Next action

Continue FR-203's implementation (grounded in cycle 80), then FR-204/205/207/208 (each needs its own fresh grounding pass per D-0014's method — do not assume scope from the title). FR-401's remaining gap (real type inference, storage-write detection) is a separate, larger undertaking that should be explicitly scoped before attempting, not bundled into a quick pass.

**Cycle 82: FR-203 IS NOW `verified`.** Built `src/pipeline/coverage-ledger.js`: a static, programmatically-verified-complete registry of all 121 `runDetector`-wrapped analyzer names (extracted directly from `engine.js`'s real cascade, zero missing/extra), tagged by applicability (110 always, 2 extension-gated, 9 policy-gated). `computeCoverageLedger(...)` computes exactly one terminal status per (file, applicable-analyzer) pair from data FR-201/FR-202 already produce; a file timeout correctly marks EVERY applicable analyzer on that file as `timed_out` (a preemptive kill destroys the whole cascade's state as one unit — no partial credit). Wired into `engine.js`'s real loop and into `scan-health.js`'s `computeScanHealth()`, replacing the `analyzers: null` placeholder whose own comment named this exact prerequisite chain. Also added a new scanHealth condition: a failed detector (not just a failed annotator) now demotes status to `partial` — a real gap that had zero signal before. Guarded against drift by a completeness test mirroring `artifact-registry-completeness.test.js`'s precedent — a future detector added without updating the registry breaks the build. 23 new tests, including a real end-to-end proof through `runScan()` and manual verification against the real shipped bundle. Full gate: `npm test` exit 0 (4761 pass, 0 fail), corpus 215/215, self-scan 447/447, build clean, smoke exit 3.

- **68 of 73 FRs verified** (67 + FR-203). Epic E2 now 3/8 (FR-201, FR-202, FR-203). 5 remain: FR-204, FR-205, FR-207, FR-208 (E2), and FR-401 (E4, real type inference/storage detection genuinely missing).

## Next action

Ground and implement FR-204 (assurance modes advisory/standard/strict) next — it's the natural foundation for FR-205 (CI deep-analysis policy) and can now use FR-203's coverage ledger as its real per-analyzer terminal-status signal.

**Cycle 83: FR-204 IS NOW `verified`.** Built `src/pipeline/assurance-mode.js`: `evaluateAssuranceMode(mode, scanHealth)` over the three PRD-named modes, built directly on FR-203's coverage ledger and FR-206's scanHealth. Advisory/standard are mechanically identical (never gate) since the PRD's literal criterion only specifies strict's failure behavior. Strict fails on any `scanHealth.status !== 'complete'` — one check covering every named case (failed/timed-out/silently-skipped analyzer via the coverage ledger, plus annotator/deep-mode failures via existing scanHealth conditions). Wired into `agentic-security ci` as `--assurance advisory|standard|strict` (default standard), running ALONGSIDE `--fail-on`/`--policy` per the existing FR-SDLC-9 precedent. 15 tests total, including 4 REAL end-to-end tests through the actual bundled CLI using a genuinely unmocked incomplete scan (FR-202's real worker-timeout mechanism, since mock.module() can't reach inside a bundled artifact). Full gate: `npm test` exit 0 (4776 pass, 0 fail), corpus 215/215, self-scan 447/447, build clean (new lazy chunk staged, 8 total), smoke exit 3.

- **69 of 73 FRs verified** (68 + FR-204). Epic E2 now 4/8 (FR-201/202/203/204). 4 remain: FR-205, FR-207, FR-208 (E2), FR-401 (E4, real gap).

## Next action

Ground and implement FR-205 (CI deep-analysis behavior explicit and policy-driven — "CI cannot silently downgrade deep analysis; any downgrade appears in the headline and machine output"). The `_deepStatus` signal already feeding `scanHealth.deepAnalysis` is the likely foundation; check whether it's already fully surfaced or has its own real gap before assuming scope.

**Cycle 84: FR-205 IS NOW `verified`.** Grounded first (per D-0014): `_deepStatus` (engine.js, comment literally cites "FR-205/FR-206") already computed the CI-downgrade signal into `scanHealth.conditions`; the only real gaps were on the surfacing side, and both were already closed by this window's own earlier work — FR-206's `toShipVerdict` fix (never prints "Safe to deploy" when `scanHealth.status != complete`) and FR-204's `--assurance strict` (fails the build outright on the same condition). Verified the full chain empirically against the freshly rebuilt bundle, not just unit tests: `AGENTIC_SECURITY_NO_STATE=1 AGENTIC_SECURITY_DEEP=1 CI=true node dist/agentic-security.mjs ci <fixture> --fail-on none` printed `[ci] scan-health=partial — deep analysis was requested but did not run: requested, but running in CI without AGENTIC_SECURITY_DEEP_IN_CI=1`, and the same run with `--assurance strict` exits 1. Added 3 dedicated regression tests (1 in report-render.test.js, 2 real end-to-end CI-subprocess tests in ci.test.js) rather than reimplementing already-working machinery — a "verify before assuming unbuilt" case matching D-0003's precedent. Full gate: `npm test` exit 0 (4779 pass, 0 fail), corpus 215/215 zero drift, self-scan zero drift, build clean (18 dist files staged, no new chunk — no new dynamic-import boundary this cycle), smoke exit 3.

- **70 of 73 FRs verified** (69 + FR-205). Epic E2 now 5/8 (FR-201/202/203/204/205). 3 remain: FR-207, FR-208 (E2), FR-401 (E4, real type-inference/storage-detection gap, already downgraded verified→ready rather than force-closed).

## Next action

Ground and implement FR-207 (freshness checks for vulnerability feeds, calibration data, rulesets, and policies — acceptance: "stale dependencies are visible and can fail strict policy"). Then FR-208 (resumable execution invalidated by version/hash/policy change) — note `scan-checkpoint.js` may already substantially satisfy this per its own documented invalidation behavior; ground before assuming scope, per D-0014.

**Cycle 85: FR-207 IS NOW `verified`.** Grounded via an independent research agent first: all 4 named freshness targets already had a per-source staleness signal SOMEWHERE (KEV's `kevCatalogMeta()`, compliance-policy.js's FR-506 `_staleness()`, policy-bundle.js's `expiresAt`), but none fed `scanHealth`, so strict mode had nothing to gate on — the literal unmet half of the acceptance criterion. Also found EPSS's dedicated freshness module (`posture/epss.js`) was dead code in practice (the LIVE enrichment path is a separate, duplicate implementation inside `engine.js` with no staleness tracking at all), and custom rule packs had no freshness signal whatsoever. Built the minimal remaining wiring: engine.js's own EPSS cache path now tracks `{score,percentile,ts}` and exposes `epssLiveMeta()` mirroring `kevCatalogMeta()`'s shape (both honor `AGENTIC_SECURITY_OFFLINE` — offline means "cannot vouch for freshness," never a fabricated true/false); `calibration.js` gained `calibrationFreshness()` against a new real, git-log-derived `_generatedAt` in `calibration-seed.json` (not fabricated); `custom-rules.js` gained `customRulesFreshness()` reusing compliance-policy.js's exact opt-in idiom (review-interval-days + reviewed-at) rather than mtime, since a CI checkout resets mtimes and would make an mtime check silently blind in CI — the one place strict mode matters most; `scan-health.js` gained `applyFreshness()`, a small merge/condition/status function called twice (engine.js for kev/epss/calibration/compliance, bin/agentic-security.js for custom-rules, since that DSL runs after scanHealth already exists). **A real regression was caught, not shipped**: the first wiring broke two PRE-EXISTING "clean scan" tests, because this dev machine's real, accumulated EPSS disk cache genuinely contains a pre-this-cycle entry with no timestamp, which correctly computed as maximally stale — real, previously-invisible staleness the new feature correctly surfaced. Fixed by isolating both tests with `AGENTIC_SECURITY_OFFLINE=1` and making the EPSS path honor offline mode like KEV already does. Live-verified against the real rebuilt bundle in both directions (offline: honest `stale:null`; live: this machine's real stale EPSS cache genuinely fails `--assurance strict`, printing the exact PRD acceptance criterion in action). 16 new tests across 5 files. Full gate (run 3 times as real issues surfaced and were fixed): npm test 4768 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (19 dist files staged, same 8 chunks, no new boundary), smoke exit 3. Deliberately out of scope: wiring policy-bundle.js's org/repo/env signed-policy layer into scanHealth, since grounding found it has zero callers in `scan`/`ci` at all today (a standalone CLI-only inspection tool) — wiring that in is "make policy bundles apply to scans for the first time," a materially larger undertaking than a freshness check; compliance-policy.js's existing, already-wired `_staleness()` is what actually satisfies the "policies" leg of this requirement today.

- **71 of 73 FRs verified** (70 + FR-207). Epic E2 now 6/8 (FR-201/202/203/204/205/207). 2 remain: FR-208 (E2), FR-401 (E4, real type-inference/storage-detection gap, already downgraded verified→ready rather than force-closed).

## Next action

Ground FR-208 (resumable execution invalidated by version/hash/policy change) before writing any code — `scan-checkpoint.js` (documented in `scanner/src/posture/CLAUDE.md`) already states "the run key covers engine version, ruleset version, bundle SHA, a content hash of every scanned and dependency file... and every AGENTIC_SECURITY_* env switch — if any of it moved, the checkpoint is discarded." This may mean FR-208 is already substantially or fully satisfied, the same "verify before assuming unbuilt" pattern that closed FR-205. Read the actual code and its test (`test/scan-checkpoint.test.js`) before scoping any new work, per D-0014.

**Cycle 86: FR-208 IS NOW `verified` — but this one was the OPPOSITE of FR-205's "verify before assuming unbuilt" outcome.** Grounding found the existing mechanism does the opposite of the acceptance criterion: it discards the ENTIRE checkpoint on ANY change (one test's own comment: "this invalidates the whole checkpoint, not just that one entry") — a real, substantial gap, not a rename/wiring job. Implemented genuine per-file granular invalidation: split run identity into a GLOBAL key (engine/ruleset/bundle/deps/env — still discards everything, since those affect every file) and a PER-FILE content hash on each checkpoint record (only that file's own analysis depends on its own content, since cross-file passes always re-run from scratch on resume — the module's own pre-existing decision #2). Added a plaintext meta summary so a global discard now names exactly what changed (`"engine version changed (1.0.0 -> 2.0.0)"`), and a new `invalidatedFiles()` accessor names per-file reasons (`"content changed since it was checkpointed"` / `"no longer part of this scan"`) — both satisfying "record the invalidation reason" literally. Checkpoint format bumped `/1`→`/2` so no old on-disk checkpoint is ever half-interpreted under new semantics. Backward-compatible by construction: a handle opened without file content tracking (every pre-existing low-level protocol test) is trusted exactly as before — 9 of those tests needed only a mechanical `runKey`→`globalKey` rename. 2 existing end-to-end tests DID need their asserted behavior changed (they tested the OLD blunt behavior directly) — rewritten to prove the new correct behavior via a real interrupted-then-resumed scan, still asserting byte-identical findings vs. an uninterrupted baseline. 8 new/extended tests total (14→19 in the file). Full gate (run twice — first hit the same pre-existing, load-dependent "no prior scan" S7 flake class already documented at D-0030, confirmed transient by a clean isolated re-run and a clean second full run): npm test 4773 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (20 dist files staged, same 8 chunks, no new boundary), smoke exit 3.

- **72 of 73 FRs verified** (71 + FR-208). Epic E2 now COMPLETE (8/8: FR-201/202/203/204/205/206/207/208). Only 1 remains in the entire PRD: FR-401 (E4), already honestly downgraded from `verified` to `ready` earlier this session — real type inference and storage-write detection are genuinely unimplemented, not force-closed.

## Next action

FR-401 is the last open item in the PRD. Its remaining gap (real type inference across languages; storage-write detection integration with the privacy IR adapter) is a separate, larger undertaking flagged in prior evidence as deserving its own explicit scoping pass, not a quick follow-on — ground it fresh (existing `STORED_TAINT_FIELD_PATTERNS`/`buildStoredTaintRegistry` may cover the storage-write half already; the IR itself tracks zero type data for any language today, per the earlier FR-401 evidence) before committing to an implementation plan.

**Cycle 87: FR-401's "storage" element is now genuinely supplied — "types" deliberately deferred, correctly still `ready`, not force-closed to `verified`.** Grounded via an independent research pass first: storage was confirmed bounded/mechanical (reindex engine.js's already-computed `buildStoredTaintRegistry` output per-file, no new detection logic); types was confirmed to have a real, non-fabricated narrow sliver theoretically available (TypeScript's type-annotation AST nodes survive into `parser-js.js`'s own custom Babel plugin visitor, since plugins run before presets in Babel's merged traversal, before `preset-typescript` strips them) but implementing it means modifying `parser-js.js` — a foundational, extremely heavily-audited hot path whose own module docs record dozens of hard-won fix-rounds and several genuine ReDoS bugs found while making past "small" changes to sibling parser files. Weighed the risk (real regression risk to correctness-critical shared infrastructure) against the reward (one narrow, TS-only PRD element) and made the deliberate call not to attempt it this cycle — exactly the documented, reasoned scope decision the PRD's own alternative path calls for, not silent scope-narrowing. Implemented storage: `ir-adapter.js` gained `_storageForFile` + an optional third `storedRegistry` param on `adaptIRForPrivacyTaint` (additive, backward-compatible); `engine.js`'s real call site passes the registry it already computes for its own `crossStoredTaint` correlation straight through — no new computation. 9 new tests including 2 real end-to-end ones (the genuine `buildStoredTaintRegistry`, not a hand-built stand-in, over a fixture that actually trips its admission rule; a real deep-mode scan proving the wiring doesn't throw). Live-verified against the freshly rebuilt bundle. Full gate (run twice — same pre-existing D-0030-class "no prior scan" flake on the first run, confirmed transient, clean second run): npm test 4779 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (20 dist files staged, same 8 chunks), smoke exit 3.

**FINAL STATE: 72 of 73 FRs `verified`; FR-401 `ready` (honest, deliberate partial — 7 of 8 named elements now genuinely supplied: declarations, assignments, parameters, calls, returns, sinks (via privacy-taint.js's existing classifySink, a reasonable and pre-existing layering), and now storage; only "types" remains unimplemented, by a documented, reasoned decision, not an oversight). This concludes the full "6 remaining items" the user asked to be implemented autonomously (FR-203, FR-204, FR-205, FR-207, FR-208, and FR-401's remaining gap) — 5 fully verified, the 6th honestly and deliberately left partial with its exact remaining scope and the reasoning behind not attempting it fully documented here, in state.json, and in the handoff file.

## Next action

None outstanding from the "6 remaining items" directive. If further work on FR-401's "types" element is ever wanted, it needs its own dedicated, carefully-scoped pass (full regression coverage across every `parser-js.js` consumer — SAST, IR-taint, privacy) explicitly authorized as worth the risk to that foundational file, not a quick follow-on.

**Cycle 88 (user-directed): FR-401 IS NOW FULLY `verified`. THE PRD IS 73/73 COMPLETE.** The user explicitly asked to find a way to verify the one remaining gap. Re-examined the risk that blocked cycle 87: the danger was specifically modifying `ir/parser-js.js`'s existing transform pipeline. Found a way to get real TypeScript type data WITHOUT that risk — a throwaway `node -e` test (run before writing any production code) confirmed `@babel/core`'s `parseSync` never runs `@babel/preset-typescript`'s stripping transform (parse-only, no transform stage at all), so a completely SEPARATE, read-only parse of `.ts`/`.tsx` source returns an AST with type annotations fully intact — zero changes to `parser-js.js`, zero risk to the shared pipeline every other consumer depends on. Implemented `_extractTsTypes`/`_tsTypeToString`/`_qualifiedTypeName` in `ir-adapter.js`: a real, honest, TypeScript-only type-string renderer (primitives, arrays, unions, literal types, generic references) that returns `null` — never a guess — for anything it doesn't recognize (mapped/conditional types, unresolvable union members, past a depth guard). `_adaptFile` gained an optional `tsTypes` param, matching a type to a decl by the same `(line, name)` pair both parses agree on. Confirmed this is a REAL, already-wired improvement, not a newly-populated inert field: `dataflow/privacy-taint.js:127` already reads `d.type` as `declaredType` and already renders it in the human-facing inventory narrative — a typed TS PII field now genuinely reports its real type instead of always saying "unknown," with zero changes to privacy-taint.js itself. 16 new tests including 2 real end-to-end ones through the actual production functions (a positive control proving a real `.ts` file's typed field reaches `declaredType`, and a negative control proving the identical `.js` shape honestly stays `null`) — caught and fixed one real bug along the way (Babel's generic-argument property is `typeArguments`, not `typeParameters`; the first draft silently dropped every generic argument, caught by a dedicated test before being trusted). Full gate, clean on the FIRST run (no flakes this cycle): npm test 4793 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (20 dist files staged, same 8 chunks, no new boundary), smoke exit 3.

**FINAL STATE: 73 of 73 FRs `verified`. The assurance-hardening PRD is complete.** No open items remain in `docs/implementation/assurance-hardening-state.json`.
