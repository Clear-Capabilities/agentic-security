# Assurance Hardening — Handoff

**If you are resuming this work:** read this file first, then `assurance-hardening-status.md`, then `assurance-hardening-state.json` for the per-FR table. Do not restart from the PRD as if nothing has happened — check `git log` and the state files for what is actually true in the repository.

## Current state (as of cycle 88, 2026-08-26) — READ THIS FIRST

**THE ASSURANCE-HARDENING PRD IS NOW 73/73 `verified`. NOTHING REMAINS OPEN.** User-directed: after cycle 87 left FR-401's "types" element deliberately deferred (citing regression risk to `ir/parser-js.js`), the user explicitly asked to find a way to verify it. Re-examined the SPECIFIC risk rather than the general one: the danger was modifying `parser-js.js`'s existing transform pipeline, not extracting type data as such. A throwaway `node -e` test (before any production code) confirmed `@babel/core`'s `parseSync` never runs `preset-typescript`'s stripping transform — it only parses — so a fully SEPARATE, read-only parse of `.ts`/`.tsx` source returns an AST with type annotations intact, with zero changes to `parser-js.js` and zero risk to its shared pipeline. Implemented `_extractTsTypes`/`_tsTypeToString`/`_qualifiedTypeName` in `ir-adapter.js` (a real, TypeScript-only type-string renderer that returns `null` — never a guess — for anything unrecognized), wired via a new optional `tsTypes` param on `_adaptFile` matching by `(line, name)`. Confirmed (by grep, before writing code) that this is a REAL, already-wired improvement: `privacy-taint.js:127` already reads `d.type` as `declaredType` and already renders it in the human-facing inventory — a typed TS field now genuinely reports its real type instead of always "unknown," no changes needed to that module. 16 new tests, including 2 real end-to-end ones (positive: a real `.ts` file's typed field reaches `declaredType`; negative: the identical `.js` shape stays honestly `null`) — caught and fixed one real bug along the way (Babel's generic-argument property is `typeArguments`, not `typeParameters`; the first draft silently dropped every generic arg, caught by a dedicated test). Full gate clean on the FIRST run this cycle (no flakes): npm test 4793 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (20 dist files staged, same 8 chunks, no new boundary), smoke exit 3. **`docs/implementation/assurance-hardening-state.json` now has 73/73 `verified` and zero non-`verified` entries — there is no more forward PRD work to hand off.**

**CYCLE 87 CLOSES OUT THE USER'S "implement the 6 remaining items" DIRECTIVE (FR-203/204/205/207/208 + FR-401's remaining gap). 72/73 PRD requirements now `verified`; FR-401 is the sole exception, correctly left `ready` (a deliberate, documented partial, not an oversight).** FR-401's "storage" element is now genuinely supplied: `ir-adapter.js` gained `_storageForFile` + an optional `storedRegistry` param on `adaptIRForPrivacyTaint`, reindexing engine.js's ALREADY-COMPUTED `buildStoredTaintRegistry` output (built once per scan for `crossStoredTaint`'s own use) down to a per-file `storage: [{field,line,snippet,named}]` array — no new detection logic, purely a reshape, same category as the prior cycle's `kind`/`returns` additions. "types" was DELIBERATELY NOT attempted after a second independent research pass confirmed a narrow, real, TypeScript-only sliver IS architecturally possible (type-annotation AST nodes survive into `parser-js.js`'s own custom Babel plugin visitor before `preset-typescript` strips them — plugins run before presets in Babel's merged traversal) — but implementing it means modifying `parser-js.js` itself, a foundational, extremely heavily-audited hot path whose own module docs record dozens of hard-won fix-rounds and multiple genuine ReDoS bugs found during past "small" changes to sibling parser files. The risk (real regression risk to correctness-critical shared infrastructure used by every JS/TS SAST/taint/privacy consumer) was judged disproportionate to the reward (one narrow, TS-only PRD element out of ~10 supported languages) — a reasoned, documented scope decision, not silent narrowing. 9 new tests including 2 real end-to-end ones (the genuine `buildStoredTaintRegistry`, not a stand-in; a real deep-mode scan proving no throw). Live-verified against the freshly rebuilt bundle: a real ORM-write-of-request-data fixture scans cleanly with `privacyIrBacked:true`. Full gate (run twice — same pre-existing D-0030-class "no prior scan" S7 flake on the first run across every cycle this session that ran the full suite twice, confirmed transient both times, clean second run): npm test 4779 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (20 dist files staged, same 8 chunks, no new boundary), smoke exit 3. **If resuming work beyond the directive: FR-401's "types" element is the only thing left in the entire 73-requirement PRD, and needs its own dedicated, carefully-scoped pass (full regression coverage across every `parser-js.js` consumer) explicitly authorized as worth the risk — not a quick follow-on.**

**FR-208 IS NOW `verified` (cycle 86) — E2 IS NOW FULLY CLOSED (8/8). ONLY FR-401 (E4) REMAINS OPEN IN THE ENTIRE 73-REQUIREMENT PRD.** Grounding found the OPPOSITE of FR-205's "already substantially built" outcome: `scan-checkpoint.js`'s existing mechanism discarded the ENTIRE checkpoint on ANY input change (a test's own comment literally said "this invalidates the whole checkpoint, not just that one entry") — precisely contradicting FR-208's acceptance criterion ("changed inputs invalidate only affected checkpoints"). Implemented genuine per-file granular invalidation: split run identity into a GLOBAL key (engine/ruleset/bundle/dependency-manifests/env — still discards everything, since those affect every file's analysis) and a PER-FILE content hash on each checkpoint record (safe because cross-file passes always re-run from scratch on resume — the module's own pre-existing decision #2, so one file's own result never depends on another file's content). A global discard now names exactly what changed via a plaintext `globalKeyMeta()` comparison (`"engine version changed (1.0.0 -> 2.0.0)"`); a new `invalidatedFiles()` accessor names per-file reasons (`"content changed since it was checkpointed"` / `"no longer part of this scan"`) — both surfaced on `scan._scanMeta.checkpoint`, satisfying "record the invalidation reason" literally. Checkpoint format bumped `/1`→`/2` so no pre-existing on-disk checkpoint is ever half-interpreted under the new semantics. Backward-compatible by construction — a handle opened without file-content tracking is trusted exactly as before, so 9 pre-existing low-level protocol tests needed only a mechanical `runKey`→`globalKey` rename; 2 tests that asserted the OLD blunt behavior directly were deliberately rewritten to prove the NEW correct behavior (still via a real interrupted-then-resumed scan, still asserting byte-identical findings vs. an uninterrupted baseline). 8 new/extended tests (scan-checkpoint.test.js: 14→19). Full gate (run twice — first hit the same pre-existing, load-dependent "no prior scan" S7 flake class already documented at D-0030, confirmed transient by both an isolated re-run and a clean second full run): npm test 4773 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (20 dist files staged, same 8 chunks, no new boundary), smoke exit 3.

**FR-207 IS NOW `verified` (cycle 85).** Grounded first via an independent research agent: all 4 named freshness targets (feeds, calibration, rulesets, policies) already had a per-source staleness signal somewhere, but none fed `scanHealth`, so strict mode had nothing to gate on. Built the minimal remaining wiring: engine.js's own EPSS cache path now tracks real timestamps (`epssLiveMeta()`, mirroring the existing `kevCatalogMeta()`); `calibration.js` gained `calibrationFreshness()` against a new real, git-log-derived `_generatedAt` in `calibration-seed.json`; `custom-rules.js` gained `customRulesFreshness()` reusing compliance-policy.js's exact opt-in idiom (review-interval-days + reviewed-at, not mtime — mtime resets on every CI checkout); `scan-health.js` gained `applyFreshness()`, called once in engine.js (kev/epss/calibration/compliance) and once in bin/agentic-security.js (custom-rules, since that DSL runs after scanHealth already exists). **A real regression was caught before shipping**: this dev machine's actual accumulated EPSS disk cache has a genuine pre-existing entry with no timestamp, which correctly computed as maximally stale and broke two pre-existing "clean scan" tests — fixed by isolating those tests with `AGENTIC_SECURITY_OFFLINE=1` and making the EPSS path honor offline mode symmetrically with KEV. Live-verified against the real rebuilt bundle both ways: offline gives honest `stale:null`; live, this machine's real stale EPSS cache genuinely fails `--assurance strict` end to end (`[ci] scan-health=partial — EPSS exploit-probability data is stale (20691 day(s) old)` → `assurance gate FAILED`), the literal PRD acceptance criterion caught happening for real, not staged. 16 new tests across 5 files. Full gate (run 3 times as real issues surfaced): npm test 4768 pass/0 fail, corpus 215/215, self-scan zero drift, build clean (19 dist files staged, still the same 8 chunks, no new boundary), smoke exit 3. Deliberately out of scope: wiring policy-bundle.js's signed org/repo/env layer into scanHealth — it has zero callers in `scan`/`ci` today (standalone CLI-only), so wiring it in is a materially larger undertaking than a freshness check; compliance-policy.js's already-wired `_staleness()` satisfies the "policies" leg today. **E2 is now 6/8. 2 remain: FR-208 (E2) + FR-401 (E4, real gap).**

**FR-205 IS NOW `verified` (cycle 84).** Grounded first: `_deepStatus` (engine.js) already computed the CI-downgrade signal into `scanHealth.conditions`; the real gaps were only on the surfacing side, and both were already closed by this window's own earlier work — FR-206's `toShipVerdict` fix and FR-204's `--assurance strict`. Verified empirically against the freshly rebuilt bundle: a CI run requesting deep analysis without the override prints `[ci] scan-health=partial — deep analysis was requested but did not run: ...` and `--assurance strict` exits 1 on it. Added 3 dedicated regression tests (1 report-render, 2 real end-to-end CI-subprocess through the bundled CLI) rather than reimplementing already-working machinery. Full gate clean (4779 pass/0 fail, corpus 215/215, self-scan zero drift, build clean — no new chunk, 18 files staged, smoke exit 3). **E2 is now 5/8. 2 remain: FR-207, FR-208 (E2) + FR-401 (E4, real gap).**

**FR-204 IS NOW `verified` (cycle 83).** Built `src/pipeline/assurance-mode.js` (advisory/standard/strict, built on FR-203's ledger + FR-206's scanHealth). Wired into `agentic-security ci --assurance <mode>` (default standard), alongside `--fail-on`/`--policy`. 15 tests including 4 REAL end-to-end tests through the actual bundled CLI using a genuinely unmocked incomplete scan (reused FR-202's real worker-timeout mechanism). Full gate clean (4776 pass/0 fail, corpus 215/215, self-scan 447/447, build clean, smoke exit 3). **E2 is now 4/8. 4 remain: FR-205, FR-207, FR-208 (E2) + FR-401 (E4, real gap).**

**FR-203 IS NOW `verified` (cycle 82).** Built `src/pipeline/coverage-ledger.js` (a programmatically-verified-complete registry of all 121 real analyzer names + their applicability), wired into `engine.js`'s real loop and `scan-health.js`'s `analyzers` field (previously hardcoded `null`). 23 new tests including a real end-to-end proof and manual bundle verification. Full gate clean (4761 pass/0 fail, corpus 215/215, self-scan 447/447, build clean, smoke exit 3). **E2 is now 3/8 (FR-201, FR-202, FR-203 verified). 5 remain: FR-204, FR-205, FR-207, FR-208 (E2) + FR-401 (E4, real gap).**

**INDEPENDENT AUDIT (cycle 81, D-0053) — the user explicitly asked to verify the PRD is truly completed.** Dispatched 16 parallel agents to independently re-check a risk-weighted sample of the 68 `verified` requirements, working from the PRD text and code only, NOT this session's own tracking files. **12/16 held up genuinely. 4/16 (25%) had real gaps** — all fixed and re-verified this cycle: FR-206 (scanHealth never surfaced in the actual human-facing verdict — a broken scan with 0 findings said 'Safe to deploy'), FR-401 (only 2-3 of 8 named IR elements genuinely supplied — fixed the 2 cheap real ones, **honestly DOWNGRADED from `verified` to `ready`** since real type inference/storage detection remain genuinely unimplemented), FR-806 (a caller-supplied findingId embedding real file/line was persisted verbatim, contradicting the module's own privacy promise — fixed via stableId/hash substitution), FR-1002 (only 'approvals' was wired to identity verification; 'exceptions and suppressions' — the other two named nouns — were not, at all — fixed by wiring the same registry into `posture/suppressions.js`). Full gate after all 4 fixes: 4738 pass/0 fail, corpus 215/215, self-scan 447/447, build clean, smoke exit 3. **Net tally: 67 verified + 3 ready (FR-203, FR-205, FR-401) + 3 pending (FR-204, FR-207, FR-208) = 73.** See D-0053 for the full audit findings and the process lesson: three of the four gaps were the SAME failure class D-0024 already named once this session — proven at the unit level, unproven at the real consuming entry point — recurring despite the lesson being written down. A standing decision does not substitute for a real, independent adversarial check.

**FR-203 grounded (cycle 80, D-0051) — not yet implemented.** Audited every one of `_runFileCascade`'s ~130 detector call sites' applicability: ~119 always-applicable, 2 file-extension-gated (`scanRegexReDoS`, `scanGraphQL`), 9 operator-policy-gated via individual `AGENTIC_SECURITY_NO_*` env vars (mapping directly onto PRD §10.2's `skipped_by_policy` status). **Decided: FR-203 does NOT require refactoring every detector into a full `AnalyzerResult`-returning object** (§10.2's canonical shape, which FR-201 itself never implemented either) — a lightweight `computeCoverageLedger(...)` pure function over data ALREADY collected (`_detectorErrors`, file-timeout markers, the env-var gate table) is the correct, smallest scope. **While grounding, found and fixed a real FR-201 completeness gap**: `scanWeb3Advanced`/`scanK8sAdmission` were never wrapped in `runDetector` during the original 128-site migration (called directly, so an exception from either would silently discard every OTHER detector's results for that file) — the original grep-based verification's COUNT match couldn't distinguish a wrapped call from an unwrapped one sharing the same callee-name substring. Fixed identically to the other 126 sites, proven via a new dedicated fault-injection test. **FR-201's `verified` status is unchanged** (isolation mechanism always correct, only coverage was incomplete — same correction-without-re-litigation pattern as FR-307/FR-1002, D-0024). Full gate: 4722 pass/0 fail, corpus 215/215, self-scan 447=447, build clean, smoke exit 3.

**MAJOR MILESTONE: FR-202 IS NOW `verified`.** The final wiring is done: `createCascadePool` is dispatched from `runFullScan`'s REAL per-file loop behind `AGENTIC_SECURITY_WORKER_CASCADE=1` (default off, zero behavior change when unset). **Two real, serious bugs were caught and fixed in the process, both BEFORE being trusted**: (1) wrapping the loop in `try{}` shrank `let i`'s scope, breaking post-loop code that reads the final counter — caught immediately by a purpose-built end-to-end test (`test/cascade-pool-wiring.test.js`, 3 tests through the REAL `runFullScan()`), before it ever reached the full suite; (2) far more seriously, manually testing the REAL SHIPPED BUNDLE (`dist/agentic-security.mjs`, not `src/`) with the flag on produced ZERO findings — because `fileURLToPath(import.meta.url)` inside bundled `engine.js` code resolves to the CLI ENTRYPOINT bundle itself (no `_runFileCascade` export, runs CLI-usage dispatch as an import side effect), not to a re-importable engine module. **This is exactly the class of bug the src/-only test suite structurally cannot catch — always verify a new worker/dynamic-import capability against the REAL BUNDLE, not just src/, before calling it done.** Fixed with `_resolveEngineModulePathForWorkers()`/`_resolveDefaultWorkerScript()` (detect bundled-vs-unbundled, walk to the real sibling `src/` when bundled — stable since `package.json`'s `"files"` ships `src/` and `dist/` as siblings in both a dev checkout and the published npm package). Re-verified against the real bundle after the fix: identical findings with the flag on vs off, and a genuine `file-timeout` marker under an impossibly low timeout. Corpus re-run WITH the flag genuinely enabled (not just proving default-off unchanged): 215/215 pass, zero drift. Full gate (default path): 4721 pass/0 fail, corpus 215/215, self-scan 447=447, build clean (`dist/265.index.js` — the new real `cascade-worker-pool` chunk, staged per D-0027, now 7 chunks total), smoke exit 3. **No known bypass**: `worker.terminate()` is OS-level preemptive V8-isolate termination, not a cooperative check a hung detector could ever skip. **E2 is now 2/8 (FR-201, FR-202 both verified). Only 5 requirements remain in the ENTIRE 73-requirement PRD: FR-203, FR-204, FR-205, FR-207, FR-208 — all in Epic E2.**

**FR-202 phase 3b's worker-pool dispatch primitive is now BUILT, TESTED, and GATE-VERIFIED (cycle 78, D-0050)** — `src/pipeline/cascade-worker-pool.js`/`cascade-worker.js`: a pool of long-lived workers, each doing whole-project setup ONCE at creation (new export `_initCascadeWorkerState(fileContents, scanRoot)`), then servicing many per-file `_runFileCascade` tasks — NOT one-shot-per-file like `runWithDeadline`, because redoing whole-project setup per file would be O(n²) across a scan. A hung task's worker is terminated and replaced in the background. 8 tests prove it, including a real hung-task termination+recovery (~537ms) and a dedicated setup-runs-once-per-worker proof. **Two real, unrelated bugs were found and fixed while grounding this**: a test (`sca-route-reachable.test.js`) using a fake, never-created scanRoot was silently leaking state into the REAL repo's `sbom-history` (fixed via the established `AGENTIC_SECURITY_NO_STATE=1` isolation idiom), and a genuine fire-and-forget error-swallowing bug in the new pool code itself (fixed by tracking `spawnFailureCount`/`lastSpawnError` instead of a bare `.catch(()=>{})`). Full gate: 4718 pass/0 fail, corpus 215/215, self-scan clean against a reviewed, updated baseline, build clean, smoke exit 3. **Every phase 3b primitive now exists** — what remains is wiring `createCascadePool` into `runFullScan`'s ACTUAL per-file loop behind a new opt-in flag, adapting the checkpoint mark/slice mechanism to accept a worker-returned delta, and a full accuracy/determinism/self-scan/corpus comparison WITH the flag on before FR-202 can be marked `verified`.

**FR-202 phase 3b's second sub-step is DONE (cycle 77): `_runFileCascade` is now a real, top-level, exported module function with ZERO closure dependencies** — `export function _runFileCascade(p, c, scanRoot, _detectorErrors)`, promoted out of `runFullScan`'s body. This closed a real blocker: a worker's `import()` cannot reach a function nested inside another function's closure. The only two closure dependencies (`scanRoot`, `_detectorErrors`) became explicit parameters with identical names, so the ~135-line body needed no internal changes. Verified live via `import('./src/engine.js')` showing it as a real callable export, plus the full gate (4710 pass/0 fail, corpus 215/215, self-scan 445=445, build clean, smoke exit 3). **Every data-shape prerequisite for phase 3b is now in place** — what remains is the actual worker-pool dispatch design and wiring itself (one-worker-per-file vs. a reused pool, warm-start cost, getting broadcast-once globals into `workerData`, adapting the checkpoint mark/slice mechanism), which has NOT been designed yet and remains the highest-regression-risk piece left in the PRD.

**FR-202 phase 3b's first sub-step is DONE (cycle 76): the per-file cascade now has ZERO direct writes to shared module-level state.** The 4 real `_suppressionLog`-writing detectors (`scanStructuralVulns`/`scanLogicVulns`/`scanExtraStructural`/`scanEntropySecrets`) each gained an optional third parameter with a backward-compatible fallback to the module global, so `_runFileCascade` can now pass a local accumulator instead and return it as `delta.suppressions` — the outer loop applies it to the real global immediately before `_ckptRecord`, preserving identical content/order/timing. Verified via the full gate (4710 pass/0 fail, corpus 215/215, self-scan 445=445, build clean, smoke exit 3). **Still remaining for phase 3b: the actual worker-pool dispatch itself** — a genuinely new architectural design question (one worker per file vs. a reused pool, warm-start cost of re-importing `engine.js`'s ~10K-line module per worker, getting the broadcast-once globals into `workerData` cleanly) that has not yet had its own grounding pass and is the highest-regression-risk piece left in the entire PRD.

**FR-202 phase 3 has been split into 3a/3b (D-0049) and phase 3a is now DONE and gate-verified.** Before writing any worker-wiring code, `engine.js`'s actual per-file loop was read directly and found to be checkpoint/resume-coupled (a complication D-0046/D-0048 had not surfaced) — `_ckptRecord` marks and slices ~10 shared accumulator arrays per file, so a worker cannot simply "return some findings"; it must reproduce that exact delta shape. Phase 3a implements the non-worker half of that: the ~130-detector-call-site per-file cascade was extracted into a new `_runFileCascade(p, c)` inner function that accumulates into fresh local arrays and returns one self-contained delta object (matching `_ckptRecord`'s own existing payload shape), with the outer loop now applying that delta to the shared arrays itself. Verified byte-for-byte behavior-identical via a scripted, assertion-gated transform (not hand-edited) and the full gate: `npm test` 4710 pass/0 fail, corpus 215/215 zero drift, self-scan zero drift (`scanner/src` 445=445 unchanged), build clean (zero new untracked chunks), smoke exit 3. **FR-202 remains correctly `ready`** — phase 3b (the actual worker dispatch, using this now-return-value-shaped cascade) is the next concrete step; see D-0049 for the full phase 3a/3b split and why `_suppressionLog` handling was deliberately deferred to 3b.

**MAJOR MILESTONE (cycle 74): FR-403 IS NOW `verified`, CLOSING EPIC E4 — the 9th of 10 epics.** Items (b) interprocedural flow and (c) cross-file flow (the latter came essentially free once (b) was built correctly on the shared, whole-scan `callGraph`) are both built, tested, and gate-verified. A real gap against the literal acceptance criterion — "INTEGRATION TESTS cover direct, aliased, interprocedural, and cross-file flows plus safe transformations" — was found and closed: aliasing and safe-transformation recognition had only unit-level proof before this cycle; `test/privacy-deep-e2e.test.js` now has 11 real end-to-end tests (through the real parser via `runScan()`) covering every named flow shape. **ONLY EPIC E2 REMAINS OPEN IN THE ENTIRE PRD** — 6 requirements (FR-202, FR-203, FR-204, FR-205, FR-207, FR-208), all downstream of FR-202's real worker-wiring phase 3.

**FR-403 step 3 now also has simple same-function aliasing (item a)** — `let a = obj; a.x = tainted;` now correctly reaches a later `sink(obj.x)`, and vice versa, via a symmetric per-function `ctx.aliasesByVar` map. Two real bugs were caught and fixed while building this (a stale-alias-not-severed bug in the implementation, and a test-design confound where the container variable's own PII-shaped name was masking whether aliasing worked at all) — both found before being accepted, not after a false claim of correctness. Remaining for step 3: (b) interprocedural flow, (c) cross-file flow — both substantially larger than what's been done.

**FR-403 step 3 now also has safe-transformation recognition (item d)** — a value hashed/masked/encrypted via a named transform callee before reaching a sink still fires (recall-preserving, per this codebase's own established philosophy) but at demoted confidence with `_privacyTransformsOnPath` recorded. A real self-scan false positive was found and reviewed during this work (an `OWNERSHIP-AUTHZ` heuristic misfiring on the walker's own generic "state" vocabulary) and accepted into the baseline. Remaining for step 3: (a) aliasing, (b) interprocedural flow, (c) cross-file flow.

**FR-403 step 3 now has a working before/after recall-comparison harness** (`bench/privacy-recall/`, item (e) of the remaining list). It empirically proved a real, concrete recall improvement over the shallow engine — not just parity — on one genuine flow shape (variable renamed before reaching a sink). Bidirectionally proven (fails on injected drift, passes clean when restored). Standalone (not wired into pre-push/release gates), since the capability it measures is still opt-in and incomplete. Items (a)-(d) — aliasing, interprocedural, cross-file, safe-transformation — remain, each extendable via this same harness.

**FR-202 phase 3's prerequisite bug is now FIXED** — `src/sast/cpp.js`'s `_entropyMod` async-singleton race (D-0048's finding) is closed via a plain static import, with new test coverage (`test/cpp-hardcoded-secret.test.js`, 3 tests — this rule had zero direct tests before). Phase 3 itself (real per-file worker wiring into `engine.js`) has not started.

**FR-403's step 3 v1 is now BUILT, WIRED, TESTED, and GATE-VERIFIED** — `dataflow/privacy-deep-walker.js` (a genuinely separate, intra-procedural CFG walker per D-0047) is wired into `dataflow/index.js`'s `runDeepAnalysis` behind `AGENTIC_SECURITY_PRIVACY_DEEP=1` (default off), and proven both at the unit level (9 tests) and end-to-end through the real parser (4 tests, including two off-by-default safety proofs). FR-403 correctly remains `pending` — v1 only covers direct-flow + declaration-based sources within one function; aliasing, interprocedural flow, cross-file flow, and safe-transformation recognition are all still unmodeled, and the before/after recall-comparison harness against `annotatePrivacyTaint` does not exist yet.

**E2's FR-202 phase 2 is now COMPLETE** — a full audit of all 128 detectors for side-channel state (D-0048) corrected phase 1's overstated `_suppressionLog` estimate, confirmed 2 more broadcast-once candidates, confirmed no hidden non-per-file detector or order dependency, and found one genuine new bug (`src/sast/cpp.js`'s `_entropyMod` async-singleton race) that phase 3's real worker wiring must fix first. Phase 3 itself (the actual wiring) has not started.

**FR-403's step 3 is now fully GROUNDED and DESIGNED (D-0047)** — a genuinely separate, standalone walker module was chosen over threading a second Set through `engine.js`'s `step`/`exprTaint` (the general taint engine's hottest, most heavily-relied-upon function), to avoid risking the already-proven general-security taint engine's correctness for an orthogonal, still-opt-in capability. `matchPrivacySink` (the sink-matching primitive the future walker will call) is built, tested, gate-verified. **The actual walker itself has NOT been built yet** — that is the concrete next step, deliberately not rushed per D-0041's own warning that step 3 "is where real regression risk begins."

**E2's FR-202 now has D-0046 phase 1 of 4 done** — `src/pipeline/analyzer-supervisor.js`'s `runWithDeadline` is a real, tested, gate-verified `worker_threads`-based primitive that preemptively terminates a genuinely hung synthetic analyzer within its configured deadline+grace period. FR-202 remains correctly `ready` (not `verified`) — phases 2 (data-contract design) and 3 (real wiring into `engine.js`, opt-in, full accuracy/determinism/self-scan/corpus comparison) are not done. See D-0046 for the full 4-phase plan and why a real worker/subprocess boundary is structurally required (both existing timeout mechanisms are non-preemptive).

**E2's FR-201 is now FULLY VERIFIED** — D-0028's step (b) is complete: all 128 of `engine.js`'s real detector call sites (confirmed via grep, before: 128 bare calls / after: 0 bare calls, 128 `runDetector(...)` occurrences) are migrated to the isolation wrapper, `scan.detectorErrors`/`toJSON().detectorErrors` surface the captured errors (mirroring `annotatorErrors`), and a real end-to-end fault-injection test (`test/detector-fault-injection.test.js`, mirroring FR-906's `test/fault-injection.test.js`) proves the literal acceptance criterion against a REAL scan, not just the unit-level `runDetector` primitive. See D-0044/D-0045 for the two material decisions made while closing this out. **E2 itself is NOT yet closed** — FR-202/203/204/205/207/208 remain (6 of E2's 8 FRs). **FR-403 is still 2/4 done** — D-0041's steps 1-2 (privacy-leak sink catalog + declaration-based source matcher) are implemented, tested, and gate-verified; step 3 (the real CFG-walk wiring) has not started. The verified-FR list and epic-closure claims below are otherwise UNCHANGED from cycle 61 except for FR-201's addition.


**67 FRs VERIFIED (FR-401 downgraded to `ready` in cycle 81's independent audit): FR-101, FR-102, FR-103, FR-104, FR-105, FR-106, FR-107, FR-108, FR-201, FR-202, FR-206, FR-301, FR-302, FR-303, FR-304, FR-305, FR-306, FR-307, FR-308, FR-402, FR-403, FR-404, FR-405, FR-406, FR-407, FR-408, FR-501(scoped), FR-502, FR-503, FR-504, FR-505, FR-506, FR-507, FR-508, FR-601, FR-602, FR-603, FR-604, FR-605, FR-606, FR-607, FR-701, FR-702, FR-703, FR-704, FR-705, FR-706, FR-707, FR-801, FR-802, FR-803, FR-804, FR-805, FR-806, FR-901, FR-902, FR-903, FR-904, FR-905, FR-906, FR-907, FR-1001, FR-1002, FR-1003, FR-1004, FR-1005, FR-1006.** (73 total FR-* requirements in the PRD.) **6 REMAIN: FR-203, FR-204, FR-205, FR-207, FR-208 (all E2, not started/grounded-only) + FR-401 (E4, partially implemented — real type inference and storage detection genuinely missing).**

**Lesson from the cycle-60 correction, still in force: verify every "epic N is fully closed" claim by reading state.json's per-FR statuses directly before writing it down** — a cycle-57/58/59 narrative bug once claimed E7 closed when FR-705 was still pending. Cycle 61's own claims below were verified this way before being written.

**9 OF 10 EPICS ARE GENUINELY FULLY CLOSED (every FR's status verified directly against state.json): E1, E3, E4, E5, E6, E7, E8, E9, E10.** FR-403 (E4's last open item) is now `verified` (cycle 74) — see the cycle 74 milestone note above for full detail. **ONLY E2 REMAINS OPEN, NOW AT 2/8 (FR-201 verified cycle 66, FR-202 verified cycle 79) — ONLY 5 REQUIREMENTS LEFT IN THE ENTIRE 73-REQUIREMENT PRD: FR-203, FR-204, FR-205, FR-207, FR-208.** All five are downstream of FR-202 to varying degrees and have not been started. Everything else in the PRD has a justified terminal (verified) status.

**IMPORTANT — standing instruction change:** the user has explicitly said **"keep going until the PRD is fully completed."** This supersedes the earlier "ask before continuing" checkpoint from mid-session — there is no further approval checkpoint expected; continue the bounded autonomous loop (max 100 cycles per the original driving prompt) through all remaining FRs without pausing between cycles, subject to the same safety boundaries that have applied the whole session (no commits/pushes, no destructive git ops, no weakening tests/gates to force a pass, never claim a blocked requirement complete).

**CRITICAL — read D-0024 before touching apply_fix, apply-fix-service.js, or fix-verify.js again.** Cycle 44 found, while surveying FR-1003, that FR-307's/FR-1002's high-impact-approval gate was UNREACHABLE from both of its real production callers: the MCP `apply_fix` tool's `fixMeta` schema (`additionalProperties:false`) never declared `approval` as a property (a real caller's approval evidence was rejected at schema validation, before the handler ran — confirmed by direct execution against the validator); that same tool's caller-supplied-`patch` branch (the MORE commonly used path — "~100% of findings that ship only a template") never called `applyVerifiedFix()` at all and so had NO gate whatsoever; and the CLI's `fix --apply` had no flag to ever supply approval evidence. All three fixed, plus a coupled honesty-gate false-trigger bug (an approval-only `fixMeta` was silently gated on an unrelated FR-308 completeness check). FR-307's and FR-1002's `state.json` evidence records were extended with this correction — their `verified` status is UNCHANGED, since the underlying gate logic was always correct; only its real-world reachability was broken. **The general lesson: a test that proves a service function end-to-end (`applyVerifiedFix()` called directly) is NOT the same as proving the real caller (MCP tool / CLI) can reach it — an `additionalProperties:false` schema makes a downstream function's new required field invisible to real callers until the schema itself is updated.** Before trusting any future "proven end-to-end" claim in this codebase's own history, check whether it was proven through the REAL entry point or just the service layer underneath it.

Since the last handoff snapshot: cycles 43-47 closed E10 (FR-1002/1003/1004/1001), cycle 48 closed FR-1006 (E10 fully closed, D-0027's gate-ordering lesson). Cycle 49 completed **FR-905** (publish false-positive adjudication and missed-findings methodology) — the last D-0020-deferred item, closed via the exact 3-step path already recorded: persisted `why-missed.mjs`'s summary to a committed file, ran it for real across the full 920-entry false-negative population (a real ~9-minute background job, not the ~34-minute worst case originally estimated), and wired the committed summary into `accuracy-scorecard.js`. Real result: 80% no-finding-at-all, 18% suppressed-downstream, 2% wrong-file/CWE — published in a real, regenerated `docs/SCORECARD.md`. **E9 is now 6/7.** A first full-gate attempt during this cycle hung for over 2 hours on `test/verifier.test.js` (unrelated file); confirmed as a transient environmental stall (not a regression) by reproducing a clean, fast standalone pass and finding zero stray processes/ports — see D-0030 for the diagnostic protocol if this recurs. Cycle 50 completed **FR-504** (bind compliance evidence to repository/commit/scope/engine/ruleset/analyzer-health/mapping-version via a new `computeEvidenceDigest`, mirroring `attestation.js`'s canonicalisation shape) and **FR-506** (evidence freshness/owner/reviewer/exception/expiry — a `stale` status for evidence exceeding an opt-in review interval, a `gap` status for an expired structured `not-applicable` exception), both in `posture/compliance-policy.js` — confirmed via a header-comment comparison (D-0029) as the real target for E5's remaining evidence-manifest items, not `auditor-walkthrough.js` (which FR-501/502 already closed). Cycle 51 completed **FR-505** (sign evidence manifests when signing is configured) via a new `posture/compliance-evidence-signing.js` — reusing `evidence-bundle.js`'s OWN Ed25519 key (same self-attestation trust domain, deliberately NOT `policy-bundle.js`'s org-distributed one — D-0026's distinction applied a second time), applying EA-03's allowlist-plus-reject-unknown-keys discipline from the first write, and signing the FULL document rather than just FR-504's narrower `evidenceDigest` (D-0031 — proven by a test that catches a `narrative_evidence` edit the digest alone would miss). "When configured" means read-only key detection (never auto-generate, unlike `attest`'s own default) — a routine scan's behavior is unchanged unless an operator already set up a key. Extended `verify-attestation` to auto-detect this third artifact shape. Cycle 52 completed **FR-503** (require positive evidence — mere existence insufficient unless the mapping explicitly defines it) via a new, additive `file-contains: {path, pattern}` primitive (existing `file-exists` left unweakened for controls that only need existence). Deliberately did NOT audit the bundled compliance framework JSON files to decide which existing mappings should upgrade — separate, fuzzy-scoped curation, left for future work. In passing, fixed a real, previously-untested TOCTOU in `sca-policy-has-entry` (self-scan count for the file genuinely went DOWN as a verified result, not just accepted). Cycle 53 completed **FR-508** (export evidence in a stable API — exported records retain source references, status semantics, and schema version), **closing Epic E5 entirely**. Added `schemaVersion`, `statusSemantics` (documents what each of the 5 real status values means, so a GRC consumer never needs this codebase's own source), and `policySource` to the exported artifact, keeping FR-505's signing allowlist in sync (proven by 2 tamper tests). Along the way, a full `npm test` run correctly caught a real state-path-seam violation (a hardcoded `.agentic-security/` literal in the new display field) — fixed properly by exporting `state-dir.js`'s previously-private `STATE_DIR_NAME` constant rather than adding a forbidden allowlist exemption (D-0032). **6 of 10 epics are now fully closed: E1, E3, E4 (except FR-403), E5, E6, E10.** Cycle 54 completed **FR-702** (enforce default and maximum TTL by artifact class — expired caches, scans, evidence, tickets, and backups are purged or archived according to policy), advancing **E7 to 4/7**. Extended `artifact-registry.js` with a `retentionClass` field on ~30 of its 47 generated entries, deliberately conservative — only unambiguous fits into 5 named categories (cache/scan/evidence/ticket/backup); ongoing self-managing state was left classless on purpose. Built `posture/retention-policy.js`: a `defaultDays`/`maxDays`-per-class model where an operator's `.agentic-security/retention-policy.yml` override is clamped to the ceiling (`Math.min(override, maxDays)`) — satisfying the literal "default AND maximum" wording; PURGE only, with "archive" explicitly deferred to FR-706's own future export-manifest scope (D-0033). Wired a new `--expired` flag into `cmdReset()`, reusing its existing preserve/keep machinery; operator-config is never touched regardless of age. Proven via 17 new tests including a real-CLI case where an operator-configured shorter TTL correctly expires an artifact the built-in default would not. Cycle 55 completed **FR-802**, **FR-803**, and **FR-805** together in `posture/risk-dollars.js`, advancing **E8 to 5/6**. A survey found FR-802 was only half-built — the "likely organizational loss" wording was already permanently refused, but 4 of its 5 named inputs had no config-collection path at all, so the label could never even theoretically unlock. Extended `risk-config.yml`'s parser to collect all 5 (organizationScale/industry/recordCount/controlStrength as flat scalars, alongside the existing impactUSD), added a `REQUIRED_FOR_ORGANIZATION_SPECIFIC_LOSS` gate tracked separately from the broader dimension list so the bonus `familyBaseProb` knob can't substitute, and added a third status (`scenario_organization_specific`) that alone may use the stronger wording — directly realizing the PRD §12.1 `--risk-config` option. For FR-803/FR-805, built ONE mechanism rather than two (D-0034): `SCENARIO_MULTIPLIERS` (conservative/base/severe) perturb only the two generic assumption inputs (probability, impact), leaving reachability discount and confidence weight — both measured per-finding facts — fixed; `range` is literally the same three numbers as the named `scenarios`. Every `riskDollars` also gained `assumptions[]` (traceable per-factor sourcing), `modelVersion`, and a `confidence` tier. `ev` stays numerically identical to before (proven by test). E8's only remaining item, FR-806, was confirmed genuinely greenfield (zero existing precedent for realized-incident/accepted-risk calibration anywhere in the codebase). Cycle 56 completed **FR-706** (manifest-based export and deletion reports), advancing **E7 to 5/7**. A survey of E7's remainder (FR-705/706/707) found `cmdReset` already computed exactly the right target/preserved data but never wrote any of it to a durable file, and no `export` command existed at all — the most tractable of the three. Built `posture/state-lifecycle-report.js` (single-overwritten "last action" files, same precedent as `last-scan.json`) wired into every `reset` (now genuinely returns 1 on a real deletion failure — previously silent) and a brand-new `agentic-security export --out <dir>` command that copies every PRESENT registered artifact — generated AND operator-config, unlike reset — with a real sha256 per file into a manifest. Both write paths reuse the existing `safeWriteState` seam, so FR-704's `--no-state` guarantee applies for free. Hit the well-documented D-0004/D-0006/D-0009 "S7 no prior scan" combined-run flake twice across 3 full-gate attempts — confirmed via standalone reruns (32/32 clean, unrelated to this cycle's diff) and did not chase a 4th attempt (D-0035). Cycle 57 completed **FR-707** (legal hold — identity-bound, reasoned, time-bounded where applicable, auditable), **closing Epic E7 entirely**. Built `posture/legal-hold.js` as a third instance of the recurring `{owner, reason, expires_at}` exception shape (D-0025's precedent: a distinct subject from `suppressions.js`'s finding-scoped and `compliance-policy.js`'s control-scoped versions). The key design finding: a hold must be enforced at TWO points, not one (D-0036) — `retention-policy.js`'s existing `findExpiredArtifacts()` AND `cmdReset`'s own PLAIN (non-`--expired`) path, which deletes every registered generated artifact unconditionally and would otherwise ignore a hold entirely. `legal-holds.json` is classified `operator-config` despite being CLI-written, deliberately — a plain reset must never be able to release every hold by deleting the record itself. New CLI: `legal-hold add/remove/list`. Full gate: run 1 clean (4562/4562), run 2 hit the same documented "S7 no prior scan" flake again (standalone-confirmed unrelated, not chased per D-0035). Cycle 58 completed **FR-907** (longitudinal production feedback measurement), **closing Epic E9 entirely**. A survey confirmed the 5 named categories (user suppression, accepted risk, invalid finding, fixed finding, verification outcome) already existed separately across 5 unrelated mechanisms. Built `posture/production-feedback.js` — a pure read-only aggregator introducing no new storage — over `suppressions.js`/`triage-memory.js`/`sca-policy.js`/`fix-history.js`/`fix-metrics.js`. Handled the "longitudinal" requirement honestly (D-0037): 3 sources are genuinely dated (real day-bucketed trending, reusing `triage.js`'s own cutoff-window pattern); 2 schemas (pro suppressions, SCA accept-risk) have no per-entry creation timestamp by design, so those events land in a disclosed `undated` bucket rather than a fabricated timestamp, a silent drop, or a silent scope-widening. Wired into the existing `triage trend` CLI rather than a new command. The dead-module guard caught a real, useful side effect: `loadSoftAccepted`/`loadProSuppressions` had been sitting in the allowlist as previously-dead — removed now that they have a real caller. Full gate: run 1 clean (4578/4578), run 2 hit the same documented flake again (standalone-confirmed unrelated, not chased per D-0035). Cycle 59 completed **FR-806** (validate model calibration against accepted and realized incidents where customers opt in), **closing Epic E8 entirely — the 9th of 10 epics.** Confirmed genuinely greenfield. Scoped "aggregated" deliberately to WITHIN one installation (D-0038) — this codebase has no SaaS control plane and takes no runtime cloud calls, both existing, load-bearing constraints, not incidental gaps. Built `posture/calibration-feedback.js`: genuinely opt-in, privacy-preserving at the source (a record snapshots only prediction signals + outcome + a truncated note — never file/line/vuln text, proven by test), and a `possibleMiscalibration` signal that only fires with a reliable sample size in BOTH outcome buckets. New CLI: `calibration-feedback record`, `calibration-report`. Found and fixed a real, self-caught TOCTOU bug in the first implementation (existsSync-then-readFileSync before a whole-file rewrite) — the same anti-pattern fixed 4+ times earlier this session — via a single `fs.appendFileSync`, matching `fix-metrics.js`'s own established pattern; self-scan genuinely dropped from +3 to +1 as a direct result. Full gate: run 1 clean (4597/4597), run 2 hit the same documented flake again (standalone-confirmed unrelated, not chased per D-0035). **Cycle 60 was a correction-and-survey cycle, no code changes**: caught and fixed the false "E7 fully closed" claim (see the lesson noted above), and freshly surveyed **FR-403** at current HEAD (D-0039) — genuinely large, not another "mostly built" case: `privacy-taint.js` covers 7/9 sink categories but does zero safe-transformation recognition and does not plug into the general taint engine's catalog/interprocedural machinery at all, so real aliased/interprocedural/cross-file tracking would need a genuine architectural rework. Correctly NOT attempted. Cycle 61 completed **FR-705** (encrypt confidential state classes when a provider is configured or required), **genuinely closing Epic E7 — verified this time by reading every one of its 7 FRs' own status fields directly rather than trusting a narrative count.** Designed as Phase 1 of a staged rollout (D-0040): marked only `compliance-evidence.json`/`.md` confidential (the two candidates with one isolated writer each and no other readers), with every deferred candidate (hash-chained audit logs, engine.js-internal writers, the widely-read `last-scan.json`/`findings.json`) named explicitly with its own reason in the registry header — not silently dropped. Built `posture/encryption-provider.js`: one real `local-key` AES-256-GCM provider (a per-install key via the same atomic hard-link pattern `integrity.js` already established, kept in a SEPARATE key file), and the fail-closed gate itself — `maybeEncryptForWrite` returns `{ok:false}` with no content field at all when required encryption is unavailable, so a careless caller cannot write plaintext anyway. Both real writer functions wired, plus transparent decrypt-on-read in `verify-attestation`. Self-scan drift (+3) reviewed carefully given the crypto code: one TOCTOU confirmed identical in shape to `integrity.js`'s own already-accepted key-read pattern (deliberately mirrored), one the familiar accepted "Synchronous Blocking I/O" pattern. Full gate: clean on the FIRST attempt (4614/4614 — no flake recurrence this cycle), zero new dist chunks. Cycle 62 (planning only, no code): produced a CONCRETE, technically-grounded 4-step decomposition plan for **FR-403** (D-0041), at the user's explicit request to plan before implementing. Read the real `catalog.js`/`summaries.js`/`ir/callgraph.js`/`sanitizer-gate.js` code and found the call graph and `SummaryCache` are ALREADY family-agnostic — only `catalog.js`'s flat entries differ per family. Proof: XXE was added across 5 languages as pure catalog-entry additions with zero engine changes. Plan: (1) add a `privacy-leak` sink family to `catalog.js` mirroring the 7 sink categories already in `privacy-taint.js` — zero engine changes, genuinely low-risk alone; (2) add a `declared-as` catalog matcher so a PII/PHI/PCI-classified DECLARATION becomes a source (the one real catalog-shape gap); (3) wire privacy sources/sinks into the real CFG walk as an opt-in second lattice, bench-compared before ever becoming default; (4) cut the default path over only after 1-3 are proven, reusing the existing `irBacked` flag. See D-0041 for full detail and D-0039 for why this is genuinely large, not a "mostly built" case. Cycle 63 implemented **D-0041's step 1 for real**: `dataflow/privacy-catalog.js`, a privacy-leak (CWE-359) sink catalog covering all 9 named sink categories. A real correction was found DURING implementation, not anticipated by the grounding pass — **D-0042**: the step-1 "zero behavior change" claim, based on the shipped XXE precedent, does not actually generalize to privacy sinks, because `dataflow/engine.js`'s taint state is a shared boolean with no per-family filtering — merging privacy sinks into the general `CATALOG` would make every already-active general source (`req.body`, etc.) immediately trigger spurious privacy findings at ordinary log/fetch/email calls. Fixed by keeping the new catalog in a genuinely separate, currently-unconsumed file, verified inert via zero corpus/self-scan drift and a clean full `npm test`. FR-403 remains correctly `pending` — only step 1 of 4 is done. Cycle 64 implemented **D-0041's step 2**: `matchPrivacyDeclSource(s)` in the same `privacy-catalog.js`, calling directly into `privacy-taxonomy.js`'s existing `classifyFieldAgainst` — modeled after `catalog.js`'s own `matchAnnotationParams` precedent (per `dataflow/CLAUDE.md`, annotation-style matching runs once per function against a side-channel, not mid-CFG-walk, exactly the shape a declaration-name classifier needs). Verified against real regex-boundary edge cases while writing tests: camelCase compounds (`medicalRecordNumber`) match because those taxonomy patterns have no internal `\b`, while a single-word pattern (`\bdiagnosis\b`) would NOT match a compound like `diagnosisCode` — caught this distinction before it became a wrong test assertion, not after a failure. Also proved the matcher honors a caller-supplied custom taxonomy, so step 3's wiring will automatically respect FR-402's organization-defined classes for free. Full gate clean on the first attempt (4627/4627), zero new dist chunks. FR-403 remains correctly `pending` — 2 of 4 steps done. Cycle 65 implemented **D-0028's step (a) for E2/FR-201**: `src/pipeline/detector-runner.js`'s `runDetector`, modeled directly on the already-shipped FR-106 `runAnnotatorAsync` precedent's exact isolation contract but deliberately SYNCHRONOUS (D-0043) — confirmed all ~128 of `engine.js`'s per-file detector call sites are synchronous, so an async wrapper would add real Promise-scheduling overhead to the engine's single hottest loop for a capability nothing needs. Error records use `{file, analyzer, err}`, "analyzer" being the PRD's own literal FR-201/FR-203 vocabulary. Confirmed the real call-site count (128, via grep) and try/catch boundaries directly in `engine.js`'s per-file loop. Deliberately did NOT touch any of the 128 call sites — per D-0028's own phasing, migrating them is step (b), a separate, batched effort needing its own gate run after every batch. `pipeline/` sits outside the dead-module guard's scanned directories, matching `annotator-runner.js`'s own precedent — no allowlist entry needed despite no production caller yet. Full gate clean on the first attempt (4634/4634), zero new dist chunks. FR-201 remains correctly `ready` — step (a) of 3 done. Cycle 66 completed **D-0028's step (b) for E2/FR-201 in full, closing out FR-201 entirely** — all 128 real detector call sites in `engine.js` migrated to `runDetector` across 5 gate-verified batches (D-0044), `scan.detectorErrors`/`toJSON().detectorErrors` wired through (mirroring `annotatorErrors` — D-0045 explains why `scan-health.js`'s FR-203-owned `analyzers` ledger and FR-206-owned `status` computation were deliberately left untouched), and a real end-to-end fault-injection test (`test/detector-fault-injection.test.js`, mirroring FR-906's precedent) proves the acceptance criterion against an actual scan. Full final gate clean (4637/4637 + 26/26 + 1/1 + 1/1 + 5/5), zero new dist chunks. **FR-201 is now `verified` — the first of E2's 8 FRs to close.** Cycle 67 grounded **FR-202** via a dedicated investigation (**D-0046**): confirmed both existing timeout mechanisms (`_perFileTimeoutMs`, deep-mode `deadlineMs`) are non-preemptive (post-hoc measurement / cooperative clock-poll), a structural consequence of JavaScript's single-threaded model, not a code-quality gap — and found `src/engine-parallel.js` is dead, unwired scaffolding with zero real callers. Recorded a 4-phase plan mirroring D-0028's FR-201 shape exactly (isolated primitive → data-contract design → real opt-in wiring with a full accuracy/determinism/self-scan/corpus comparison → default-cutover, explicitly out of scope for the literal acceptance criterion). Implemented **phase 1 for real**: `src/pipeline/analyzer-supervisor.js`'s `runWithDeadline`, a genuine `worker_threads`-based primitive proven against a REAL unmocked infinite loop (`hangForever()`) — terminated within its configured 300ms+200ms deadline+grace window (~501ms measured), the literal FR-202 scenario, at the primitive level. Zero `engine.js` changes. Full gate clean (4643/4643 + 26/26 + 1/1 + 1/1 + 5/5), zero new dist chunks. **FR-202 remains correctly `ready` — 1 of 4 D-0046 phases done.** Cycle 68 grounded **FR-403's step 3** via a dedicated investigation (**D-0047**), mirroring D-0046's approach: mapped exactly where the general CFG walk consults `catalog.js` (`step`'s node-kind switch → `_matchCallCatalog`/`_sinkFindingsForCall` → `matchSinkOrSanitizer`/`matchSource`), confirmed the per-variable taint state is a bare `Set<string>` with no per-path metadata, and confirmed `_unionAnnotationTaint` is the exact precedent for seeding entry state from a pre-walk classification. Faced a real design choice — thread a second Set through `step`/`exprTaint`'s existing signature (efficient but touches the general engine's single hottest, most heavily-relied-upon function) vs. a genuinely separate walker module (some duplication, zero shared risk) — and **chose the separate walker**, extending D-0042's isolation mandate from catalog DATA to the WALKER CODE PATH. Implemented the smallest safe next increment: `matchPrivacySink` in `privacy-catalog.js`, mirroring `matchSinkOrSanitizer`'s contract via an independent linear scan, tested against both real IR callee shapes, receiver-type gating in both directions, and language gating. Zero `engine.js` changes — the actual walker (intra-procedural v1 scope) remains the next concrete, deliberately-not-rushed step. Full gate clean (4649/4649 + 26/26 + 1/1 + 1/1 + 5/5), zero new dist chunks. **FR-403 remains correctly `pending` — step 3 now fully designed, its sink-matching primitive built, the walker itself not yet started.** Cycle 69 built and wired **FR-403 step 3's v1 walker for real**: `dataflow/privacy-deep-walker.js` (`analyzePrivacyFunction`/`runPrivacyTaintEngine`/`exprPrivacyTaint`) — a genuinely separate intra-procedural CFG walker, zero shared code with `step`/`exprTaint`, reusing only `access-paths.js`'s pure Set utilities, seeding entry state via `matchPrivacyDeclSources(fn.params)` (mirroring `_unionAnnotationTaint`'s exact precedent) and checking `matchPrivacySink` at call/return nodes. Wired into `dataflow/index.js`'s `runDeepAnalysis` behind `AGENTIC_SECURITY_PRIVACY_DEEP=1` (default off), mirroring the existing `AGENTIC_SECURITY_IFDS=1` block exactly. Proved it two ways: 9 hand-constructed-fn unit tests (mirroring `test/annotation-taint-engine.test.js`'s established pattern) and a real end-to-end test (`test/privacy-deep-e2e.test.js`, mirroring `test/deep-taint.test.js`) scanning a real fixture through the real parser — a declaration named `socialSecurityNumber` (classified by NAME alone, proving the D-0041-step-2 declaration-source path specifically, since its RHS does not independently classify) fires a real finding with the flag on, and produces ZERO findings with the flag off (both deep-on-flag-off and deep-off cases) — the critical off-by-default safety proof. Full gate clean (4662/4662 + 26/26 + 1/1 + 1/1 + 5/5), corpus 215/215 ZERO drift (this touches `dataflow/index.js`, a REAL file in the default execution path, unlike `privacy-catalog.js`), self-scan zero drift, build clean (new modules now bundled into `dist/index.js` via real static imports, zero new untracked chunks). **FR-403 remains correctly `pending`** — v1 covers only direct-flow + declaration-based sources within one function; aliasing, interprocedural flow, cross-file flow, safe-transformation recognition, and the before/after recall-comparison harness are all still needed. Separately, a background investigation completed **E2's FR-202 D-0046 phase 2** (recorded as **D-0048**): the full 128-detector side-channel audit, correcting phase 1's overstated `_suppressionLog` writer count (4 real in-loop detectors, not "dozens"), confirming 2 more broadcast-once candidates, confirming no hidden non-per-file detector or order dependency, and finding one genuine NEW bug independent of FR-202 — `src/sast/cpp.js`'s `_entropyMod` async-singleton race, which a worker-per-file design would expose (today's single-process timing happens to avoid it). Not fixed yet; flagged as phase 3's prerequisite. **FR-202 remains correctly `ready` — 2 of 4 D-0046 phases done.** Cycle 70 fixed **D-0048's `_entropyMod` prerequisite bug**: `src/sast/cpp.js`'s `cpp-hardcoded-secret` gate resolved `_secret-entropy.js` via a fire-and-forget dynamic `import()` into a module-level `let`, racing the first `scanCpp()` call — safe today only by single-process timing luck, unsafe under a future worker-per-file boundary. The stated "circular dep" justification didn't hold (`_secret-entropy.js` has zero imports of its own; `engine.js` already imports the same function statically). Fixed to a plain static import — synchronous and race-free by construction. Added `test/cpp-hardcoded-secret.test.js` (3 tests) — this rule had zero direct coverage before. Full gate clean (4665/4665 + 26/26 + 1/1 + 1/1 + 5/5), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3. **FR-202's phase-3 prerequisite is now closed; phase 3 itself (real per-file worker wiring) has not started.** Cycle 71 built **FR-403 step 3's before/after recall-comparison harness** (item (e), `bench/privacy-recall/`) — a new, standalone `measure.mjs`/`check.mjs`/`BASELINE.json` mirroring `bench/self-scan/check.mjs`'s exit-code contract, since `bench/layer-recall` was not reusable (always deep-on, no on/off diff axis, no privacy-shaped corpus). 3 hand-written fixtures empirically proved a REAL recall improvement, not just parity: `renamed-before-sink` (a privacy-classified value reassigned to a differently-named variable before the sink) scores shallow=0/deep=1 — the shallow engine's name-in-text matching structurally cannot see this, the new walker's real Set-based propagation does. `same-name-direct` shows parity (shallow=1/deep=1) and `clean-negative` shows no false positive (0/0). Proved the gate fails on injected drift (exit 1) and passes clean when restored (exit 0). Found and fixed a real state-pollution issue along the way: `disableStateWrites()` doesn't cover every writer (threat-model/sbom-history annotators write regardless) — added explicit `purgeScanState()` calls, confirmed zero stray state afterward. Wired as `bench:privacy-recall`/`:check`/`:update-baseline`, deliberately left standalone (not hard-gated in pre-push/release, matching `iac-coverage`/`secrets-precision`'s precedent, since the capability itself is still opt-in and incomplete). Full gate clean (4665/4665 + 26/26 + 1/1 + 1/1 + 5/5, unaffected by the new bench files), corpus 215/215 zero drift, self-scan zero drift, build clean, smoke exit 3. **FR-403 remains correctly `pending`** — items (a)-(d) (aliasing, interprocedural, cross-file, safe-transformation) remain, each extendable via this same harness rather than a new one. Cycle 72 implemented **item (d), safe-transformation recognition**: added `PRIVACY_TRANSFORM_CALLEES` (private, named-list only, mirroring `_UNSANITIZER_CALLEES`'s precedent) and `isPrivacyTransformCallee` to `privacy-catalog.js`; threaded a per-function `ctx.transformsByVar` side-map through the walker, mirroring `_sanitizersByVar` exactly. A value that passed through a recorded transform (hash/bcrypt/encrypt/mask/redact/etc.) still fires — recall-preserving, per the general engine's own documented sanitizer philosophy — but at demoted confidence (0.6→0.18) with `_privacyTransformsOnPath` recorded; a re-assignment from a non-transform source correctly clears the credit (proven by test). Caught and fixed a real dead-modules violation (`PRIVACY_TRANSFORM_CALLEES` needed to be private, not exported) and reviewed a genuine self-scan false positive (`OWNERSHIP-AUTHZ`'s lifecycle-gate heuristic misfiring on the walker's own generic vocabulary) before accepting it into the baseline. Full gate clean (4668/4668 + 26/26 + 1/1 + 1/1 + 5/5), corpus 215/215 zero drift confirmed twice (before and after the baseline update), self-scan clean against the reviewed baseline, build clean, smoke exit 3. **FR-403 remains correctly `pending`** — items (a)-(c) (aliasing, interprocedural, cross-file) remain. Cycle 73 implemented **item (a), simple same-function aliasing**: added a symmetric `ctx.aliasesByVar` side-map; `exprPrivacyTaint` gained an optional third `ctx` parameter, resolved through the alias map via a new `_isCoveredByWithAliases`; `stepPrivacy`'s `assign` case records a bare `a = obj` as a mutual alias, propagates a MEMBER WRITE through either name to the other, and severs ALL of a variable's existing alias relationships (both directions) before recording a new one on reassignment. Two real bugs caught and fixed during test-writing, not after: (1) the first implementation added a new alias without removing the stale one on reassignment, so `a` could end up aliasing both its old and new target simultaneously — caught by a dedicated reassignment test; (2) all three alias tests initially used a PII-shaped CONTAINER variable name, which meant the container's own declaration-based classification tainted the whole object independent of aliasing — every test passed for the wrong reason, caught before being trusted, and rewritten with a neutral container name plus a genuinely PII-shaped parameter as the real source. Reviewed one more self-scan false positive of the same class already accepted in cycle 72, confirmed identical, not new. Full gate clean (4671/4671 + 26/26 + 1/1 + 1/1 + 5/5), corpus 215/215 zero drift confirmed twice, self-scan clean against the reviewed baseline, build clean, smoke exit 3. **FR-403 remains correctly `pending`** — items (b)-(c) (interprocedural, cross-file) remain, both substantially larger than what's been done so far. Cycle 74 implemented **items (b) and (c), interprocedural and cross-file flow**, closing FR-403 in full. Item (b): threaded an `interprocCtx` (`{callGraph, summaryCache, stack}`) through the walker; a call whose args are tainted and whose callee resolves via `callGraph.resolveKnownCallee()` to a real `functionRecord` with a CFG re-runs the shared worklist core under a synthetic entry state seeded from the tainted parameter positions, with a recursion guard (`stack`) and a per-`(qid, taintedParamIdx)` `summaryCache` — deliberately reusing only `ir/callgraph.js`'s pure, already-built call graph rather than inventing a second one, per the `D-0047` isolation rule (pure read-only infrastructure is safe to share; the walker's own mutable taint state is not). Proactively designed the interprocedural test fixture with NEUTRAL call-site argument names (not PII-shaped), applying cycle 73's test-design-confound lesson before writing a single test rather than after a false pass. Item (c), cross-file flow, required **zero additional code** — verified empirically with a real 2-file fixture (`require()` across files) before assuming otherwise, since `callGraph` already spans the whole scan, not one file. Closed a real acceptance-criterion gap found on re-reading FR-403's literal text: aliasing (item a) and safe-transformation (item d) had only unit-level proof, not the PRD-required integration-test coverage — added real e2e fixtures/tests for both alongside the new interprocedural/cross-file ones (`test/privacy-deep-e2e.test.js` grew to 11 tests spanning direct, aliased, interprocedural, cross-file, and safe-transformation flows, each through the real parser). Full gate clean (4682/4682 + 26/26 + 1/1 + 1/1 + 5/5), corpus 215/215 zero drift, self-scan clean, build clean, smoke exit 3. **FR-403 is now `verified` — closing Epic E4, the 9th of 10 epics. Only E2 remains, with 6 requirements: FR-202/203/204/205/207/208.**

**Nothing has been committed.** This is intentional — leave it as a clean reviewable diff unless/until asked to commit.

### Exact current git status (verify this matches before trusting anything else in this file)

Modified this cycle (88): `scanner/src/privacy/ir-adapter.js` (new imports `parseSync`/`traverse` from `@babel/core`, `presetTypescript` from `@babel/preset-typescript`; new `_tsTypeToString`, `_qualifiedTypeName`, `_extractTsTypes` functions; `_adaptFile` gained an optional second `tsTypes` param; `adaptIRForPrivacyTaint` computes and passes it per file; module header comment rewritten to describe the real mechanism instead of "deliberately out of scope"; `_internals` export list extended). `scanner/test/privacy-ir-adapter.test.js` (+16 tests, imports extended). `docs/implementation/assurance-hardening-{state.json,status.md}`, this file. `scanner/dist/` rebuilt and fully re-staged — 20 files (same 8 chunks, no new boundary — `@babel/preset-typescript` was already a dependency `parser-js.js` pulls in, so this adds no new package). **Separately, this same turn, release prep for v0.144.0 (user asked to "push a new version to github and npm"): version bumped 0.143.0→0.144.0 in `scanner/package.json`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (both occurrences), `gemini-extension.json`, `CLAUDE.md`, `README.md` (all 6 files `scripts/release-check.mjs`'s `VERSION_FILES` checks); root `CHANGELOG.md` gained a `## 0.144.0 — ...` section summarizing the whole assurance-hardening PRD body of work, prepended above the two already-`## Unreleased` perf-fix entries which now fall under the same heading (nothing in that section had been published yet, so folding them into the same release is correct, not a rewrite of already-released history). `scanner/CHANGELOG.md` is gitignored/generated — not touched directly, regenerates via `sync-scanner-changelog.mjs` at publish time. Full release-check gate was run once **before** this cycle's FR-401 work (found 3 real issues: `expected-package-manifest.json`'s fileCount stale against ~90 new files, `npm test` exit 1 — later confirmed the same pre-existing "no prior scan" flake class, and two behind-latest dependencies (`@types/node`, `js-yaml`) needing either an upgrade or a documented hold) — **none of those three items have been addressed yet as of this checkpoint**; the release-check gate has NOT been re-run since the FR-401 types changes landed. **Do not tag/push a release from this state without first: (1) fixing or holding the dependency-currency findings, (2) updating `expected-package-manifest.json`'s fileCount deliberately, (3) re-running `npm run release:check` (no `--fast`) clean.**

Modified this cycle (87): `scanner/src/privacy/ir-adapter.js` (new `_storageForFile` helper; `adaptIRForPrivacyTaint`'s signature extended with an optional third `storedRegistry` param; output shape gains a `storage` field; module header comment extended explaining both storage's real data source and why types was deliberately deferred). `scanner/src/engine.js` (one-line call-site change: `adaptIRForPrivacyTaint(_sharedIR.perFile, fc, storedRegistry)`, reusing the `storedRegistry` const already computed earlier in the same function for `crossStoredTaint`). `scanner/test/privacy-ir-adapter.test.js` (+9 tests: `_storageForFile` unit coverage, `adaptIRForPrivacyTaint`'s storage field, 2 real end-to-end tests; 1 existing `deepEqual` assertion updated for the new `storage:[]` field in the no-IR-entry degrade path). `docs/implementation/assurance-hardening-{state.json,status.md}`, this file. `scanner/dist/` rebuilt and fully re-staged — 20 files (same 8 chunks, `calibration-seed.json` still carries cycle 85's `_generatedAt`, no new chunk — purely additive statically-imported code). Verify with `git diff --cached --name-only -- scanner/dist/` and `git log -1` still showing `f57f496` (no new commit). **NOTHING in `scanner/src/ir/` (the parser layer) was touched this cycle** — the "types" deferral means zero changes to `parser-js.js` or any sibling parser.

Modified this cycle (86): `scanner/src/posture/scan-checkpoint.js` (FORMAT bumped `/1`→`/2`; new exports `computeGlobalKey`, `globalKeyMeta`, `invalidatedFiles`; `openCheckpoint`'s param renamed `runKey`→`globalKey`, new `meta`/`fileContents` params; `recordFileDone` now hashes and stores each record's source content; `_recover` does per-file content comparison and returns a discard reason; `computeRunKey` untouched). `scanner/src/engine.js` (checkpoint call site switched from `computeRunKey`/`{runKey}` to `computeGlobalKey`/`globalKeyMeta`/`{globalKey,meta,fileContents}`; new `_ckptInvalidated` local; `_scanMeta.checkpoint` extended with `discarded`/`discardReason`/`invalidatedFileCount`/`invalidatedFiles`/`invalidatedFilesTruncated`). `scanner/test/scan-checkpoint.test.js` (9 low-level tests mechanically renamed `runKey`→`globalKey`; 2 end-to-end tests rewritten to assert the NEW granular behavior instead of the old blunt one, now split into 3 (change/add/remove); 1 test extended with discard-reason assertions; 5 new unit tests for `computeGlobalKey`/`globalKeyMeta`/`invalidatedFiles` — 14→19 tests total). `docs/implementation/assurance-hardening-{state.json,status.md}`, this file. `scanner/dist/` rebuilt and fully re-staged — 20 files (same 8 chunks as before, `dist/calibration-seed.json` still carries cycle 85's `_generatedAt`, no new chunk — the checkpoint changes are all statically-imported, no new dynamic-import boundary). Verify with `git diff --cached --name-only -- scanner/dist/` and `git log -1` still showing `f57f496` (no new commit).

Modified this cycle (85): `scanner/src/engine.js` (EPSS live-cache path now stores `{score,percentile,ts}`; new `epssLiveMeta()` export honoring `AGENTIC_SECURITY_OFFLINE`; `applyFreshness()` call wired right after `computeScanHealth()`), `scanner/src/pipeline/scan-health.js` (new `applyFreshness()` export + `freshness` field), `scanner/src/posture/calibration.js` (new `calibrationFreshness()` export), `scanner/src/posture/calibration-seed.json` (new real `_generatedAt` field, git-log-derived, not fabricated), `scanner/src/posture/custom-rules.js` (new `customRulesFreshness()` export), `scanner/bin/agentic-security.js` (imports `customRulesFreshness`, patches `scan.scanHealth` via `applyFreshness` right after the custom-rules DSL block). New test file: `scanner/test/custom-rules-freshness.test.js` (6 tests). Extended: `scanner/test/scan-health.test.js` (+7), `scanner/test/assurance-mode.test.js` (+2), `scanner/test/calibration.test.js` (+1), `scanner/test/sca-batch.test.js` (+2) — plus `AGENTIC_SECURITY_OFFLINE=1` added to two PRE-EXISTING tests (`scan-health.test.js`'s "reports complete" and `ci.test.js`'s "--assurance strict ... exits 0") that broke against this dev machine's real, previously-invisible stale EPSS disk cache once FR-207's wiring made that staleness visible — a genuine regression caught by the gate, not a flake, fixed by isolating the tests from real host cache state (see this cycle's status.md entry for the full story). `scanner/package.json` (registered the new test file in `test:posture`). `docs/implementation/assurance-hardening-{state.json,status.md}`, this file. `scanner/dist/` rebuilt and fully re-staged — 19 files (still the same 8 chunks as before, `dist/calibration-seed.json` newly modified from the `_generatedAt` addition, no new chunk). Verify with `git diff --cached --name-only -- scanner/dist/` and `git log -1` still showing `f57f496` (no new commit).

Modified this cycle (84): No `src/` changes — FR-205 was closed purely by test additions proving already-existing machinery. New/modified test files: `scanner/test/report-render.test.js` (+1 test, 32 total), `scanner/test/ci.test.js` (+2 real end-to-end tests, 12 total). `docs/implementation/assurance-hardening-state.json` (FR-205 `ready`→`verified`), `docs/implementation/assurance-hardening-status.md`, this file. `scanner/dist/` rebuilt and fully re-staged (`git add dist/`) — 18 files total (17 already-tracked chunks + `agentic-security.mjs` + its `.sha256`), **no new chunk** (no new dynamic-import boundary this cycle); the working-tree diffs on those 17 chunks vs. the prior cycle's staged content are ncc's own minifier-output variance across separate builds of otherwise-unchanged source, not a code regression — re-staged so the index matches what's actually built. Verify with `git diff --cached --name-only -- scanner/dist/` and `git log -1` still showing `f57f496` (no new commit).

Modified this cycle (80): `scanner/src/engine.js` (fixed 2 of the original 128 detector call sites — `scanWeb3Advanced`/`scanK8sAdmission` — to wrap in `runDetector`, matching the other 126; no other engine.js changes, FR-203 itself is not yet implemented). New: `scanner/test/detector-fault-injection-k8s-admission.test.js` (1 test, its own file per `detector-fault-injection.test.js`'s documented `mock.module()` binding constraint). `scanner/package.json` (registered the new test as its own top-level `test` script step, alongside `detector-fault-injection.test.js`). `docs/implementation/assurance-hardening-decisions.md` (D-0051 — the FR-201 correction AND the FR-203 grounding plan). No new dist chunk.

Modified this cycle (79): `scanner/src/engine.js` (new `_resolveEngineModulePathForWorkers()` helper; new `_emptyCascadeDelta(c)`; the per-file loop now creates `_cascadePool` when `AGENTIC_SECURITY_WORKER_CASCADE=1`, dispatches via `pool.runFile(...)` instead of the direct `_runFileCascade` call, wrapped in `try{}finally{await pool.shutdown()}`; `let i` hoisted above the `try` to fix a real scoping bug the new wrapper introduced). `scanner/src/pipeline/cascade-worker-pool.js` (new `_resolveDefaultWorkerScript()`, fixing the same bundled-path problem for the worker script itself). New: `scanner/test/cascade-pool-wiring.test.js` (3 tests through the real `runFullScan()`). `docs/implementation/assurance-hardening-decisions.md` (D-0050, extended with the final wiring + bundling-bug lesson). `scanner/dist/265.index.js` **NOW ALSO STAGED** (`git add`, not committed) — a SEVENTH dist chunk, from `engine.js`'s new dynamic `import('./pipeline/cascade-worker-pool.js')`, following the same D-0027 protocol as the other six. Verify with `git diff --cached --name-only` — it must show EXACTLY `scanner/dist/1.index.js`, `scanner/dist/144.index.js`, `scanner/dist/265.index.js`, `scanner/dist/552.index.js`, `scanner/dist/730.index.js`, `scanner/dist/736.index.js`, and `scanner/dist/920.index.js`, nothing else, and `git log -1` must still show `f57f496` (no new commit). **FR-202 marked `verified`** in state.json.Modified this cycle (78): `scanner/src/engine.js` (new export `async function _initCascadeWorkerState(fileContents, scanRoot)`, mirroring `runFullScan`'s own pre-loop setup sequence for use by a worker before it starts handling `_runFileCascade` calls). New: `scanner/src/pipeline/cascade-worker-pool.js` (`createCascadePool`), `scanner/src/pipeline/cascade-worker.js` (the long-lived worker entry point), `scanner/test/cascade-worker-pool.test.js` (8 tests), `scanner/test/fixtures/cascade-worker-pool/synthetic-cascade.mjs` (test fixture). Fixed: `scanner/test/sca-route-reachable.test.js` (wrapped its `runFullScan` call in `AGENTIC_SECURITY_NO_STATE=1`/`finally{delete...}`, fixing a real state leak into the real repo). `docs/implementation/assurance-hardening-decisions.md` (D-0050). `bench/self-scan/BASELINE.json` (scanner/src 445→447, +2 reviewed and accepted — the same pattern-matcher false-positive class already accepted elsewhere this session). Deleted the one corrupted `scanner/.agentic-security/sbom-history/0a3e50e733c6.json` snapshot the leak had produced (pure generated scan state, not user data). No new dist chunk — neither new pipeline file has a production caller yet.Modified this cycle (77): `scanner/src/engine.js` (`_runFileCascade` moved from an inner `const ... = (p, c) => {...}` closure inside `runFullScan` to a top-level `export function _runFileCascade(p, c, scanRoot, _detectorErrors) {...}`; its call site in `runFullScan`'s loop updated to `_runFileCascade(p,c,scanRoot,_detectorErrors)`; zero changes to the function's ~135-line body beyond the signature, since its only two closure dependencies were promoted to identically-named parameters). No test files changed — purely a relocation + explicit parameterization, verified by the EXISTING suite showing zero drift plus a live `import()` confirming the export works. No new dist chunk.Modified this cycle (76): `scanner/src/engine.js` (the 4 real `_suppressionLog`-writing detectors — `scanStructuralVulns`/`scanLogicVulns`/`scanExtraStructural`/`scanEntropySecrets` — each gained an optional third parameter `_suppOut`, defaulting to the module-level `_suppressionLog` when omitted; `_runFileCascade` now declares a local `_aSupp` accumulator, passes it as the third argument to all 4 calls, and returns it as `suppressions:_aSupp` in its delta object; the outer loop applies `_suppressionLog.push(..._delta.suppressions)` immediately after applying the other 9 delta arrays, before `_ckptRecord`). No test files changed — purely additive, backward-compatible parameter; verified by the EXISTING suite showing zero drift. No new dist chunk.Modified this cycle (75): `scanner/src/engine.js` (per-file cascade extracted into a new inner function `_runFileCascade(p, c)` — all 9 previously-shared accumulator arrays (`aR/aF/aSrc/aSink/aSan/aLogic/aSecrets/aCiphersRest/aCiphersTransit`) renamed to fresh locals inside it and returned as one delta object `{content, pfr, routes, findings, sources, sinks, sanitizers, logic, secrets, ciphersRest, ciphersTransit}`; the outer per-file loop now calls `_runFileCascade` and applies the returned delta, with the two skip-checks, the checkpoint-replay check, and the cooperative-yield check all deliberately left exactly where they were in the outer loop). `docs/implementation/assurance-hardening-decisions.md` (D-0049). No test files changed — this is a pure internal refactor verified by the EXISTING test/corpus/self-scan suites showing zero drift, which is the point (see D-0049). No new dist chunk.Modified cycle 74: `scanner/src/dataflow/privacy-deep-walker.js` (added item (b) interprocedural resolution — `_nestedCallReturnPrivacyTainted`, `_resolvableCalleeNameSimple`, `_computeReturnTaintUnderEntry`; extracted the worklist core into `_runWorklist`/`_freshCtx`; `runPrivacyTaintEngine` now builds a per-scan shared `{callGraph, summaryCache, stack}`), `scanner/src/ir/callgraph.js` import added (`functionRecord` — a pure, read-only lookup), `scanner/test/privacy-deep-walker.test.js` (4 more tests, 19 total, plus a new `multiCallGraph()` helper with a minimal honest `resolveKnownCallee` stub), `scanner/test/privacy-deep-e2e.test.js` (7 more tests, 11 total: 2 aliasing, 2 interprocedural, 2 cross-file, 1 safe-transformation — closing the "unit-only" gap against the literal acceptance criterion), `scanner/test/fixtures/privacy-deep/{interprocedural,cross-file,aliasing,safe-transform}/` (4 new fixture directories, `cross-file/` genuinely spans 2 files), `bench/privacy-recall/fixtures/interprocedural/` + re-baselined `BASELINE.json` (4th fixture, shallow=0/deep=1). Full gate clean.

Modified cycle 73: `scanner/src/dataflow/privacy-deep-walker.js` (added `ctx.aliasesByVar` side-map; `exprPrivacyTaint` gained an optional third `ctx` param, resolved via new `_isCoveredByWithAliases`; `stepPrivacy`'s `assign` case records/severs aliases and propagates member writes across them), `scanner/test/privacy-deep-walker.test.js` (extended, 3 more tests, 15 total — rewritten once after a test-design confound was caught), `bench/self-scan/BASELINE.json` (scanner/src 444→445, +1 — a reviewed, accepted `OWNERSHIP-AUTHZ` false positive of the SAME class as cycle 72's, not a new issue). Full gate clean.

Modified cycle 72: `scanner/src/dataflow/privacy-catalog.js` (added `PRIVACY_TRANSFORM_CALLEES` — private, not exported — and `isPrivacyTransformCallee`), `scanner/src/dataflow/privacy-deep-walker.js` (threaded a per-function `ctx.transformsByVar` side-map through `stepPrivacy`/`analyzePrivacyFunction`, mirroring `_sanitizersByVar`; `_privacySinkFindingsForCall` now demotes confidence and records `_privacyTransformsOnPath` when a transform is on the path), `scanner/test/privacy-deep-walker.test.js` (extended, 3 more tests, 12 total), `bench/self-scan/BASELINE.json` (scanner/src 443→444, +1 — a reviewed, accepted `OWNERSHIP-AUTHZ` false positive on the new file's own generic vocabulary, not a real defect). Full gate clean.

Modified cycle 71: `bench/privacy-recall/` (new directory — `measure.mjs`, `check.mjs`, `BASELINE.json`, `fixtures/{same-name-direct,renamed-before-sink,clean-negative}/app.js`), `scanner/package.json` (registered `bench:privacy-recall`/`:check`/`:update-baseline`). Nothing inside `scanner/src/` or `scanner/test/` changed — this is a new, standalone bench directory only, confirmed not to affect `npm test`'s count. `git status --short bench/privacy-recall/` shows `?? bench/privacy-recall/` (new, untracked directory — nothing staged or committed).

Modified cycle 70: `scanner/src/sast/cpp.js` (replaced the fire-and-forget `import('./_secret-entropy.js').then(...)` module-level singleton with a plain static `import { classifySecretCandidate as _entropyClassifySecret }`; the `cpp-hardcoded-secret` rule's gate now calls it directly — no more race window), `scanner/test/cpp-hardcoded-secret.test.js` (new, 3 tests — this rule had ZERO direct test coverage before), `scanner/package.json` (registered it in `test:sast`). Full gate clean, zero new dist chunks.

Modified cycle 69: `scanner/src/dataflow/privacy-deep-walker.js` (new — `analyzePrivacyFunction`/`runPrivacyTaintEngine`/`exprPrivacyTaint`, the FR-403 step 3 v1 intra-procedural walker), `scanner/src/dataflow/index.js` (added the `AGENTIC_SECURITY_PRIVACY_DEEP=1` opt-in wiring block in `runDeepAnalysis`, mirroring the existing `AGENTIC_SECURITY_IFDS=1` block exactly), `scanner/test/privacy-deep-walker.test.js` (new, 9 tests), `scanner/test/privacy-deep-e2e.test.js` (new, 4 tests), `scanner/test/fixtures/privacy-deep/direct-flow/app.js` (new fixture), `scanner/package.json` (registered both new test files in `test:dataflow`). This DOES touch a real, non-dormant file in the default execution path (`dataflow/index.js`) — verified via corpus 215/215 zero drift and self-scan zero drift that the opt-in gate genuinely produces zero behavior change when unset (which is every existing scan, since the flag defaults off). No new dist chunk — both new modules are picked up by ncc's static-import tracing into the main bundle (size grew 3876kB→3887kB, expected).

Modified cycle 68: `scanner/src/dataflow/privacy-catalog.js` (extended — added `matchPrivacySink(calleeExpr, file, receiverType)`, mirroring `matchSinkOrSanitizer`'s contract via an independent linear scan; imports the pure `_languageFamilyExtensions` helper from `catalog.js`, its only catalog.js dependency), `scanner/test/privacy-catalog.test.js` (extended — 6 more tests, 19 total). No changes to `engine.js` — the actual walker is not built yet, only its sink-matching primitive. No new dist chunk.

Modified cycle 67: `scanner/src/pipeline/analyzer-supervisor.js` (new — `runWithDeadline`, D-0046 phase 1's real worker_threads deadline primitive), `scanner/src/pipeline/analyzer-worker.js` (new — the generic worker-thread entry point it spawns), `scanner/test/fixtures/analyzer-supervisor/synthetic-analyzers.mjs` (new — a REAL busy-wait `hangForever()` plus `quickWork`/`throwsError` fixtures), `scanner/test/analyzer-supervisor.test.js` (new, 6 tests), `scanner/package.json` (registered it in `test:report`). No changes to `engine.js` — this is an isolated, currently-unwired primitive, exactly matching FR-201's D-0028 step (a) precedent. No new dist chunk (neither new module has an importer yet, so ncc's static bundler excludes both — noted for phase 3: `analyzer-worker.js` is referenced via a runtime string path to `new Worker()`, not a static import, and will need explicit attention when real wiring happens).

Modified cycle 66: `scanner/src/engine.js` (all 128 detector call sites in the per-file and cross-file loops migrated to `runDetector(_detectorErrors, file, 'name', fn)`; `_detectorErrors` array declared alongside the other per-scan accumulators; `detectorErrors:_detectorErrors` added to the return object), `scanner/src/report/index.js` (added `detectorErrors` to `toJSON()`'s output, mirroring `annotatorErrors`), `scanner/test/detector-errors.test.js` (new, 3 tests), `scanner/test/detector-fault-injection.test.js` (new, 1 test, real end-to-end fault injection via `mock.module()` — mirrors `test/fault-injection.test.js`'s FR-906 precedent), `scanner/package.json` (registered `detector-errors.test.js` in `test:report`; registered `detector-fault-injection.test.js` as its own top-level `test` script step, alongside `fault-injection.test.js`). No new dist chunk — `engine.js`'s changes are all within its existing static import graph, no new dynamic-import boundary. `git diff --cached --name-only` should be UNCHANGED from cycle 59's six staged chunks (the modification is to `engine.js`/`report/index.js`'s WORKING TREE content, not to what's staged).

Modified cycle 65: `scanner/src/pipeline/detector-runner.js` (new — `runDetector`, the E2/FR-201 isolation helper, D-0028 step (a)/D-0043), `scanner/test/detector-runner.test.js` (new, 7 tests), `scanner/package.json` (registered it in `test:report`, alongside `annotator-runner.test.js`). No changes to `engine.js` or any production caller — zero of the ~128 real detector call sites touched, by design. No new dist chunk — `git diff --cached --name-only` should be UNCHANGED from cycle 59's six staged chunks.

Modified cycle 64: `scanner/src/dataflow/privacy-catalog.js` (extended — added `matchPrivacyDeclSource`/`matchPrivacyDeclSources`, a declaration-based source classifier calling into `privacy-taxonomy.js`), `scanner/test/privacy-catalog.test.js` (extended — 7 more tests, 13 total). No changes to `catalog.js`, `dataflow/engine.js`, or any production caller. No new dist chunk.

Modified cycle 63: `scanner/src/dataflow/privacy-catalog.js` (new — dormant, isolated privacy-leak sink catalog per D-0041 step 1 / D-0042's isolation correction), `scanner/test/privacy-catalog.test.js` (new, 6 tests), `scanner/package.json` (registered it in `test:dataflow`). No changes to `catalog.js`, `dataflow/engine.js`, or any production caller — the new module has zero importers by design. No new dist chunk (the module isn't reachable from the bundle's entry point yet). Cycle 62 was planning-only (no files changed).

Modified cycle 61: `scanner/src/posture/encryption-provider.js` (new — local-key AES-256-GCM provider, policy loader, fail-closed gate, transparent decrypt), `scanner/src/posture/artifact-registry.js` (new `confidential` field + `confidentialOf()` export; `compliance-evidence.json`/`.md` marked confidential; `encryption-policy.yml` registered as operator-config; header extended with the full deferred-candidate rationale), `scanner/src/posture/compliance-policy.js` (both `emitEvidenceJsonLd`/`emitEvidenceMarkdown` now route through the fail-closed gate before writing), `scanner/bin/agentic-security.js` (`cmdVerifyAttestation` now transparently decrypts an encrypted bundle before parsing), `scanner/test/encryption-provider.test.js` (new, 17 tests), `scanner/package.json` (registered it in `test:report`), `bench/self-scan/BASELINE.json` (scanner/src 440→443, +3 reviewed and accepted — one TOCTOU pattern confirmed identical to `integrity.js`'s own precedent, one accepted "Synchronous Blocking I/O" pattern). No new dist chunk this cycle (`encryption-provider.js` is a static import inside `compliance-policy.js`, not a new dynamic-import boundary) — `git diff --cached --name-only` should be UNCHANGED from cycle 59's six staged chunks.

Modified cycle 59: `scanner/src/posture/calibration-feedback.js` (new — opt-in calibration feedback record/load/aggregate/render), `scanner/src/posture/artifact-registry.js` (registered `calibration-feedback.jsonl` as operator-config), `scanner/bin/agentic-security.js` (new `cmdCalibrationFeedback`/`cmdCalibrationReport` functions + `case 'calibration-feedback'`/`case 'calibration-report'` dispatch + USAGE text), `scanner/test/calibration-feedback.test.js` (new, 19 tests), `scanner/package.json` (registered it in `test:report`), `bench/self-scan/BASELINE.json` (scanner/src 439→440, net +1 after a genuinely-fixed TOCTOU that would otherwise have been +3). **`scanner/dist/1.index.js` is now ALSO staged** (`git add`, not committed) — a sixth dist chunk, from the two new command functions' dynamic imports, following the same D-0027 protocol as the other five. Verify with `git diff --cached --name-only` — it must show EXACTLY `scanner/dist/1.index.js`, `scanner/dist/144.index.js`, `scanner/dist/552.index.js`, `scanner/dist/730.index.js`, `scanner/dist/736.index.js`, and `scanner/dist/920.index.js`, nothing else, and `git log -1` must still show `f57f496` (no new commit).

Modified cycle 58: `scanner/src/posture/production-feedback.js` (new — read-only 5-source aggregator), `scanner/bin/agentic-security.js` (`cmdTriage`'s `trend` sub now also renders a "Production feedback" section), `scanner/test/production-feedback.test.js` (new, 16 tests), `scanner/test/no-dead-modules.test.js` (removed 2 stale allowlist entries — `suppressions.js::loadSoftAccepted`/`::loadProSuppressions` now have a real caller), `scanner/package.json` (registered the new test file in `test:report`), `bench/self-scan/BASELINE.json` (scanner/src 438→439, +1 for a genuine regex-heuristic false positive — `total === 0` matching the "quantity/amount/price/total compared to 0" pattern, same accepted class as prior cycles). **`scanner/dist/920.index.js` is now ALSO staged** (`git add`, not committed) — a fifth dist chunk, from `cmdTriage`'s new dynamic import, following the same D-0027 protocol as the other four. Verify with `git diff --cached --name-only` — it must show EXACTLY `scanner/dist/144.index.js`, `scanner/dist/552.index.js`, `scanner/dist/730.index.js`, `scanner/dist/736.index.js`, and `scanner/dist/920.index.js`, nothing else, and `git log -1` must still show `f57f496` (no new commit).

Modified cycle 57: `scanner/src/posture/legal-hold.js` (new — hold load/check/list/add/remove), `scanner/src/posture/retention-policy.js` (`findExpiredArtifacts` now skips held artifacts), `scanner/src/posture/artifact-registry.js` (registered `legal-holds.json` as operator-config), `scanner/bin/agentic-security.js` (`cmdReset`'s target-filtering now excludes held artifacts in BOTH modes; new `cmdLegalHold` function + `case 'legal-hold'` dispatch + USAGE text), `scanner/test/legal-hold.test.js` (new, 18 tests), `scanner/package.json` (registered it in `test:report`), `bench/self-scan/BASELINE.json` (scanner/src 437→438, +1 for the new module, same accepted pattern as every prior config-loader module). **`scanner/dist/144.index.js` is now ALSO staged** (`git add`, not committed) — a fourth dist chunk, from `cmdLegalHold`'s new dynamic import, following the same D-0027 protocol as the other three. Verify with `git diff --cached --name-only` — it must show EXACTLY `scanner/dist/144.index.js`, `scanner/dist/552.index.js`, `scanner/dist/730.index.js`, and `scanner/dist/736.index.js`, nothing else, and `git log -1` must still show `f57f496` (no new commit).

Modified cycle 56: `scanner/src/posture/state-lifecycle-report.js` (new — deletion/export report builders+writers), `scanner/src/posture/artifact-registry.js` (registered `deletion-report.json`/`export-report.json` as generated/evidence-class, updated header), `scanner/bin/agentic-security.js` (`cmdReset` now writes a deletion report and returns 1 on a real failure; new `cmdExport` function + `case 'export'` dispatch + USAGE text), `scanner/test/state-lifecycle-report.test.js` (new, 13 tests), `scanner/package.json` (registered it in `test:report`). **`scanner/dist/552.index.js` is now ALSO staged** (`git add`, not committed) — a third dist chunk, from `cmdExport`'s new dynamic imports, following the same D-0027 protocol as the other two. Verify with `git diff --cached --name-only` — it must show EXACTLY `scanner/dist/552.index.js`, `scanner/dist/730.index.js`, and `scanner/dist/736.index.js`, nothing else, and `git log -1` must still show `f57f496` (no new commit).

Modified cycle 55: `scanner/src/posture/risk-dollars.js` (FR-802's 5-input config gate + `scenario_organization_specific` status; FR-803's `assumptions`/`modelVersion`/`confidence`/`range`; FR-805's `scenarios` — all in one coherent change, no new files), `scanner/test/risk-scenario-disclosure.test.js` (+17 tests; 1 pre-existing assertion intentionally updated per D-0034/FR-802's precision, not weakened). No new dist chunk this cycle (checked via `git status --short scanner/dist/` after build — empty).

Modified cycle 54: `scanner/src/posture/artifact-registry.js` (added `retentionClass` field to ~30 entries + `retentionClassOf`/`listArtifactsWithRetentionClass` exports + `retention-policy.yml` operator-config registration), `scanner/src/posture/retention-policy.js` (new module), `scanner/bin/agentic-security.js` (new `cmdReset --expired` branch), `scanner/test/retention-policy.test.js` (new, 17 tests), `scanner/package.json` (registered it in `test:report`), `bench/self-scan/BASELINE.json` (scanner/src 436→437, +1 for the new module). **`scanner/dist/730.index.js` is now ALSO staged** (`git add`, not committed) — a second dist chunk, from `cmdReset`'s new dynamic `import('../src/posture/retention-policy.js')`, following the same D-0027 protocol as `736.index.js` below. Verify with `git diff --cached --name-only` — it must show EXACTLY `scanner/dist/730.index.js` and `scanner/dist/736.index.js`, nothing else, and `git log -1` must still show `f57f496` (no new commit).

Modified cycle 53: `scanner/src/posture/compliance-policy.js` (added `schemaVersion`/`statusSemantics`/`policySource` exported constants + fields), `scanner/src/posture/compliance-evidence-signing.js` (kept `EVIDENCE_FIELDS` allowlist in sync), `scanner/src/posture/state-dir.js` (exported `STATE_DIR_NAME`, D-0032), `scanner/test/compliance-evidence-binding.test.js` (+1 test), `scanner/test/compliance-evidence-signing.test.js` (+2 tests). Cycle 52: `scanner/src/posture/compliance-policy.js` (added `file-contains` verifier primitive; fixed a real pre-existing TOCTOU in `sca-policy-has-entry`), `scanner/test/compliance-file-contains.test.js` (new, 11 tests), `scanner/package.json` (registered it in `test:posture`), `bench/self-scan/BASELINE.json` (scanner/src 437→436, net improvement). Cycle 51: `scanner/src/posture/compliance-evidence-signing.js` (new), `scanner/src/posture/compliance-policy.js` (wired signing into `emitEvidenceJsonLd`), `scanner/bin/agentic-security.js` (extended `verify-attestation` to auto-detect ComplianceEvidence documents), `scanner/test/compliance-evidence-signing.test.js` (new, 13 tests), `scanner/package.json` (registered it in `test:posture`), `bench/self-scan/BASELINE.json` (scanner/src 436→437). Cycle 50: `scanner/src/posture/compliance-policy.js` (added `computeEvidenceDigest`/`_currentCommit`, `_staleness`/`_exceptionExpired` + DSL fields owner/reviewer/reviewed-at/review-interval-days/structured not-applicable, new `stale`/`gap` statuses), `scanner/test/compliance-evidence-binding.test.js` (new, 18 tests), `scanner/package.json` (registered it in `test:posture`). Cycle 49: `bench/independent/why-missed.mjs` (persists a committed summary), `bench/independent/why-missed-summary.json` (new, real 920-entry data), `scanner/src/posture/accuracy-scorecard.js` (+whyMissed committedInputs field + markdown section), `scripts/scorecard.mjs` (+whyMissed read), `scanner/test/why-missed.test.js` (+1), `scanner/test/accuracy-scorecard.test.js` (+3), `docs/SCORECARD.md`/`docs/scorecard.json` (regenerated for real via `npm run scorecard`). Cycle 48: `scanner/src/posture/fleet.js` (scanHealth/policyDrift + governance section), `scanner/src/posture/policy-bundle.js` (`computePolicyDrift`), `scanner/test/fleet.test.js` (+10). **`scanner/dist/736.index.js` remains STAGED (`git add`, not committed) — the one deliberate exception to "nothing staged/committed this session," required by `test/dist-chunks-tracked.test.js`'s own design (D-0027). Verify with `git diff --cached --name-only` — it must show ONLY that one file, and `git log -1` must still show `f57f496` (the session's starting HEAD, no new commit).** Earlier cycles (43-47): E10's FR-1002/1003/1004/1001 — see status.md for full per-cycle file lists if needed; not repeated here to keep this file navigable. Nothing has been committed or pushed this session.

### Verification state as of the last checkpoint (this cycle, 2026-08-26)

`npm test`: exit 0, clean (4722 pass / 0 fail aggregated across all suites, including `test:python` — 1 more than cycle 79, the new k8s-admission fault-injection test). `bench:cve-replay:check`: 215/215, zero drift. `bench:self-scan:check`: zero drift, `scanner/src` 447/447 unchanged. `npm run build`: clean, ZERO new untracked chunks. `npm run smoke`: exit 3 (expected).

Cycle 79's verification state (for reference): `npm test` exit 0 (4721 pass / 0 fail). Corpus 215/215 zero drift (both default and WITH AGENTIC_SECURITY_WORKER_CASCADE=1 genuinely enabled). Self-scan zero drift, scanner/src 447/447 unchanged. Build produced dist/265.index.js (new, staged per D-0027). Smoke exit 3.

`npm test`: exit 0, clean (4721 pass / 0 fail aggregated across all suites, including `test:python`). `bench:cve-replay:check`: 215/215, zero drift (default path). `AGENTIC_SECURITY_WORKER_CASCADE=1 npm run bench:cve-replay:check`: ALSO 215/215, zero drift — a real accuracy comparison WITH the opt-in path genuinely enabled, not just proving default-off is unchanged. `bench:self-scan:check`: zero drift, `scanner/src` 447/447 unchanged. `npm run build`: clean, one new chunk (`dist/265.index.js`, staged per D-0027). `npm run smoke`: exit 3 (expected). Manually verified the REAL BUNDLE (`dist/agentic-security.mjs`) produces identical findings with the flag on vs off, and a real `file-timeout` marker under an impossibly low per-file timeout — see D-0050.

Cycle 78's verification state (for reference): `npm test` exit 0 (4718 pass / 0 fail). Corpus 215/215 zero drift. Self-scan clean against a reviewed, updated baseline (scanner/src 445→447). Build clean, zero new untracked chunks. Smoke exit 3.

`npm test`: exit 0, clean (4718 pass / 0 fail aggregated across all suites, including `test:python` — 8 more than cycle 77, the new `cascade-worker-pool.test.js`). `bench:cve-replay:check`: 215/215, zero drift. `bench:self-scan:check`: clean against a reviewed, UPDATED baseline (`scanner/src` 445→447, +2 reviewed and accepted false positives — see D-0050). `npm run build`: clean, ZERO new untracked chunks. `npm run smoke`: exit 3 (expected).

Cycle 77's verification state (for reference): `npm test` exit 0 (4710 pass / 0 fail). Corpus 215/215 zero drift. Self-scan zero drift, scanner/src 445/445 unchanged. Build clean, zero new untracked chunks. Smoke exit 3.

`npm test`: exit 0, clean (4710 pass / 0 fail aggregated across all suites, including `test:python`). `bench:cve-replay:check`: 215/215, zero drift. `bench:self-scan:check`: zero drift, scanner/src 445/445 unchanged. `npm run build`: clean, ZERO new untracked chunks. `npm run smoke`: exit 3 (expected).

Cycle 76's verification state (for reference): `npm test` exit 0 (4710 pass / 0 fail). Corpus 215/215 zero drift. Self-scan zero drift, scanner/src 445/445 unchanged. Build clean, zero new untracked chunks. Smoke exit 3.

`npm test`: exit 0, clean (4710 pass / 0 fail aggregated across all suites, including `test:python`). `bench:cve-replay:check`: 215/215, zero drift. `bench:self-scan:check`: zero drift, scanner/src 445/445 unchanged. `npm run build`: clean, ZERO new untracked chunks. `npm run smoke`: exit 3 (expected).

Cycle 75's verification state (for reference): `npm test` exit 0 (4710 pass / 0 fail). Corpus 215/215 zero drift. Self-scan zero drift, scanner/src 445/445 unchanged. Build clean, zero new untracked chunks. Smoke exit 3.

`npm test`: exit 0, clean (4710 pass / 0 fail aggregated across all suites, including `test:python`). `bench:cve-replay:check`: 215/215, zero drift. `bench:self-scan:check`: zero drift, scanner/src 445/445 unchanged — the exact file that changed (`engine.js`) introduced zero new self-flagged findings, confirming the extraction is behavior-identical. `npm run build`: clean, ZERO new untracked chunks (`git status --short scanner/dist/` shows only the same six D-0027-staged chunks plus ordinary unstaged `M` on already-tracked chunks + `agentic-security.mjs`/`.sha256`, exactly as expected from an `engine.js` source change). `npm run smoke`: exit 3 (expected).

Cycle 74's verification state (for reference): `npm test` exit 0 (4682/4682 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection + 5/5 python). Corpus 215/215 zero drift. Self-scan zero drift, scanner/src 445/445 unchanged. Build clean, zero new untracked chunks. Smoke exit 3. `bench:privacy-recall:check`: PASS, 4/4 fixtures match the re-baselined baseline.

Cycle 73's verification state (for reference): `npm test` exit 0 (4671/4671 + 26/26 + 1/1 + 1/1 + 5/5). Corpus 215/215 zero drift (confirmed twice). Self-scan clean against the reviewed, updated baseline (scanner/src 445/445). Build clean, zero new untracked chunks. Smoke exit 3.

Cycle 72's verification state (for reference): `npm test` exit 0 (4668/4668 + 26/26 + 1/1 + 1/1 + 5/5). Corpus 215/215 zero drift (confirmed twice). Self-scan clean against the reviewed, updated baseline (scanner/src 444/444). Build clean, zero new untracked chunks. Smoke exit 3.

Cycle 71's verification state (for reference): `npm test` exit 0 (4665/4665 + 26/26 + 1/1 + 1/1 + 5/5 — unaffected by the new bench files). Corpus 215/215 zero drift. Self-scan zero drift (scanner/src 443/443). Build clean, zero new untracked chunks. Smoke exit 3. `bench:privacy-recall:check`: PASS, 3/3 fixtures match baseline — proven bidirectionally (fails exit 1 on injected drift, passes exit 0 when restored), zero stray `.agentic-security` state left behind after repeated runs.

Cycle 70's verification state (for reference): `npm test` exit 0 (4665/4665 + 26/26 + 1/1 + 1/1 + 5/5). Corpus 215/215 zero drift. Self-scan zero drift. Build clean, zero new untracked chunks. Smoke exit 3.

Cycle 69's verification state (for reference): `npm test` exit 0 (4662/4662 + 26/26 + 1/1 + 1/1 + 5/5). Corpus 215/215 zero drift — proves zero behavior change to the 6 deep-tier corpus entries despite touching `dataflow/index.js`. Self-scan zero drift (scanner/src 443/443). Build clean, zero new untracked chunks (new modules bundled into `dist/index.js` via real static imports, size grew 3876kB→3887kB as expected). Smoke exit 3.

Cycle 68's verification state (for reference): `npm test` exit 0 (4649/4649 + 26/26 + 1/1 + 1/1 + 5/5). Corpus 215/215 zero drift. Self-scan zero drift (scanner/src 443/443 — `matchPrivacySink` introduced zero self-flagged patterns). Build clean, zero new untracked chunks. Smoke exit 3.

Cycle 67's verification state (for reference): `npm test` exit 0, clean on the FIRST attempt (4643/4643 main suite + 26/26 + 1/1 + 1/1 + 5/5). Corpus 215/215 zero drift. Self-scan zero drift (scanner/src 443/443 — two new unwired modules introduced zero self-flagged patterns). Build clean, zero new untracked chunks. Smoke exit 3.

Cycle 66's verification state (for reference): `npm test` exit 0, clean on the FIRST attempt of the FINAL (post-full-migration) run (4637/4637 main suite + 26/26 cpp-dataflow + 1/1 fault-injection[annotator] + 1/1 detector-fault-injection[new] + 5/5 python). Every one of the 5 intermediate migration batches also ran the full gate clean before that final run. Corpus 215/215 zero drift every batch. Self-scan zero drift every batch (scanner/src 443/443 — wrapping existing calls in `runDetector` introduced zero new self-flagged patterns). Build clean, zero new untracked chunks. Smoke exit 3.

**Reminder from cycle 49 — D-0030:** a full-gate `npm test` run once hung for over 2 hours on an unrelated test file. If a future cycle's `npm test` appears to be taking far longer than ~10 minutes, check `ps -o pid,ppid,%cpu,etime,command` for a flat-CPU (not just long-elapsed) stuck child BEFORE assuming it is still progressing — do not let a suspected hang run for hours.

**If resuming cold, your first action should be:** `git status` to confirm it matches the above, then `cd scanner && npm test` and read the actual output — ALWAYS `cd scanner` first; running `npm run <anything>` from the repo root fails with an ENOENT against your home directory (no package.json there), hit twice this session already. Before running any fixture-scanning tests or benchmarks, read decisions D-0009 AND D-0015 IN FULL — wipe `.agentic-security/` dirs under `test/fixtures` EXCLUDING `custom-rules`/`sca-suppression`, then **immediately** check `git status --short . | grep '^ D'` and restore anything with `git checkout --`. `bench/cve-replay` lives at the REPO ROOT, not under `scanner/`. Read D-0010 before timing-dependent tests, D-0011 before annotator-closure scoping, D-0012 (and D-0022) before any new optional-config reader, D-0013 before toggling a global flag, D-0014 before any "verify mature infrastructure" cycle, D-0016 before positional-backreference regexes, D-0018 before adding any new `statePath()`/`stateDir()` caller, D-0019 before state-file prose naming destructive operations, D-0020 before assuming partial satisfaction of a multi-part acceptance criterion is the whole thing, D-0021 before a second `mock.module()` scenario in one test file, D-0023 before inventing a new notion of "verified identity," D-0024 before trusting any "proven end-to-end" claim without checking it reached the real entry point, D-0025 before assuming a per-finding "exception" and a per-CI-check "waiver" are the same mechanism, D-0026 before writing or copying any Ed25519-signed-bundle canonicalisation shape (copy the unknown-key REJECTION, not just the allowlist), D-0027 before trusting a green `npm test` as proof `dist/` is fully consistent (check `git status --short scanner/dist/` after a build), D-0028 before attempting any part of E2 (FR-201's detector loop has ~150-200 individual call sites — decompose into small, gate-verified batches, never one mechanical pass), D-0029 before assuming which compliance module ("compliance-policy.js" vs "auditor-walkthrough.js") a new E5 requirement targets, D-0030 before assuming a stuck-looking `npm test` is just slow, D-0031 before assuming a signature over one existing digest (FR-504's `evidenceDigest`) covers everything a BROADER signing requirement (FR-505) needs, D-0032 before writing ANY `.agentic-security` string literal in `src/`/`bin/` for ANY reason (even pure display, not a real file operation) — import `STATE_DIR_NAME` from `state-dir.js` instead; the seam guard does not distinguish the two and an allowlist exemption is explicitly the wrong fix. D-0033 before touching E7's remaining FRs — FR-702 is purge-only by design, with "archive" deliberately deferred to FR-706 (do not re-litigate that scoping inside FR-706 as if it were undecided; it is the intended split), and any operator-configurable TTL must keep the default/max-ceiling shape (an override can only shorten retention, never lengthen it past a built-in ceiling). D-0034 before touching `risk-dollars.js` again — `range` and `scenarios` are the SAME computation (`SCENARIO_MULTIPLIERS`), not two independent uncertainty models; extend that one table rather than adding a second, differently-scoped confidence-interval concept, and keep the multipliers scoped to ONLY probability/impact (generic assumptions), never reachability discount or confidence weight (measured per-finding facts). D-0035 before re-litigating the "S7 no prior scan" combined-run failure from scratch if it recurs — check D-0004/D-0006/D-0009 first, confirm standalone-clean with a quick re-run of `compliance-command.test.js`/`labs-command.test.js`/`posture-command.test.js`, and move on; a 4th+ full-gate re-run chasing this specific flake to zero has never once found a real bug. D-0036 before adding any future deletion path over the artifact registry — the legal-hold guarantee is enforced at the CALLER (`cmdReset`), not centralized inside `retention-policy.js` alone, so a new deletion path must explicitly consult `isUnderHold` itself; and never reclassify `legal-holds.json` as 'generated' for consistency with `baseline.json` — the two have inverted deletion-safety requirements. D-0037 before extending `production-feedback.js` to a 6th source — classify whether that source has a real per-entry creation date FIRST; if not, its events go through `at: null` into the disclosed `undated` bucket, never a guessed/fabricated timestamp. D-0038 before building any cross-installation calibration aggregation — that is a SEPARATE, much larger feature (hosted collection, consent, a real privacy review), not a natural extension of `calibration-feedback.js`, which is deliberately scoped to one installation's own local data per this codebase's "no SaaS control plane" architecture. D-0039 before attempting FR-403 — the sink-category gap (2 missing) is bounded, but the flow-shape gap (aliased/interprocedural/cross-file + safe-transformation) needs the general engine's catalog/SummaryCache/callGraph machinery wired in, a genuine architectural rework; do not conflate the two or force-close on the bounded half alone. D-0040 before extending FR-705's confidentiality coverage — follow the same one-artifact-at-a-time, own-tests, own-gate-run pattern; do NOT attempt `egress-audit.log` (hash-chain design question), `last-scan.json`/`findings.json` (dozens of plain-JSON readers), or the `engine.js`-internal writers without resolving each one's specific named blocker first. D-0041 before touching FR-403's implementation — follow its concrete 4-step order (catalog sink family → catalog declared-as source matcher → opt-in CFG-walk wiring with a before/after recall comparison → default-path cutover reusing the existing `irBacked` flag); step 1 is genuinely zero-risk and worth doing alone; step 4 is a one-way door on the default scan path and must never be bundled with step 3. D-0042 before EVER merging `dataflow/privacy-catalog.js`'s `PRIVACY_SINK_CATALOG` into `catalog.js`'s shared `CATALOG` export — `dataflow/engine.js`'s taint state is a shared boolean with no per-family filtering, so a merge would make every already-active general source trigger spurious privacy findings at ordinary log/fetch/email calls; step 3's CFG-walk wiring MUST consult the privacy catalog as a genuinely isolated lattice, never by merging the two arrays. D-0043 before touching `src/pipeline/detector-runner.js` — it is deliberately SYNCHRONOUS, never wrap `runDetector` calls in `await`/a Promise chain when migrating the real ~128 detector call sites in step (b); error records use the field name `analyzer`, not `detector`, matching the PRD's own FR-201/FR-203 vocabulary. D-0044 before writing a mechanical multi-site transform script again — verify EVERY `old_string`'s uniqueness via `content.count()==1` before replacing, print the matched-vs-intended-names diff before touching anything, and hand-edit (never mechanically batch) a call site whose guard/behavior should change as part of the migration, not just its syntax. D-0045 before touching `src/pipeline/scan-health.js`'s `analyzers`/`conditions`/`status` fields — that ledger is FR-203's scope, not FR-201's; FR-206 (which owns `scanHealth.status`) is already `verified`, so feeding `_detectorErrors` into `conditions` would change already-verified behavior and must be a deliberate FR-203/FR-206 decision, not an incidental side effect of a different FR's cycle. D-0046 before touching FR-202 — both existing timeout mechanisms (`_perFileTimeoutMs`, `AGENTIC_SECURITY_DEEP_TIMEOUT_MS`/`deadlineMs`) are non-preemptive (post-hoc measurement / cooperative clock-poll respectively), so a real fix needs an actual `worker_threads` (or subprocess) boundary — follow the 4-phase order (isolated `runWithDeadline` primitive → work-unit data-contract design, auditing ALL 128 detectors for side-channel globals like `_suppressionLog`, not just the 5 sampled so far → real opt-in per-file wiring with a full accuracy/determinism/self-scan/corpus comparison → default-cutover, which is explicitly OUT OF SCOPE for the acceptance criterion and should not be attempted). `src/engine-parallel.js` is dead, unwired scaffolding — a design sketch, not something to build on top of as if it already worked. D-0047 before touching FR-403 step 3 — build a GENUINELY SEPARATE walker module (new file, own CFG traversal, own entry-state Set), never thread a second Set through `dataflow/engine.js`'s `step`/`exprTaint` by changing their state-parameter shape; those two functions are the general taint engine's hottest, most heavily-relied-upon code and must not be touched to add an orthogonal, still-opt-in privacy capability. `matchPrivacySink` (privacy-catalog.js) already exists as the sink-matching primitive the new walker should call — do not duplicate it. Scope v1 to intra-procedural only (no SummaryCache/higher-order/k-CFA) and build a real before/after recall-comparison harness (new, not `bench/layer-recall`) before ever gating a default-path change. **D-0047's walker was BUILT in cycle 69** (`dataflow/privacy-deep-walker.js`, wired into `dataflow/index.js` behind `AGENTIC_SECURITY_PRIVACY_DEEP=1`) — this note now describes the DESIGN CONSTRAINT for extending it (aliasing/interprocedural/cross-file/safe-transforms), not a not-yet-started task. D-0048 before touching FR-202 phase 3 (real per-file worker wiring) — a full 128-detector audit found 2 more broadcast-once candidates (`_projectIndex`, `SOURCE_PATTERNS`/`SINK_PATTERNS`/`SANITIZER_PATTERNS`) beyond `_GLOBAL_JAVA_TAINTED_METHODS`, corrected the `_suppressionLog` writer count down to 4 real in-loop detectors (not "dozens" — the rest are post-loop orchestration), confirmed no order dependency between detectors, and found a genuine PREREQUISITE bug: `src/sast/cpp.js`'s `_entropyMod` is an async module-load-time singleton that races unsafely under a worker-per-file design (fine today only because of single-process timing luck) — fix this FIRST, independently, before phase 3's wiring lands. D-0049 before touching FR-202 phase 3 again — phase 3 is split into 3a (DONE, cycle 75: the per-file cascade returns a self-contained delta instead of mutating shared arrays, matching `_ckptRecord`'s existing checkpoint payload shape) and 3b (NOT started: actual worker dispatch of that cascade, `_suppressionLog` output converted to a return value, checkpoint mark/slice adapted to a worker-returned delta). Do not attempt 3b by threading worker calls into the OLD closure-mutating shape — it no longer exists; build on `_runFileCascade`'s return-value contract. The checkpoint/resume mark-and-slice mechanism (`_mk`/`_ckptRecord`/`_ckptReplay`) is a real constraint on worker design that D-0046/D-0048 did not surface — read D-0049 in full before designing 3b's worker-result-to-checkpoint adaptation. **D-0049 update (cycle 77): `_runFileCascade` is now a top-level, exported, zero-closure-dependency function** (`export function _runFileCascade(p, c, scanRoot, _detectorErrors)`), the prerequisite for a worker to import and call it at all. Every data-shape blocker for phase 3b is now resolved — what remains is purely the worker-pool dispatch design itself (pool sizing, `workerData` broadcast, checkpoint adaptation), not any further refactoring of the cascade. **D-0050 (cycle 78) before touching phase 3b's dispatch design again:** the worker-pool primitive is BUILT (`src/pipeline/cascade-worker-pool.js`/`cascade-worker.js`) — a REUSED pool of long-lived workers with one-time setup, never one-shot-per-file (that would be O(n²) across a scan, since whole-project setup — `_buildProjectIndex`/`_buildGlobalJavaTaintedMethodIndex`/`_loadCustomRules` — would redo per file). Tested standalone via a synthetic fixture, zero `engine.js` wiring yet. Also: if a test ever passes a `scanRoot` that is not a REAL, existing directory, `state-dir.js`'s `resolveProjectRoot()` silently falls through to `process.cwd()` and can leak real state into the actual repo — always wrap such a test's scan call in `AGENTIC_SECURITY_NO_STATE=1`/`finally{delete...}` (the established idiom in `test/ci.test.js`/`test/fix-metrics.test.js`/`test/no-stray-state.test.js`/`test/streak-cli-wiring.test.js`, now also `test/sca-route-reachable.test.js`). **D-0050 update (cycle 79): FR-202 is now `verified` in full** — the pool is wired into `runFullScan`'s real loop behind `AGENTIC_SECURITY_WORKER_CASCADE=1`. **The critical lesson from this final step: ALWAYS manually verify a new worker/dynamic-import capability against the REAL SHIPPED BUNDLE (`dist/agentic-security.mjs`), not just `src/`** — the `src/`-only test suite passed completely while the real bundle silently produced ZERO findings, because `fileURLToPath(import.meta.url)` resolves to the CLI entrypoint bundle once bundled, not to a re-importable engine module. Any future worker/dynamic-import wiring in this codebase must re-derive its target module path via a bundled-vs-unbundled check (see `_resolveEngineModulePathForWorkers()`/`_resolveDefaultWorkerScript()` for the pattern), never assume `import.meta.url` inside the target module reliably points at itself once ncc has inlined it elsewhere. D-0051 before touching FR-203 — the coverage ledger is grounded but NOT built: `_runFileCascade`'s ~130 detectors split into ~119 always-applicable, 2 file-extension-gated (`scanRegexReDoS`, `scanGraphQL`), and 9 env-var-policy-gated (mapping to PRD §10.2's `skipped_by_policy`) — build `computeCoverageLedger(...)` as an isolated pure function over `_detectorErrors`/timeout markers/the gate table, NOT a refactor of every detector into a full `AnalyzerResult` object (FR-201 itself never did that either, and the acceptance criterion doesn't require it). D-0051 also documents a real FR-201 gap found and fixed in the same cycle: `scanWeb3Advanced`/`scanK8sAdmission` were unwrapped call sites the original migration's grep-count check couldn't distinguish from wrapped ones — verify completeness of any future call-site audit by the ABSENCE of the old pattern, not just a count match on the new one.

**Next requirement: ONLY 5 REMAIN, ALL IN EPIC E2.** FR-403 verified in cycle 74 (closing Epic E4) and FR-202 verified in cycle 79 — 9 of 10 epics (E1, E3, E4, E5, E6, E7, E8, E9, E10) are now genuinely, verifiably fully closed, and E2 is at 2/8. **E2 is the only epic left open**, with 5 requirements: FR-203, FR-204, FR-205, FR-207, FR-208.

- **FR-202 is `verified` as of cycle 79** — see the MAJOR MILESTONE bullet above for full detail.
- **FR-203 is grounded (cycle 80, D-0051) but NOT implemented.** Build `computeCoverageLedger(...)` as an isolated, pure, tested module (new file, e.g. `src/pipeline/coverage-ledger.js`) using the applicability table already grounded: ~119 always-applicable detectors, 2 file-extension-gated, 9 env-var-policy-gated (→ `skipped_by_policy`). Consume `_detectorErrors` (→ `failed`), the file-timeout marker (→ `timed_out` for every analyzer on that file, since a preemptive worker kill loses ALL in-flight analyzer state, not just one), and "ran without throwing, file didn't time out" (→ `completed`). Test standalone first, then wire into `scan-health.js`'s currently-`null` `analyzers` field. Full accuracy/self-scan/corpus comparison before calling this verified — a wrong coverage claim is a false-assurance regression.
- **E2's FR-204/205/207/208**: all still `pending`, none started, all downstream of FR-202/FR-203 to varying degrees. **Read each one's own literal PRD text fresh before starting** (per D-0014's three-step method) — a title-based scope assumption has been wrong before this session (D-0008, D-0020).

**Read each of the 5 remaining FRs' literal PRD acceptance criteria fresh before starting any of them — none has been grounded yet.**

- General reminder: this session has now found real, fixable TOCTOU bugs via the advisory-scan hook on 5+ separate occasions (D-0012, D-0022, FR-503's `sca-policy-has-entry` fix, FR-806's `calibration-feedback.js` fix) — when the hook flags PRE-EXISTING code adjacent to whatever you are already editing, fix it in the same cycle if the cost is low and add the test coverage that was probably also missing, rather than just baselining around it. Similarly, `test/no-stray-state.test.js` has now caught a real seam violation once (D-0032) — expect it to fire again on any future field that references a state-dir-relative path, even for display purposes.

**STANDING INSTRUCTION (still in force): the user has said "keep going until the PRD is fully completed."** Do not stop to ask whether to continue — proceed through the bounded loop (max 100 cycles) autonomously, checkpointing all four state files every cycle as this session has done throughout, until every FR-* has a justified terminal status or a genuine blocker (not just "this is large") is hit. The safety boundaries from the original driving prompt remain unconditionally in force regardless of this instruction: no commits, no pushes, no destructive git ops, no weakening a gate/test to force a pass, never mark a blocked requirement complete.

This session's overall pattern across 50 cycles: roughly 60% of investigated FRs turned out to have existing infrastructure needing evidence/a small connecting piece; the rest needed genuinely new, bounded capability built fresh (FR-1001's signing, FR-504/506's digest/staleness). Always read the actual PRD acceptance criterion, trace EVERY real caller path, and re-verify a prior cycle's `dependsOn` against real code (D-0008) rather than trusting it as written. Do not force-close a multi-part requirement when only some of its named parts are satisfied (D-0020) — but DO keep whatever real, valuable fixes were found along the way, even when the epic-level box cannot yet be checked. Do not attempt a large, structurally-coupled epic (E2) in an unplanned cycle just because "keep going" is standing (D-0028) — deferring with a clear, evidence-based reason IS keeping going, on everything else.

No FR selected yet for the next cycle — that decision belongs to the next Orient step. Whichever is picked: apply D-0014's three-step method before concluding a requirement is either "done" or "needs new implementation."

## Prior state (as of cycle 1, 2026-08-23)

- Repository: clean working tree, branch `main`, HEAD `f57f496`, scanner version `0.143.0`.
- PRD fully read (66 `FR-*` requirements across 10 epics, 4 delivery milestones + cross-cutting evaluation epic E9).
- State files created under `docs/implementation/`. No FR yet implemented.
- A background research agent (Explore) was dispatched to re-verify the PRD's §2.1 evidence table (A-01..A-12) against current HEAD, since the PRD's line-number citations target an older commit (`248c4cf`, v0.141.0) and the repo has since moved to v0.143.0. Its findings determine exactly where Milestone 0 code lands in `engine.js` and related files.

## Exact next command / action

1. Read the verification agent's findings (it will arrive as a task notification; if resuming cold, check for any summary already recorded in this file below "## Verification findings").
2. Begin Milestone 0 concretely:
   - Draft `scanner/src/pipeline/finding-schema.js`: versioned finding envelope per PRD §10.1, additive only.
   - Add additive `scanHealth` fields to the top-level scan result per PRD §10.3, without changing default gate/exit behavior (Milestone 0 exit criterion: "current behavior is observable and contracts are approved," not "gates changed").
   - Run `cd scanner && npm run test:smoke` after any engine.js touch to confirm no regression.
3. Update `assurance-hardening-state.json` (FR-105, FR-201..FR-206 status) and this handoff file at the end of the cycle.

## Verification findings

(Populated once the background agent — agentId not persisted here per instructions — returns. If this file is being read and that section is still empty, the agent may have been lost; re-dispatch a fresh read-only Explore pass over the same 11 items listed in `assurance-hardening-status.md`'s "This cycle" section.)

## Safety / process reminders for whoever resumes

- Never mark an FR `verified` without implementation + passing tests + acceptance criteria + docs + evidence + no known bypass (per the driving prompt's rules).
- Do not push, publish, or create PRs — this work stays as a local reviewable diff unless explicitly authorized.
- Do not touch `bench/cve-replay/corpus-baseline.json` expected verdicts or any benchmark answer keys.
- Run `cd scanner && npm run build` after any `src/` change before trusting `dist/agentic-security.mjs` behavior.
- Full gate before any push (not authorized yet in this session): `npm test`, `npm run bench:cve-replay:check`, `npm run bench:self-scan:check`.
- No background process should be left running between cycles — confirm before ending a session.

## Is the repo currently safe to hand off?

**RELEASE COMPLETE.** v0.144.0 is tagged, pushed, and published to npm with provenance (`@clear-capabilities/agentic-security-scanner@0.144.0`, SLSA provenance attestation confirmed via `npm view ... dist.attestations`). `main` is at commit `370c27e` and origin is fully up to date — nothing is staged, nothing is pending. Three real, genuine issues were found and fixed AFTER the initial release commit, before anything was published, all pushed as their own follow-up commits with hosted CI re-verified green each time:

1. **A real CI-only test failure** (`test/privacy-ir-adapter.test.js`): two tests requested deep mode via the `{deep:true}` runScan option and asserted it stayed enabled, which held locally but failed on the hosted runner — `engine.js` downgrades deep mode under `CI=true` regardless of which mechanism requested it, and `test/ci-parity.test.js`'s static checker incorrectly treats the `{deep:true}` option shape as exempt from needing the `deepInCi` opt-in. Fixed the two tests directly; the checker's incorrect exemption is a documented, NOT-yet-fixed gap (~40 other test files use the same option shape and have not been audited for the same exposure).
2. **A real bundle-reproducibility bug**: `ci.yml` used bare `npm install` (re-resolves against the live registry every run) instead of `npm ci` (lockfile-exact) for 5 of its jobs — `release.yml` already used `npm ci` correctly. This let the committed bundle silently drift from what a fresh install would produce. Fixed by switching all 5 `ci.yml` install steps to `npm ci`, and by rebuilding the bundle from a verified clean-room `npm ci` (two independent clean rebuilds matched each other byte-for-byte; neither matched the previously-committed bundle, proving the earlier one was built from a slightly-inconsistent local `node_modules` state).
3. The scorecard was regenerated a final time once the corrected bundle's SHA was known, so `docs/scorecard.json`'s `bundleSha256` genuinely describes what shipped.

Original context below (superseded — kept for the historical record of this cycle):

Partially, as of cycle 88 (2026-08-26) — the PRD work itself is fully done and gate-clean, but a RELEASE IS MID-FLIGHT and must not be tagged/pushed from this exact state. `git diff --cached --name-only` shows exactly the SAME TWENTY staged `scanner/dist/` files as cycles 85/86/87 (the 17 index chunks, `agentic-security.mjs`, `agentic-security.mjs.sha256`, `calibration-seed.json`) and nothing else — no new chunk this cycle, since the types wiring is purely additive statically-imported code. Verified the rebuilt bundle contains `_extractTsTypes`/`_tsTypeToString`, and the full FR-401 test suite (40 tests) passes against it. **BUT**: `scanner/package.json` and the other 5 `VERSION_FILES` are bumped to 0.144.0 and `CHANGELOG.md` has a `## 0.144.0` entry — release prep that is NOT YET COMPLETE. The one full `npm run release:check` run so far (before this cycle's FR-401 work) found 3 real issues, none yet fixed: (1) `scanner/expected-package-manifest.json`'s fileCount is stale against this session's ~90 new files, (2) `npm test` exited 1 on that run (very likely the same pre-existing "no prior scan" S7 flake documented at D-0030 across nearly every cycle this session, but NOT re-confirmed since), (3) `@types/node` and `js-yaml` are behind their latest published versions (need an upgrade or a documented `.dependency-holds.json` entry). **Before tagging `v0.144.0` and pushing: fix or hold all three, then re-run `npm run release:check` (no `--fast`) to a clean exit, then commit everything, `git tag v0.144.0`, and push both** (this repo's `git push` needs `-c credential.helper='!gh auth git-credential'` — osxkeychain fails on this machine). Everything else under "Exact current git status" above (the large accumulated `M`/`??` set spanning many prior cycles' work) is unchanged from before this cycle. `git log -1` still shows `f57f496` (the session's starting HEAD, no new commit). No process is running. Nothing has been committed or pushed.
